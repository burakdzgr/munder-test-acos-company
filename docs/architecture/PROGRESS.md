# PROGRESS

İlerleme kaydı — görevler 35-CLAUDE-CODE-HANDOFF.md §9 (T01–T50) sırasına göre işaretlenir.

| Görev | Durum | Tarih | Not |
|---|---|---|---|
| T01 Scaffold monorepo | ✅ Tamamlandı | 2026-08-11 | pnpm 9.15.9 (corepack) + Turborepo; 13 workspace (`@acos/*`) exports map + composite tsconfig (references = §4 bağımlılık matrisi) + trivial index + 1'er geçen Vitest testi; kök turbo.json 28 §6 task grafiği; tsconfig.base.json strict bayrakları; eslint.config.mjs flat + ADR-004 agent-framework yasağı (`no-restricted-imports`); .env.example 28 §7 anahtarları Mode-B localhost defaultları; infrastructure/docker/compose.yaml (postgres pgvector:pg16, nats 2.10 `-js`, temporal auto-setup namespace=acos, temporal-ui) — 4 servis healthy doğrulandı, `acos` namespace kayıtlı. `pnpm install && build && lint && typecheck && test` 13 workspace'te yeşil. Not: nats healthcheck için `nats:2.10-alpine` varyantı kullanıldı (scratch imajda shell yok); boundaries/check-deps/CI T02'de. |
| T02 Boundary enforcement | ✅ Tamamlandı | 2026-08-11 | Üç bağımsız net: (1) eslint-plugin-boundaries v7 (13 element tipi, 35 §4 matrisi `policies` olarak; ui→contracts yalnızca `dependency.kind=type`; app→app yapısal olarak deny) + `import/no-internal-modules` (deep-import yasağı; izinli subpath'ler ilgili görevlerde eklenecek) + `no-restricted-imports` agent-framework yasağı; specifier çözümü build gerektirmesin diye resolver-only `tsconfig.eslint.json` (kaynak dosyalara paths map). (2) `scripts/check-deps.ts`: manifest ⊆ matris, Redis istemcileri + agent framework kara listesi (ADR-004/006), 13 workspace varlık kontrolü. (3) TS project references — check-deps `references == izinli bağımlılıklar` eşitliğini yapısal doğruluyor. Kabul: `scripts/*.test.ts` fixture süitleri (16 test) — her nette kasıtlı ihlal error üretiyor, temiz manifest/import geçiyor; gerçek repo her iki netten temiz. Kök scriptler: `pnpm check-deps`, `pnpm test:repo` (T06'da CI aşamalarına bağlanacak). |
| T03 packages/config | ✅ Tamamlandı | 2026-08-11 | `loadConfig(processEnv)`: 27 §13'teki tüm anahtarlar Zod (v4) şemasıyla; zorunlular (DATABASE_URL, NATS_URL, TEMPORAL_ADDRESS, MASTER_KEY 32-byte base64, SESSION_SECRET, INTERNAL_API_TOKEN) eksikse TÜM sorunları adlandırıp listeleyen `ConfigError`; `loadConfigOrExit` yazdırıp exit 1 (IO enjekte edilebilir, testli). Boş string = unset (opsiyoneller); hiç LLM sağlayıcı yoksa `llm.offlineProfile=true` (A3). Sabitler: `TASK_QUEUES` (agent-tasks/execution/memory/intake), `NATS_SUBJECT_PREFIX="co."`, `DEFAULT_BUDGETS.companyDailyCents=5000`. 11 test yeşil. `.env.example` 27 §13 ile hizalandı (EGRESS_PROXY_URL=egress-proxy DNS, OTEL boş⇒no-op). |
| T04 Compose infra services | ✅ Tamamlandı | 2026-08-11 | Temel compose.yaml T01'de: postgres (pgvector:pg16, `${DATA_DIR:-./data}/postgres`, pg_isready), nats (2.10 `-js`, `-m 8222` + healthz), temporal (auto-setup 1.25.2, ayrı `temporal`/`temporal_visibility` DB'leri, namespace `acos`, cluster-health healthcheck), temporal-ui (2.31.2). Bu görevde `compose.dev.yaml` overlay'i eklendi (şimdilik boş; app servislerinin `develop.watch` konfigi T05'te). Kabul: `up -d postgres nats temporal temporal-ui` → 4/4 healthy (iki kez doğrulandı), overlay `config --quiet` geçiyor. |
| T05 App Dockerfiles + full compose | ✅ Tamamlandı | 2026-08-11 | Stub'lar: server (Fastify, /healthz, gerçek boot T15'te), sandbox-manager (Fastify, 3010), agent-worker/execution-worker (node:http /healthz 3020/3021, Temporal kaydı T31/T40'ta), web (React 19 + Vite hello-world, gerçek shell T20'de). `Dockerfile.node` (paylaşımlı, APP_FILTER/APP_MAIN arg'lı) + `Dockerfile.web` (build stage → nginx prod stage; nginx `/api`+`/ws` proxy + SPA fallback + /healthz). compose.yaml'a 5 app servisi eklendi (S1: docker sock yalnız sandbox-manager'da); compose.dev.yaml Mode A: `develop.watch` sync + tsx watch/vite dev, web dev'de `ACOS_API_PROXY_TARGET=http://server:3000`. Kabul: tam `docker compose up` → 9/9 healthy; web SPA + /api proxy'si doğrulandı; Mode B (`pnpm turbo dev`) 5 servis host'ta ayakta. Not: bu makinede 3000 portu başka bir uygulamada — host mapping `${SERVER_PORT:-3000}:3000` ile override edilebilir (test SERVER_PORT=3100 ile koşuldu); egress-proxy T08'de. |
| T06 CI pipeline | ✅ Tamamlandı | 2026-08-11 | GitHub repo `burakdzgr/agent-company-os` (private) + Actions `ci.yml` (32 §9 aşamaları): check-deps + boundary fixture'ları → lint+typecheck → unit (coverage kapıları; domain ≥%90 satır/dal CI-zorunlu) → integration (T07'ye kadar no-op) → e2e-smoke (runner'da tam `compose up --build --wait` + healthz/SPA/proxy probları). PR'larda turbo-affected (`--filter=[origin/main]`). Kabul: main run 31441064311 **success** (3m37s, e2e-smoke dahil); kanıt PR #1'deki kasıtlı başarısız test Stage 2'de 27s'de kırmızı (run 31441114302), merge edilmeden kapatıldı. Not: branch protection (merge'ü zorla bloklama) private repoda ücretsiz planda kapalı (403 "Upgrade to GitHub Pro or make public") — repo public yapılırsa veya plan yükseltilirse `checks`+`e2e-smoke` zorunlu check olarak bağlanacak; şimdilik kırmızı check görünür ama merge teknik olarak engellenmiyor. |
| T07 Testcontainers harness | ✅ Tamamlandı | 2026-08-11 | `test/containers.ts` (repo kökü) paylaşılan helper'lar: `startPostgres` (pgvector/pgvector:pg16), `startNats` (nats:2.10-alpine `-js`, /healthz wait), `startTemporal` (temporalio/temporal:1.8.2 `server start-dev --namespace acos`); imaj sürümleri tek yerden pin'li. Int testler 32 §12 düzeninde (`test/integration/*.int.test.ts`): `packages/db` PG süiti (PostgreSQL 16 + pgvector benzerlik sorgusu), `workers/agent-worker` Temporal süiti (gRPC getSystemInfo + `acos` namespace, retry'lı connect). db/agent-worker unit script'i `vitest run src`'ye ayrıldı (int testler unit koşumuna karışmaz); kök `pnpm test:int` → `turbo run test:integration` (CI Stage 3 artık gerçek). Yerel: 4/4 test yeşil. Ayrıca: repo public yapıldı, branch protection bağlandı (`checks`+`e2e-smoke` zorunlu + strict; admin bypass açık — solo doğrudan-push akışı için). |
| T08 Egress proxy + workspace network | ✅ Tamamlandı | 2026-08-11 | `infrastructure/docker/egress-proxy/`: squid 6.13 (ubuntu/squid) + 27 §12'deki allowlist konfigi birebir (default deny, `access_log stdio:/dev/stdout`; squid'in `proxy` kullanıcısı root'a ait stdout pipe'ına yazabilsin diye entrypoint'te `chmod a+w /dev/stdout`). compose: `acos-workspaces` network'ü `internal: true` + subnet 172.30.0.0/16 (squid ACL ile eşleşik); egress-proxy ve sandbox-manager hem default hem workspace network'üne bağlı; healthcheck `squid -k check`. `workspace.egress.denied` plumbing stub: sandbox-manager'da `parseSquidAccessLine` (denied/host/client çıkarımı, 4 unit test; event emisyonu T37'de). Kabul: workspace network'ündeki container proxy üzerinden registry.npmjs.org'a 200; example.com CONNECT 403 TCP_DENIED (stdout'ta loglu); proxy'siz direkt çıkış imkânsız (route/DNS yok). |
| T09 packages/domain core | ✅ Tamamlandı | 2026-08-11 | Sıfır runtime bağımlılık (check-deps doğruluyor; UUIDv7 elle, `globalThis.crypto`, zaman/rastgelelik enjekte edilebilir — RFC 9562 layout + timestamp çıkarımı). Value object'ler: `Money` (integer minor units, para birimi uyum guard'ları), `RiskClass` R0–R3 + `riskAtMost`, `TaskRisk`, `ResourceScope`, `AutonomyLevel` 0–5, `Seniority` + sıralama + `requiresPromotionReview`. Entity + factory'ler (03 §4 sketch'lerine birebir): Company, OrgUnit, Position, OrgEdge (tam-bir-hedef CHECK'i, kind-hedef uyumu, self-report reddi, end-date-asla-silme), Agent (kutsal invariant: model/provider alanı YOK; draft başlar; EMP-007 formatı), Task (16 kanonik durum enum'u — geçiş tablosu T10'da; TASK-81 formatı), Project (+`taskBranchName` slug'lama, Türkçe karakter normalizasyonu), Memory (scope/scopeRef tutarlılığı, [0,1] aralıkları, zorunlu provenance), Approval (yapılandırılmış brief zorunlu, pending başlar). 42 unit test; coverage %100 satır / ≥%90 dal (CI-zorunlu eşik). |
| T10 Domain state machines + policies | ✅ Tamamlandı | 2026-08-11 | `state-machines/`: generic `defineStateMachine` (data + canTransition/assertTransition/terminal) + 8 kanonik makine — task (07 §4 mermaid'e birebir), agent, agent-session, project, approval, workspace, experiment, memory. `authorizeTaskTransition`: 07 §5 izin matrisi satır satır; author==reviewer/QA yapısal red. `policies/`: `maxRisk(level, scopeRelation)` (06 §1 tablosu) + `authorize()` (06 §2'nin 8 adımı sırayla: S6 founder-only → grant/constraint → policy deny → bütçe hard-deny/soft-escalate → cap aşımı L0 deny/L1 manager/risk-scope'a göre approver → R3 standing-grant → S5 dış-etki incelemesi); delegasyon (derinlik ≤5, reassign <3, %20 rezervli pro-rata bütçe bölüşümü — toplam korunumu testli); 6 runaway guard (bütçe, deadline, step cap 40/50, loop dedektörü FNV-1a + arg normalizasyonu [lowercase/timestamp/uuid-strip], ping-pong sayacı >8, delegasyon derinliği); memory kuralları (0.55/0.2/0.15/0.1 retrieval skoru, ≥3 evidence×≥2 task agent→project, ≥2 proje+manager onayı project→company, tek olay asla company-scope); org-forest (cycle check, ≤1 aktif yönetici, escalation chain — Founder sanal düğüm hariç). Subpath exports: `@acos/domain/state-machines`, `@acos/domain/policies` (28 §2); `no-internal-modules` src/dist yasağına daraltıldı. Kabul: her makinede TÜM (durum,durum) çiftleri exhaustive doğrulanıyor; maxRisk her (seviye,scope) çifti; 88 test; coverage %99.3 satır / %94.7 dal (≥90 kapısı). Not: literal §19'a küçük eklemeler belgelendi (session starting→failed/cancelled, workspace provisioning→failed, project proposed/intake→cancelled, memory candidate→rejected). |
| T11 Drizzle schema + migrations 0001–0003 | ✅ Tamamlandı | 2026-08-11 | `packages/db/src/schema/`: identity (users [lower(email) uq, Argon2id], sessions, PAT'ler, model_providers, rate_limits UNLOGGED), companies (companies, company_members partial-uq, company_settings geniş tipli satır, company_sequences [PK (company_id,name), gap-free sayaç temeli], secrets [coalesce expression-uq; project_id FK'sı 0004'te], model_profiles, idempotency_keys [NULLS NOT DISTINCT uq]), org (org_units self-ref + slug uq, positions), agents (agents [model alanı YOK, partial name-uq, CHECK'ler], agent_model_bindings [tek agent↔model bağı, provider FK], agent_sessions [workflow_id+run_id uq, canlı-monitor partial idx; task_id FK'sı 0004'te], agent_steps [PARTITIONED RANGE(created_at), PK (id,created_at), aylık 3 partisyon DO bloğuyla], org_edges [tam-bir-hedef + kind-hedef CHECK'leri, tek-aktif-yönetici partial uq]). Tüm id'ler app-side UUIDv7 (`$defaultFn` → @acos/domain), enum'lar text+CHECK. Migration'lar drizzle-kit ile aşamalı üretilip spec numaralandırmasına (0001–0003) çevrildi ve hand-audit edildi (extension'lar, UNLOGGED, partisyonlama). `runMigrations`: pg_advisory_lock altında drizzle migrator; `db:generate`/`db:migrate` scriptleri; `drizzle-kit check` db lint'ine bağlı. Kabul: boş PG16 → migrate temiz; **race testi**: iki eşzamanlı runner → tam 3 migration, idempotent 3. koşum; ek doğrulamalar: 3 extension, 19 tablo, partisyon stratejisi 'r' + 3 partisyon + satır yönlendirme, UNLOGGED, CHECK/unique ihlalleri (23514/23505). 9 int test yeşil. |
| T12 Migrations 0004–0011 | ✅ Tamamlandı | 2026-08-11 | 9 yeni şema dosyası, 33 yeni tablo (toplam 52): 0004 projects/project_members/repositories/environments/deployments + tasks (16-durum CHECK, delegasyon/reassign CHECK'leri, workload partial idx) /task_dependencies/task_assignments (tek-aktif-owner partial uq)/artifacts (içerik CHECK'i)/reviews (reviewer≠author CHECK); 0005 events (PARTITIONED RANGE(occurred_at), PK (id,occurred_at), gap-free seq uq, outbox partial idx, payload GIN, 3 aylık + DEFAULT partisyon) + dead_events; 0006 channels (dm_key/task_thread/review partial uq'ları, kind↔ref CHECK) /channel_members (Founder=NULL, coalesce partial uq)/messages (trgm + refs GIN)/notifications; 0007 memories (scope/scope_ref CHECK, [0,1] CHECK'ler, **HNSW 1536+768 partial expression index'leri**, entities GIN, title trgm) + versions/evidence/relations/promotions + decisions/experiments/experiment_results/incidents; 0008 skills/agent_skills/skill_evidence (weight [-1,1])/performance_snapshots; 0009 tools [platform]/policies/tool_permissions/approvals/tool_invocations (S3 audit)/audit_log; 0010 workspaces (canlı tek-workspace partial uq)/workspace_locks/terminal_sessions/budgets (coalesce uq)/cost_entries (PARTITIONED + 3 partisyon)/llm_calls + **cost_rollup_daily matview** + geç-FK'lar (reviews/tool_invocations→workspaces); 0011 karanlık marketing: assets (HNSW ×2)/content_items/publish_jobs/metric_snapshots. Geç-FK deseni: secrets.project_id+agent_sessions.task_id+deployments.task_id+projects.intake_report_artifact_id 0004 sonunda; tasks.approval_policy_id+memory_promotions.rule_policy_id 0009 sonunda. Kabul: 11 migration boş PG16'da sırayla uygulanıyor (drizzle-kit check temiz); **her tabloya row-level insert/read** (karanlık tablolar dahil; boş tablo taraması ile kanıtlı); events/cost_entries partisyon yönlendirmesi; 4 HNSW index; matview refresh + doğru agregat; reviewer==author 23514; memory scope CHECK 23514. 15 int test yeşil. |
| T13 Repositories + tenancy + outbox | ✅ Tamamlandı | 2026-08-11 | `CompanyContext` (uuid doğrulamalı); **tenancy guard** (S4): tenant/platform tablo listeleri şema metadata'sından türetiliyor (`TENANT_TABLES`/`PLATFORM_TABLES` — companies hariç tüm domain tabloları tenant), `createGuardedDb` pool+client proxy'siyle her SQL'i denetliyor — SELECT/UPDATE/DELETE'te WHERE/ON/USING içinde `company_id`, INSERT'te kolon listesinde `company_id` yoksa `TenancyViolationError` (select-listesindeki company_id sayılmaz — `select *` açığı kapalı); platform/migrator için unguarded `createDb`. **Outbox**: `appendEvents(tx, ctx, batch)` + `withOutbox` — 10 §3'ün UPDATE...RETURNING kilitli sayacıyla gap-free ardışık `seq`, `domain.entity.action` ad doğrulaması, commit'te `pg_notify('acos_outbox')` (relay T21'de dinleyecek); `nextSequenceValue/Block` tüm sayaç adları için ortak. **Idempotency helper**: fresh/in_flight/replay/mismatch yaşam döngüsü. Repository'ler: Agent/Task/Org (units+positions+edges, `activeReportsToEdges` domain forest girdisi)/Company (settings satırıyla) — hepsi ctx-scoped. Kabul: tenancy negatif testleri (ham SQL + query-builder reddi, scoped + platform geçişi, metadata kapsam testi); **20 eşzamanlı tx → seq 1..20 boşluksuz**, rollback boşluk bırakmıyor (21 devam), batch ardışık; **outbox atomikliği** (fail→ikisi de yok, success→ikisi de var + subject ref); iki-şirket izolasyon probu (R2 temeli); idempotency yaşam döngüsü. 26 int test yeşil. |
| T14 packages/events catalog | ✅ Tamamlandı | 2026-08-11 | `EventEnvelopeSchema` 10 §4'e birebir; `defineEvent()` registry'si (`(type,version)` anahtarlı, çift kayıt + kötü ad reddi), versiyon yardımcıları (`isHandledVersion`, `parseEventPayload`), subject builder'ları (`subjectFor` = `co.<companyId>.<type>`, wildcard, parse). 9 katalog modülünde **190 event tipi** (189 durable + 1 ephemeral `workspace.terminal.output`) — doc 10 §10 tablosundan script'le çıkarılan listeyle birebir; alan tipleri tablodaki key field'lardan (domain enum'ları yeniden kullanılıyor: TASK_STATUSES, SENIORITIES, MEMORY_SCOPES...). Payload politikası (kayıtlı karar): §8 additive kuralı gereği alanlar optional, kimlik alanları (id'ler, `office.*`'ta causeEventId, task.status.changed'de from/to/byActor §8.2'ye birebir) required. Kabul: **registry↔doc CI karşılaştırması** (test doc'u parse edip set eşitliği doğruluyor — 190/190); her tip için ≥1 fixture parse yeşil (85 zengin + geri kalanı minimal); yanlış-şekil reddi testleri (bilinmeyen durum, eksik aktör, causeEventId'siz office hareketi — INV-12). 201 test yeşil. |
| T15 apps/server skeleton + contracts base | ✅ Tamamlandı | 2026-08-11 | **contracts tabanı**: RFC7807 problem+json şeması + 16 kodluk stabil hata kataloğu (21 §2.5 birebir), `HealthResponseSchema`, `createAcosClient` (Zod kaynağından elle türetilmiş typed SDK — health namespace; PAT bearer + X-Company-Id başlıkları; problem+json → `AcosApiError`), `./client` subpath'i. **apps/server**: `buildApp` (IO'suz) — Fastify 5 + fastify-type-provider-zod (validator/serializer compiler'lar), @fastify/swagger OpenAPI 3.1, problem+json error handler (ApiError kod eşlemesi, Zod validation→400 errors[], 404 not_found, fallback internal), 16 domain modülü stub plugin olarak kayıtlı (28 §2 listesi); `/healthz` (container liveness) + **`/api/health`** (postgres SELECT 1 + NATS/Temporal TCP probe, latencyMs'li agregasyon, biri düşerse 503 degraded). **Boot sırası** (`main.ts`): `loadConfigOrExit` → `runMigrations` (advisory lock) → route'lar → listen; SIGTERM/SIGINT temiz kapanış. **OpenAPI pipeline'ı**: `pnpm --filter @acos/contracts gen:openapi` server app'ini stub checker'larla kurup `openapi.json` yazıyor (commit'li); server testi üretileni commit'liyle karşılaştırıyor (drift check). Compose'a dev fallback MASTER_KEY/SESSION_SECRET/INTERNAL_API_TOKEN eklendi. Kabul: OpenAPI snapshot testi yeşil; SDK derleniyor + 3 client testi; health inject testleri (ok + degraded 503); **gerçek compose boot doğrulandı** — config→11 migration→up, /api/health 3 bağımlılığı ok raporladı. |
| T16 Auth module | ✅ Tamamlandı | 2026-08-11 | **Kripto** (18 §2): Argon2id m=64MiB/t=3/p=4 (@node-rs/argon2), 256-bit opak session/PAT token'ları (sha256 hash'li saklama), elle RFC 6238 TOTP (30s, ±1 pencere, sabit-zaman karşılaştırma), TOTP secret'ı MASTER_KEY-türevli libsodium **sealed box** ile şifreli. **AuthService**: first-run wizard (`setupNeeded`/`createFounder` — platform_role=owner), login (başarısız denemelerde `rate_limits` UNLOGGED tablosuyla 5/15dk limit + üstel audit), session doğrulama (24h idle sliding + 30d mutlak tavan), anında iptal eden logout, PAT üret/doğrula/listele/iptal (`acos_pat_<id>_<secret>`; **founder:approve scope yasak**), TOTP enable→confirm→disable; **her auth olayı audit_log'a** (`auth.*` dotted action'lar, S7). **Route'lar**: `/api/v1/auth/{setup,login,logout,me,pats,totp/*}` — zod şemalı, contracts'a eklendi; SDK için şemalar dışa açık. **Wiring**: @fastify/cookie; preHandler principal çözümü (session cookie veya PAT bearer); cookie-session mutasyonlarında **CSRF double-submit** (bootstrap route'ları muaf). OpenAPI 11 path'e güncellendi (snapshot yenilendi). Kabul: 11 int test — wizard (needed→create→409), login fail(audit)/success/logout(revoke)/me, CSRF reddi, PAT yaşam döngüsü (bearer auth + revoke sonrası 401), TOTP tam akış (enable→confirm→totpRequired→yanlış kod 401→doğru kod 200→disable), rate limit 5→429. Demo adım 1 API-level geçiyor. |
| T17 Companies module + seed v1 | ✅ Tamamlandı | 2026-08-11 | **CompanyService**: `create` tek transaction'da company + settings satırı + founder üyeliği + **`company.created` outbox'tan** (guarded db üzerinden, gap-free seq=1); `listForUser`, `membership` (21 §2.3 — eşleşmeyene 404, asla 403), `getSettings`/`updateSettings` (+`company.settings.updated` event'i, seq=2 devam ediyor). **Route'lar**: `GET/POST /api/v1/companies`, `GET /:id`, `GET/PATCH /:id/settings` (viewer'a settings düzenleme yasak; slug çakışması pg 23505+constraint eşlemesiyle 409). Contracts şemaları + OpenAPI 14 path'e yenilendi. **Seed v1** (27 §4): `ensureSeed` idempotent (slug 'acme' + founder email anahtarlı) — Founder kullanıcı (`founder@acme.local` / bir kez basılan rastgele şifre) + "Acme Technologies" (USD) + settings + üyelik; `pnpm seed` kök scripti; boot'ta `SEED_DEMO=true` ise otomatik + "ACOS ready — founder@acme.local / <şifre>" logu (27 §14). Kabul: 17 int test — `company.created` outbox emisyonu (payload + publishedAt NULL), çift slug 409, settings okuma/güncelleme + event, non-member 404, **seed iki kez → tek şirket/tek kullanıcı/tek event**, seed'lenen founder basılan şifreyle login olabiliyor. |
| T18 Org module | ✅ Tamamlandı | 2026-08-11 | **OrgService** (04 §1–4, §6): unit CRUD (kind'e göre `department.created`/`team.created`/`org.unit.created` event'leri; `moveUnit` parent değişiminde unit-tree CTE cycle check), position CRUD (+`position.created`), **edge yazımı** — tüm org mutasyonları `pg_advisory_xact_lock(hashtext('org:'||companyId))` altında; `reports_to` için 04 §2'deki recursive CTE (yukarı yürü, agent'a ulaşırsan ORG_EDGE_WOULD_CYCLE, derinlik ≤50) + **inverse `manages` edge otomatik** (04 §6.1); `endEdge` (end-date, silme yok); tek-aktif-yönetici partial-uq ihlali tipli 409. **Read model'ler**: `escalationChain` (CTE ile yukarı yürüyüş + **sanal Founder terminatörü** — root'lar doğrudan Founder'a), `teamRoster` (04 §4 SQL'i birebir: is_lead önce), listEdges/listUnits. Route'lar `/api/v1/companies/:id/org/*` (units/positions/edges/chain/roster) membership kontrollü; contracts şemaları + OpenAPI 21 path. Kabul: 8 org int testi — kind-specific event'ler, **unit-tree döngü reddi** (`org_cycle_detected` 409), Dev→EM→CTO→CEO forest'ı inverse manages çiftleriyle, **döngülü reports_to reddi**, ikinci yönetici 409, **zincir = Dev→EM→CTO→CEO→Founder(virtual)** (CEO zinciri = [founder]), roster lead-first sıralı. Demo adım 3 ve 5 API-level yeşil. |
| T19 Agents module | ✅ Tamamlandı | 2026-08-11 | **AgentsService**: `hire` 04 §6.1'e birebir TEK transaction — org advisory lock → gap-free `employee_number` → agents satırı (draft/active) → `member_of` + `reports_to` (+otomatik inverse `manages`) + opsiyonel `leads` edge'leri (OrgService.createEdgeInTx yeniden kullanımı, cycle check dahil) → company `model_profiles`'tan binding seed'i (reasoning→primary eşlemesi) → `agent.hired` (+`agent.started`). **Yaşam döngüsü** domain `agentMachine` guard'ıyla (illegal geçiş 412): `activate` (05 §3.1 doğrulamaları: çözülebilir primary model + reporting line/topLevel), `pause`/`resume`, **`offboard`** (05 §3.3 v1: TÜM aktif edge'ler end-date, direct report'lar eski yöneticiye yeni edge'lerle re-point, memory/history korunuyor, `agent.offboarded`). **Binding hot-swap** (05 §7): PUT upsert + `agent.model.binding.changed` — identity satırı değişmiyor (testle byte-byte doğrulanıyor). Sessions read API (T31'e kadar boş). **Seed 8 agent'a genişledi** (27 §4): Engineering + Backend/Frontend, 7 pozisyon, Dev→Lead→EM→CTO→CEO forest'ı, hepsi hire akışıyla (event'li). **Şema invariant testi** (INV-9 CI): model/provider kolonu yalnız 4 izinli tabloda — tüm şema metadata'sı taranıyor. Kabul: 12 agents int testi — 8 seed agent (EMP-001..008, hepsi aktif), 8×`agent.hired`, API şeklinde model alanı yok, zincir Lead→EM→CTO→CEO→Founder, pause⇄resume + 412, offboard re-point + sıfır aktif edge, hot-swap identity-untouched, sessions boş liste. OpenAPI 29 path. Demo adım 4 API-level yeşil. |
| T20 Web shell + org/agents UI | ✅ Tamamlandı | 2026-08-11 | **SDK genişledi** (contracts/client): auth/companies/org/agents namespace'leri, tüm yanıtlar Zod-parse'lı, mutasyonlarda `acos_csrf` cookie'sinden otomatik `x-csrf-token`. **packages/ui**: `acosPreset` (Tailwind token'ları) + primitifler (Button/Card/Input/Select/Textarea/Field/Dialog/DataTable/StatusPill) + domain widget'ları (AgentAvatar deterministik renkli, AgentStatusPill, RiskBadge, MoneyText minor-unit, EventRow) — veri çekmez, react peer. **apps/web**: TanStack Router code-based ağaç (`/login`, `/setup`, `/` company select, `/c/$companyId` layout — 24 §2), `requireAuth` beforeLoad redirect'i; **AppShell** 24 §1 komut-merkezi düzeni (TopBar + CompanySwitcher, 14 girişli LeftNav [implementler aktif, kalanlar disabled], StatusBar placeholder); TanStack Query company-first key'lerle (24 §4.1, staleTime 30s), Zustand `uiPrefsStore` (persisted nav collapse); Tailwind 3 + preset. **View'lar**: Organization (Cytoscape+dagre org chart [reports_to forest, BT], unit/position formları — cycle hatası API'den yüzeye çıkıyor, HireWizard entegre), Agents grid (durum filtresi, avatar/pill kartlar), AgentDetail (kimlik + binding'ler + escalation chain + sessions + pause/resume/offboard), HireWizard (pozisyon/birim/yönetici/seniority/otonomi/persona → hire&activate). Seed'e `SEED_FOUNDER_PASSWORD` env desteği (e2e/dev; boşsa rastgele). Kabul: **Playwright `02-company-and-org` + `03-hire-agents` spec'leri gerçek compose Mode-A stack'ine karşı 3/3 yeşil** (login → Acme → org chart + seed birimleri → UI'dan unit oluşturma → wizard'la hire → grid + detay + zincirde 'Founder (virtual)'); CI e2e-smoke job'ına Playwright bağlandı (chromium + trace artifact'ları). M1 demo uçtan uca tıklanabilir. |
| T21 Outbox relay + JetStream | ✅ Tamamlandı | 2026-08-11 | **JetStream provisioning** (10 §5): `ACOS_EVENTS` stream (subjects `co.*.>`, file storage, 72h max_age, discard old, **2m duplicate window**) + 4 durable pull consumer konfigürasyonu (office-projector/memory-trigger/cost-aggregator/notification; ack_wait 30s, max_deliver 5 — handler'lar kendi görevlerinde). **OutboxRelay** (10 §4): `pg_try_advisory_lock(hashtext('acos:outbox-relay'))` ile lider seçimi (takipçiler 5s'te bir dener), `LISTEN acos_outbox` + 1s polling fallback'i (doğruluk mekanizması polling), batch 500 `ORDER BY company_id, seq`, tam envelope JSON'ı `co.<companyId>.<type>`'a `Nats-Msg-Id=event.id` ile publish, ack sonrası `published_at` işareti (yalnız id ile — **bulunan bug**: JS Date round-trip'i timestamptz mikrosaniyelerini kırptığından occurred_at eşitliği hiç tutmuyordu ve relay sonsuz republish ediyordu; Msg-Id dedupe bunu maskeliyordu), null correlation → self-correlate. **Consumer idempotency** (10 §6): migration `0012_consumer_offsets` + `withSeqDedupe` — HWM satırı handler'ın yan etkileriyle AYNI transaction'da kilitlenip ilerliyor. **DLQ handler** (10 §5): `$JS.EVENT.ADVISORY.CONSUMER.MAX_DELIVERIES.*` aboneliği → zehirli mesajı stream'den çekip `dead_events`'e kopyalar + alert callback'i. Boot wiring: NATS bağlan → provision → relay + DLQ başlat (NATS yoksa relay devre dışı, boot sürer); graceful shutdown. Kabul (32 §2 outbox süiti): commit ⇒ **exactly-once publish** (envelope katalog şemasıyla parse, seq=1, self-correlation); publish-mark arası crash simülasyonu ⇒ republish, stream'de hâlâ 1 mesaj (Msg-Id dedupe); per-company seq sıralı; **iki relay → tek lider**, lider durunca takipçi devralıp yayına devam; HWM: artan seq'ler bir kez, redelivery'ler skip, consumer başına ayrı cursor. 5 test yeşil (server int toplam 37). |
| T22 Event emission audit + timeline API | ✅ Tamamlandı | 2026-08-11 | **Emisyon denetimi**: `emitDomainEvent` (server modules/events/emit.ts) artık TEK emisyon yolu — payload append'ten ÖNCE @acos/events kataloğuyla parse ediliyor; kataloglanmamış tip veya şema uyumsuzluğu tüm transaction'ı geri sarıyor (state değişikliği parse edilemez event'le commit olamaz). companies/org/agents servislerindeki tüm `withOutbox` çağrıları geçirildi. Audit bulguları: `company.member.added` founder üyeliği için emit ediliyordu → eklendi (create'te seq 2; companies testindeki settings seq beklentisi 3'e güncellendi); `org.edge.ended` payload'una `endedAt` eklendi (endEdge + offboard). **Timeline API** (21 §3.11): `GET /companies/:id/events` (types exact+`agent.*` prefix OR'lu, actorKind/actorId [jsonb ->>], taskId/projectId/agentId, from/to, seq-keyed base64url cursor, newest-first, limit≤200), `GET /events/replay?afterSeq&limit≤500` → `{items asc, nextSeq}` (WS resume fallback'i), `GET /events/:eventId` — hepsi membership 404 korumalı, guarded db. Contracts: `EventSchema` (10 §4 envelope) + query/response şemaları + SDK `events` namespace (list/replay/get, repeated types serileştirme); OpenAPI 32 path (snapshot yenilendi). Kabul: **producer contract matrix** int testi — tüm M1 mutasyonları API'den koşulup company'nin TÜM event satırları envelope+payload olarak katalogla parse ediliyor (26+ event); emit-tip seti 17 tipli beklenen matrisle birebir eşitlik; kataloglanmamış tip/kötü payload reddi + tx atomikliği; cursor sayfalaması boşluksuz/duplikasyonsuz tam tarama; prefix/exact/subject/zaman filtreleri; replay 1..N gap-free + nextSeq devamı + boş kuyruk; tekil fetch + yabancı company 404; **seed'li şirketin timeline'ı** company.created + 8×agent.hired döndürüyor. 9 yeni int test; server int toplam 46/46. Ayrıca T21 CI kırığı düzeltildi (0012 sonrası migration sayısı 12 + consumer_offsets kapsamı; run 31452581459 yeşil). |
| T23 /ws gateway | ✅ Tamamlandı | 2026-08-11 | **Wire contract** (21 §4 otorite; 22 iç yapı): `packages/contracts/src/realtime.ts` — ClientOp (subscribe/resume/unsubscribe/ping) + server frame'leri (hello{heartbeatSec:20,maxTopics:32,version:1}, sub_ok{current_seq,mode}, data{topic,seq,events[]}, replay_done, snapshot, gap, pong, error{forbidden_topic/limit_exceeded/replay_too_large/bad_op}) Zod şemaları, `parseTopic`, 4401/4408/4409/4413/4429 close-kod registry'si. **Gateway** (`apps/server/src/modules/realtime/gateway.ts`, @fastify/websocket 11, maxPayload 128KB): upgrade auth = session cookie (global preHandler principal'ı) veya `?pat=` (scope `read:events` zorunlu, yoksa 4401); connection registry (session başına ≤8 → 4429, connection başına ≤32 topic → limit_exceeded); **events topic'i**: subscribe→head seq (`sub_ok current_seq`) + canlı akış NATS `co.>` fanout'undan seq-dedupe'lu (≤lastSent suppressed), server-side gap tespitinde tablodan otomatik catch-up; **resume after_seq** → 500'lük DB sayfalarıyla replay + replay sırasında gelen canlı event'ler park edilip seq-dedupe'la splice + `replay_done` (22 §6.1 birebir), 5000 event cap → replay_too_large; slow consumer (buffered >1MB) → 4409 close (event asla drop edilmez, 21 §4.6); **presence**: snapshot-then-delta iskeleti (boş-geçerli snapshot; gerçek state T25 projector'da); **terminal**: topic auth zinciri terminal_session→workspace→project→company + founder/admin rolü (satır yokken fail-closed; frame kaynağı T41'de); heartbeat sweep (2×20s sessizlik → 4408) + 60s revocation sweep (session iptali → 4401, üyelik iptali → force-unsubscribe + forbidden_topic). Boot: `app.realtime` decorator, main.ts NATS bağlanınca `attachNats`; `/ws` OpenAPI'den gizli (snapshot değişmedi). Kabul (32 §14): **connect → canlı akış (outbox→relay→NATS→WS) → drop → 2 mutasyon → resume after_seq ⇒ TAM gap replay'i [head+2, head+3] sonra canlı head+4, boşluksuz/duplikasyonsuz**; envelope persisted şekliyle (id/type/payload); auth 4401 (cookie'siz + scope'suz PAT), forbidden_topic (yabancı şirket + bilinmeyen terminal), ping→pong + bad_op, presence snapshot, PAT `?pat=` happy path. 6 yeni int test; server int 52/52. |
| T24 Web realtime client + Events view | ✅ Tamamlandı | 2026-08-11 | **RealtimeClient** (`contracts/src/realtime-client.ts`, 22 §9): auto-connect + hello'dan heartbeat, full-jitter backoff (cap 30s; 4401 → `closed_auth`, retry yok), CursorStore'dan **auto-resume** (events topic'lerinde `resume after_seq`, 1s throttle'lı cursor yazımı), **seq dedupe** (duplicate/regression handler'lara asla ulaşmaz), **client-side gap healing** (delikli frame düşürülüp cursor'dan re-resume), replay/live splice, multiplexing (N handler → 1 sunucu aboneliği, refcount), `status$` (connecting/open/replaying/backoff/closed_auth); WebSocket impl + backoff enjekte edilebilir. **Kabul: scripted frame-sequence süiti** — fresh subscribe, duplicate suppression, out-of-order regression, **gap → re-resume → boşluksuz devam**, **drop → reconnect → cursor'dan resume → splice overlap'i dedupe**, 4401 retry-yok, refcount multiplexing, presence snapshot meta — 8/8 yeşil (FakeSocket, deterministik). **Web**: `getRealtimeClient` (sessionStorage cursor store, `/ws` same-origin), `RealtimeDispatcher` (/c/$companyId layout'unda; events:→EventSchema parse→`eventTickerStore` 1000'lik ring + `invalidationKeysFor` family-map'iyle Query invalidation [agent./org./company. aileleri], presence:→`presenceStore` snapshot), `useRealtimeStatus`; AppShell StatusBar artık ws durumu + son event'i gösteriyor; EVENTS nav aktif. **Events view** (24 §6.11): tip filtresi (virgüllü, `agent.*` prefix), **Live** (ticker'a pinli) / **Paged** (seq-cursor'lı `useInfiniteQuery`, load-older) modları, satırlar seq+zaman+actor pill+tip ve genişleyen ham payload JSON'ı. E2e `04-events-live` spec: paged modda seed geçmişi (agent.hired + company.created), Live modda REST'le yaratılan org birimi **reload'suz WS'ten akıyor** (outbox→relay→NATS→gateway→client→ticker) + status bar'da; Mode-A compose'da lokal 4/4 yeşil (02/03 dahil). Vite `/ws` proxy'si ve nginx Upgrade başlıkları hazırdı (T05). |
| T25 Office Projector (server) | ✅ Tamamlandı | 2026-08-11 | **Contracts** (`contracts/src/office.ts`, 23 §12 birebir): Cell/OfficeLayout (zones/desks/walls) + 11 presence badge'i + OfficeInstruction discriminated union (avatar.moved [path≤200, 5 reason], interaction.started/ended [endedBy enum], status.changed) — `causeEventId: z.uuid()` **zorunlu, optional/null yok**; PresenceState (snapshotEpoch'lu). **Auto-layout** (`office/layout.ts`, 23 §2): departman odaları uuid-sıralı deterministik soldan sağa, headcount'a göre desk sayısı (+%25 spare), üye→home desk ataması layout'ta persist, merkezi meeting alanı; `assignHomeDesk`/`freeDesk`/`gridPath` (L-şekilli deterministik). **Projector** (`office/projector.ts`): şirket başına tek-yazar aktör; `sealInstruction` choreoSeq (monoton) + emittedAt atayıp **şemayla parse eder — causeEventId'siz taslak publish'ten ÖNCE throw** (sert invariant); choreography tablosu 23 §4.1: hired→entrance'tan desk'e desk_assign yürüyüşü+IDLE, dm→alıcının desk'ine yürüyüş+interaction+COMMUNICATING+**12s dwell** (timer bitişi orijinal event id'siyle; gönderenin sonraki mesaj-dışı aktivitesi erken bitirir — neden=yeni event), kanal mesajı→4s speech (yürüyüş yok), review_request/review.requested→review yürüyüşü, review.started→REVIEWING, escalated→reports_to yöneticisine kırmızı-bubble yürüyüşü/Founder-seviyesinde yalnız ESCALATING badge, task started/IN_PROGRESS→WORKING, status.changed→badge (dupe coalesced), paused→OFFLINE+interaction cleanup, offboarded→cleanup+desk free+despawn, settings.updated(officeLayout)→layoutVersion+epoch bump; **lag kuralı** (23 §13): >30s bayat event'te yürüyüş yok badge'ler doğru; restart'ta state SQL'den yeniden kurulur (aktif agent'lar home desk'te IDLE). **Wiring**: layout `company_settings.extra.officeLayout`'tan (20 §3.3'ün geniş satırında office_layout kolonu yok — sunum konfigi extra'da, kayıtlı sapma); instruction'lar NATS `co.<cid>.office.*`'a ephemeral publish (events tablosuna asla yazılmaz); gateway artık domain event'leri projector'a besliyor, `office.*` subject'lerini presence:<cid> aboneliklerine gateway sayaçlı delta olarak yönlendiriyor ve **presence snapshot'ı projector'dan** geliyor (T23'teki boş stub kaldırıldı; ws int testi layoutVersion=1'e güncellendi). Kabul: **golden süit 13 test** — hired/dm+dwell/erken-bitiş/speech/escalation(iki dal)/badge eşlemeleri/offboard/**determinizm (aynı stream ⇒ bayt-özdeş çıktı)**/lag/snapshot-seed; **invariant testleri**: causeEventId'siz veya null taslak `sealInstruction`'da throw + karışık stream'in TÜM instruction'ları şemadan geçiyor + choreoSeq kesin artan. Server unit 18/18, int 52/52. |
| T26 PixiJS office skeleton + Agent Monitor | ✅ Tamamlandı | 2026-08-11 | **Headless engine** (`features/office/sceneState.ts`, 23 §6–7): saf state makinesi (Pixi/DOM/timer yok, dışarıdan `tick(dt)`) — avatar başına FIFO animasyon kuyruğu, **6 cell/s** yürüyüş interpolasyonu (Manhattan path), badge komutları anında, **time compression** (>3 bekleyen yürüyüş veya >10s öngörülen playback → yalnız son yürüyüş oynar, atlananlar sayaçla `collapsedCount`, debug ring'de causeEventId'leriyle kalır), snapshot kuyrukları komple değiştirir (resume'da choreography replay'i yok), debugRing(200) + `lastAppliedEventId`. **officeStore** (`stores/office.ts`): client-taraf invariant — causeEventId'siz instruction **dev'de throw, prod'da warn+drop**; şema reddi aynı politika. **OfficeCanvas** (Pixi 8 bridge — modülde animasyon API'sine dokunabilen TEK dosya): zone/desk/label katmanları, avatar Graphics+Text node'ları engine state'inden senkron, etkileşim bubble'ları (escalation kırmızı), kamera pan/zoom (0.25–2.0), **WebGL yoksa DOM listesi fallback** (23 §15); `window.__acosOffice` debug hook'u (lastAppliedEventId/agentCount/interactionCount/snapshotCount/debugRing-20). OfficeView: `/c/$companyId/office` + agent card popover ("Open in Agent Monitor") + debug inspector paneli; OFFICE nav aktif. **Dispatcher**: presence snapshot → presenceStore+officeStore; delta'lar → `officeStore.enqueue` + `office.status.changed`'de badge map. **Agent Monitor**: kartlarda WS-beslemeli canlı presence badge'i (polling yok, 24 §6.2). **Office lint kuralı aktif** (29 §6.3): `features/office/**`'ta rAF/setInterval/setTimeout + pixi/gsap importları yasak (bridge+engine+test hariç) — negatif probe dosyasıyla kural ateşi kanıtlandı. Kabul: **headless-ticker replay süiti 7 test** (yürüyüş+interaction akışının kesin final state'i, 6 cell/s hız, compression collapse+ring, snapshot-resets-queues, canlı hire spawn, şema reddi, store invariant throw); **M2 demo e2e `05-office-live`**: İKİ tarayıcı bağlamında canlı hire → her ikisinde reload'suz avatar spawn (`__acosOffice.agentCount` artışı + lastAppliedEventId dolu), reload → **snapshot'tan replay catch-up** (kat sayısı +1) — Mode-A compose'da lokal yeşil. Not: 12×4 sprite-sheet avatar ailesi yerine skeleton'da badge-renkli daireler (5.1 asset boru hattı sonraki görsel iş). **Bulunan bug (T24 client'ında)**: RealtimeClient data-frame'lerde `seq` alanı olmayan öğeleri filtreliyordu — office instruction'ları `choreoSeq` taşıdığından presence delta'ları client'a hiç ulaşmıyordu; filtre yalnız events-topic'ine daraltıldı + regresyon testi (frame süiti 9 test). |
| T27 Task engine service | ✅ Tamamlandı | 2026-08-11 | **TasksService**: create (07 §2 kind merdiveni — level-skip reddi, `goal`/`task` parentless olabilir [WRITER-DECISION], delegation_depth=parent+1 ≤5 **insert'e yazılıyor**), gap-free per-company `task_number` (TASK-n), list (tüm 21 §3.7 filtreleri), PATCH (+task.updated), recursive-CTE tree (id'ler CTE'den satırlar ORM'den), **DAG**: addDependency aynı-tx recursive cycle check (c→b→a varken a→c → `dependency_cycle_detected` 409, self-dep dahil), DELETE=resolve + `task.dependency.resolved`, `GET /tasks/dag` (board'un DAG sekmesi için tüm edge'ler). **TaskStateService — TEK status yazarı**: her geçiş tek transaction'da `FOR UPDATE` kilidiyle; **aktör sınıfları server-side çözülür** (asla client beyanı): owner/creator satırdan, reviewer=aktif assignment rolü VEYA position default_role∈{reviewer,lead}, qa=assignment 'qa' VEYA default_role reviewer, manager+=owner'ın reports_to zincirinde (CTE) VEYA executive (ownersız taskta manager/lead) — sonra saf domain `authorizeTaskTransition` (T10) sınıf sınıf denenir; red → `task_transition_invalid` 409. Terminal'de closedAt; task.status.changed (+FAILED/CANCELLED'da task.failed/task.cancelled); **DONE → dependent'ların edge'leri resolve + event**. `assign`: PLANNED→ASSIGNED matristen yetkili (üye reddi), owner değişiminde reassignment_count++ ≤3 → `TASK_REASSIGNMENT_LIMIT` (tam 3'te), task_assignments tarihçesi (tek-aktif-owner uq) + agent.task.assigned + task.reassigned. Contracts: Task/Create/Update/Transition/Dependency/Assignment/TreeNode şemaları + **`TASK_NEXT_STATUSES`** (UI affordance'ı domain makinesinden türetilir — sunucu yine yetkilendirir) + SDK `tasks` namespace; OpenAPI 40 path. **Kabul — transition-permission süiti (11 int test)**: founder groom akışı; PETE (üye) assign reddi; MIRA assign; owner-only start/submit (manager+reviewer reddi); **kendi işini onaylayamama** (owner QA reddi + founder'ın API'den QA denemesi 409 — demo 12'nin kanıtı); reviewer changes→rework→QA→DONE; illegal DONE→IN_PROGRESS 409; yalnız manager cancel; APPROVAL→DONE founder'a bile kapalı (engine T35); **byActor'lı 10 adımlık status-trail birebir**; DAG cycle 409 + DONE'da resolve; reassignment tam 3'te patlıyor; tree endpoint. **Board UI** (24 §6.3): Kanban (16 durum 10 kolonda, kart→detay diyaloğu: makine-legal geçiş butonları + 409'u aynen gösterir, PLANNED/ASSIGNED'da owner atama), Tree (parent hiyerarşisi), DAG (Cytoscape+dagre, resolved edge'ler kesikli); TASKS nav aktif; `task.*`/agent.task.assigned invalidation'ları. **Demo adım 12 e2e `06-tasks-board`**: composer→DRAFT, groom→PLANNED, Alex Demir'e atama→ASSIGNED kolonunda; founder'ın ASSIGNED→IN_PROGRESS denemesi **UI'da 409 task_transition_invalid** gösterip kart yerinde kalıyor — Mode-A compose'da yeşil. |
| T28 Delegation + budgets | ✅ Tamamlandı | 2026-08-11 | **DelegationService** (`tasks/delegation.ts` — ayrı delegationWorkflow YOK, 09 §4: T32'deki manager loop'un çağıracağı create_task/delegate_task aktivite semantiği): `createChildTask` (hiyerarşi+derinlik TasksService'ten; estimatedEffort 1–13 context'te), `delegateTask` = **raporlama-hattı kuralı** (transitive manages closure CTE'si; executive'ler cross-team; **tepe CEO yalnız executive/manager/lead'e — IC'ye asla**) → **WIP kapasite kontrolü** (07 §6.1: rol-türevli limitler member 2/lead 3/manager 5/executive 8 + ASSIGNED kuyruğu ≤5 + takım 2×aktif üye; kanonik 20 §4 şemasında wip_limit kolonu olmadığından limitler rol-default'u — kayıtlı sapma; fail'de `{ok:false, reason, candidates[+yük]}` manager'ın re-plan girdisi) → **pro-rata bütçe** → tek-status-yazarından assign. **Pro-rata (07 §9, 80/20)**: `floor(B×0.8×wi/Σw)` — Σw TÜM açık kardeşler üzerinden (pay istikrarı), %20 parent rezervi, havuz `0.8B−Σallocated` cap'i, geç çocuk havuz bitince bütçesiz; `budgets(scope=task, period=total, mode=hard)` satırı materialize (guard'lar ağaç yürümez) + task.budget_cents senkron + budget.created. **Reassignment (07 §8)**: 4. deneme `TASK_REASSIGNMENT_LIMIT` + task `needsManagerIntervention` bayrağı + **manager'a otomatik P1 intervention task'i** (idempotent); `force_reassign` yalnız Manager+/Founder — sayaç cap'te kalır (DB CHECK ≤3), task.reassigned'a audit notu. **CostService** (`costs/service.ts`, 26 §1,4,5): append-only cost_entries ledger'ı (+task.spent_cents artışı + cost.entry.recorded), setBudget upsert (budget.created/updated), pencereli tüketim (daily/weekly/monthly UTC, total resetsiz; task scope'u **subtree** CTE toplamı), breş tespiti **crossing yazımında** (bir kez: önce<limit≤sonra) → soft `budget.warning` / hard `budget.exceeded`; **şirket devre kesici**: hard company breşinde kritik-olmayan TÜM aktif agent'lar paused (`agent.paused` + employment.paused_by_breaker) — kritik = seniority_track'te lead/executive VEYA REVIEW/QA/APPROVAL'da task sahibi VEYA employment.critical (26 §5 WRITER-DECISION); pausedAgentIds budget.exceeded payload'ında; `restoreBudget` → budget.restored + breaker'ın durdurduklarına otomatik resume. Tenancy guard'ın yakaladığı gerçek bug: budgets update'leri company_id'siz yazılmıştı — düzeltildi. **Kabul (8 int test)**: derinlik **tam 5'te** (5 ok, 6 red); raporlama hattı (üye delegasyonu red, CEO→IC red, CEO→manager ok); WIP limiti 3. aktif task'te devreye girip **yüklü candidates listesi** dönüyor; pro-rata 10000→4000/2400/1600 (**Σ=8000=0.8B korunumu**, hepsi hard/total) + geç çocuk bütçesiz; reassignment **tam 3'te** + P1 task + force yalnız manager; breaker: 1000'lik daily hard'da 600+600 → tek budget.exceeded, OWEN/PETE paused, CEO/MIRA/RITA(REVIEW sahibi) aktif, ikinci aşımda re-emit yok, restore'da resume + budget.restored; task-hard pause'suz / agent-soft warning. Server int 71/71. **A4 (2026-08-15) — devre kesicinin UÇTAN UCA kanıtı** (`apps/server/test/integration/cost-breaker.int.test.ts`): var olan breaker testi `CostService.recordCost`'u DOĞRUDAN çağırıyordu, yani B1 (LLM maliyeti 0) geri gelse hiçbir test kırılmazdı. Yeni test zincirin tamamını gerçek bileşenlerle koşturuyor — gerçek Postgres + gerçek Temporal worker'ı + gerçek `agentTaskWorkflow` + gerçek aktiviteler + gerçek `computeCostCents`; sahte olan yalnız sağlayıcı transport'u (çağrı başına 100k girdi token'ı, `model_providers.pricing` 26 §3.1 şekliyle 400¢/Mtok ⇒ 40¢/adım). 200¢ görev bütçesi + 100¢ şirket günlük hard bütçesiyle doğrulananlar: `llm_calls.cost_cents`>0 (B1 nöbetçisi), 4 `cost_entries` satırı (kind=llm, task/agent atıflı), `tasks.spent_cents` defterle birebir, 3. adımda şirket eşiği aşılınca `budget.exceeded{scope:company}` + `tripBreaker` → SPENDER/PEER `paused` + `employment.paused_by_breaker="true"` iken executive track (CEO) ve REVIEW sahibi (GOVERNOR) ayakta (26 §5 kritik-ajan yüklemi), 5. turda guard (a) adımı BAŞLATMADAN kesip `agent.guard.triggered{guard:"budget"}` + `agent.escalated` + görev WAITING + `guard_stopped`, `restoreBudget` → `budget.restored` + 2 `agent.resumed` + `paused_by_breaker` temizlendi + status `active`. **Kırmızı kanıtı** — üç ayrı sabotaj testi düşürdü: workflow guard (a) devre dışı ⇒ döngü 50-adım tavanına gidip 2000¢ harcadı (`expected 50 to be 5`), `persistStepActivity`'deki `recordCostInTx` çağrısı devre dışı (B1 regresyonu) ⇒ `spent_cents` 0 kalıp guard hiç tetiklenmedi (`expected 50 to be 5`), `tripBreaker` no-op ⇒ `pausedAgentIds` boş (`expected [] to deeply equal [ …(2) ]`). Not: 26 §5 devre kesiciyi "policy engine consumer" + `managerDirective(pause)` sinyali olarak tarif ediyor; kodda duraklatma `CostService.tripBreaker` içinde senkron ve **yalnız DB seviyesinde** — koşan workflow'lara sinyal gitmiyor, restore'da da workflow yeniden başlatılmıyor. Test kodda gerçekte var olanı doğruluyor; sinyal yarısı açık iş. |
| T29 packages/llm ModelRouter + adapters | ✅ Tamamlandı | 2026-08-11 | **Port** (ADR-015 birebir — vendor tipi sınırı geçmez): `types.ts` kendi şekillerimiz (LlmMessage/Usage/Complete/Embed, `LlmError` 5 kind'lı [rate_limited/provider_unavailable=fallback-eligible; auth/bad_request=asla maskelenmez; no_target], pricing JSONB→`computeCostCents` cached-token indirimli, `LlmCallLogEntry` accounting hook'u). **Resolution** (`resolution.ts`, _DECISIONS §17): saf fonksiyon — purpose→binding eşlemesi (reasoning/coding/vision→primary, fast→fast, embedding→embedding), agent binding override önce, sonra enabled company profilleri priority sırasıyla = fallback zinciri; provider+model dedupe. **ModelRouter** (`router.ts`): 429/5xx'te sıradaki hedefe fallback (bad_request/auth asla — kendi hatamız maskelenmez), yapılandırılmamış provider atlanır, zincir biterse son hata; **her deneme (başarı+hata) logCall'a** — accounting hiçbir yoldan atlanamaz (llm_calls yazımı app-side callback, paket repository-free 28 §2); token cap min-birleşimi (request ∧ profil maxTokensPerCall ∧ router default); `embed()` aynı port/fallback/accounting'le; router kendisi retry etmez (retry Temporal'da, ADR-005). **Adapter'lar** (`adapters/ai-sdk.ts` — AI SDK v5 SADECE bu dosyada): generateText/embed üzerinden anthropic (@ai-sdk/anthropic), openai, openai-compatible (openrouter + ollama `<base>/v1`); APICallError→LlmError eşlemesi (429/401-403/5xx/4xx), finishReason normalizasyonu; `createProvidersFromConfig(Config)` (offline profil = yalnız Ollama, A3). **Boundary lint aktif**: `ai`/`@ai-sdk/*` importu adapters/ dışında repo genelinde yasak (negatif probe ile kanıtlı; agent-framework yasakları korunarak). **Kabul**: resolution+fallback unit süiti **11 test** — override sırası, purpose eşlemesi, disabled skip+priority+dedupe, 429→sonraki hedef (iki deneme de loglu), 5xx zinciri→son hata (3 log), bad_request fallback yok, unconfigured skip, no_target, cap min-birleşimi, cached-discount maliyet, embed yolu; **adapter smoke env-flag arkasında** (`LLM_SMOKE=1`+key; CI'da otomatik skip — placeholder testi skip'i doğrular). |
| T30 Fake ModelRouter | ✅ Tamamlandı | 2026-08-11 | **AgentAction union** (`llm/src/agent-action.ts`): 08 §4'e birebir 13-aksiyonluk strict discriminated union (12 kanonik + `think`) + TaskResult (07 §10) gömülü. Kayıtlı sapma: doc 08 dosyayı packages/domain'e koyar; domain'in T09 sıfır-runtime-bağımlılık invariantı zod'a izin vermediğinden saf şema `@acos/llm`'de (IO yok; worker'lar model çıktısını parse ettikleri paketten import eder; TaskResult contracts'a T35'te API için ayrıca gerekirse oradan türetilecek). **`@acos/llm/testing` subpath'i** (32 §6 — prod yolları asla import etmez): `loadScript` YAML script'i parse edip **her adımı gerçek AgentAction union'ına karşı yüklemede doğrular** — shorthand args (`{status: IN_PROGRESS}`) `normalizeStep` ile tam aksiyona genişletilir (placeholder UUID'lerle; runtime'da gerçek id ikamesi; uuid-olmayan reviewerHint'ler summary'ye düşer), drift `ScriptLoadError` ile anında patlar; `ScriptedSession` — minimal `when: {lastToolResult: {exitCode: zero|nonzero}}` / `onSignal: {reviewVerdict: ...}` dallanması (32 §6.1'in bilinçli sınırı), eşleşmeyen dal adımları atlanır; `createScriptedAdapter` — port-uyumlu ProviderAdapter: mesajlardaki `[role:..] [taskFixture:..]` marker'larıyla script seçer, `[lastExitCode:n]`/`[signal:k=v]` gözlemleriyle ilerler, aksiyonu JSON text olarak döner (callModelActivity'nin parse edeceği şekilde), embed'i pseudo-embedding'e bağlar; **pseudo-embedding**: FNV-1a content-hash'ten deterministik L2-normalize vektör (default 768, boyut parametreli — ADR-020 1536/768 ikilisiyle uyumlu); **canned consolidation** fixture-keyed sabit çıkarımlar (T44 memory pipeline'ı için). **Kanonik script** `packages/llm/testing/scripts/backend-dev.task-implement.yaml` 32 §6.1'den birebir (7 adım, iki dal). **Kabul (11 test)**: kanonik YAML yüklenir + happy-path sekansı (dallar atlanır: 5 aksiyon) + **nonzero-exit dalı ve changes_requested sinyal dalı alınır** (7 aksiyonluk tam akış, hepsi union'dan geçer); **drift load'da patlar** (bilinmeyen aksiyon, DOING enum'u, bozuk wait_for, match'siz şekil); adapter marker eşleşmesi + bilinmeyen fixture'a gürültülü red; pseudo-embedding determinizm/içerik-duyarlılık/birim-norm/boyut. llm süiti 24/25 (smoke skip). |
| T31 agent-worker scaffold | ✅ Tamamlandı | 2026-08-11 | **Worker kaydı** (09 §4.1 kanonik şekli, Temporal SDK 1.22): tek unprivileged container'da 3 worker — `agent-tasks` (workflowsPath + aktiviteler; maxConcurrentWorkflowTask 40 / activity 64 / cachedWorkflows 200 / shutdownGrace 30s), `memory` (activity 8) ve `intake` (activity 4) kendi kuyruk izolasyonlarıyla; boot `loadConfigOrExit` → NativeConnection → 3×Worker.create → ready olana dek 503 dönen `/healthz` korunur; SIGTERM/SIGINT graceful shutdown. İskelet workflow'ları: `pingWorkflow` (proxyActivities→`echoActivity` round-trip'i), memory/intake placeholder'ları (T44/T42 bundle path'leri hazır). **Client modülü** (09 §5): `workflowIds` deterministik ID builder'ları (`agent-task.<taskId>.<agentId>`, `agent-inbox.<agentId>`, `review.`, `consolidation.`, `intake.`), `startAgentTaskWorkflow` **REJECT_DUPLICATE** ile, `signalAgentInbox` signalWithStart ile (server-side ikizi T33'te). **Altyapı düzeltmeleri**: `Dockerfile.node` alpine→**bookworm-slim** (@temporalio/core-bridge glibc-only native — musl'da ERR_DLOPEN; healthcheck'ler için wget+ca-certificates kuruldu); compose agent-worker'a dev-fallback MASTER_KEY/SESSION_SECRET/INTERNAL_API_TOKEN + `temporal: service_healthy` dependency eklendi. **Kabul**: (1) **gerçek Temporal'da round-trip** — Testcontainers int süiti: pingWorkflow → `pong:<cid>:echo:...`, agent-task **dedupe testi** (aynı deterministik id ile 2. start `WorkflowExecutionAlreadyStartedError`), ID şekil testleri; (2) **TestWorkflowEnvironment harness'i** — createTimeSkipping + stub aktiviteyle aynı workflow (09 §10 deseni); worker int 6/6; (3) **compose Temporal'da canlı round-trip** — full stack 10/10 healthy, host'tan `client.workflow.execute` → compose agent-worker container'ı yürüttü (`COMPOSE ROUNDTRIP OK`); yeni base'de e2e 6/6. |
| T32 agentTaskWorkflow core loop | ✅ Tamamlandı | 2026-08-11 | **Mimari taşıma (kayıtlı)**: TasksService+TaskStateService `@acos/db/task-engine`'e taşındı — worker aktiviteleri (08 §3) ve server route'ları TEK status-yazarı implementasyonunu paylaşmak zorunda ve worker server kodunu import edemez (matris db→domain/events'e izin verir); server modülü re-export, tüm route/test import yolları değişmedi. Domain'e **saf `uuidv5`** (RFC 9562 SHA-1, crypto'suz — workflow içinde deterministik `stepId = uuidv5(String(stepNo), sessionId)`; RFC referans vektörüyle testli). **Workflow** (`agent-task.workflow.ts`, 08 §1–2): build Working Set → callModel → **strict AgentAction parse + 2 sınırlı repair** (Zod hatası prompt'a eklenerek; 3. başarısızlık failed step olarak persist edilip abandoned) → executeAction → persistStep; terminal yollar request_review/complete_task/abandon; 60-adım sert emniyet (T34 guard'ları gelene dek); saf orkestrasyon — tüm IO aktivitelerde. **Aktiviteler** (deps-enjekteli factory): `startAgentSession` (upsert running + agent.task.started + presence WORKING), `buildWorkingSet` (08 §8 bölüm sırası 1-3,8-10; memory 4-6 T45'te, thread 7 T33'te; scripted eşleşme için `[role:][taskFixture:][lastExitCode:][signal:]` marker'ları working set'in içinde — böylece fake adapter gerçek prompt'tan sürülür), `callModel` (ModelRouter + **idempotent llm_calls** `uuidv5('llm:<repair>', stepId)`), `executeAction` (update_task_status/request_review **tek yazardan**; use_tool stub'ı T39/T40'a kadar exitCode'lu gözlem döner; T33+ aksiyonları yapılandırılmış redle), `persistStep` (agent_steps check-then-insert + cost_entries `uuidv5('llm-cost',stepId)` + task.spent_cents + session sayaçları — tek tx, **yalnız taze insert'te sayaç artar**; `think` şema CHECK'i 12 kind olduğundan actionKind=record_decision proxy'siyle, gerçek tip action JSON'ında — kayıtlı sapma), `closeAgentSession` (+agent.session.ended + presence IDLE). main.ts: **LLM_MODE=scripted** → kanonik YAML'ler + gerçek model_providers satırı (llm_calls FK'sı dürüst) arkasında fake adapter; live → model_providers satırlarından kind-bazlı adapter haritası (row.id anahtarlı — profil/binding'lerin referansladığı şey) + DB routing loader'ı. **Kabul (4 int test, gerçek Temporal+PG)**: scripted loop **ASSIGNED→IN_PROGRESS→REVIEW** sürdü (`{outcome:'review_requested', steps:4}`; byActor'lı birebir trail); **step sırası** [update_task_status, use_tool, use_tool, request_review] + **her step.id === uuidv5(stepNo, sessionId)** + llm_calls anahtarları doğrulandı; session running→completed sayaçlarla + presence çifti; **replay edilen persistStep no-op** (satır/sayaç değişmez). Worker int 10/10, server int 71/71 (taşıma sonrası), repo 52 task yeşil. |
| T33 Signals, inbox, communication | ✅ Tamamlandı | 2026-08-11 | **Comms çekirdeği `@acos/db/comms`** (task-engine ile aynı gerekçe — TEK gönderim yolu 11 §0.2'yi server REST'i ve worker aktivitesi paylaşır): `ChannelService` — race-free get-or-create provisioning (unique partial index + ON CONFLICT + re-select; dm `dmKey`, task_thread, team/department), üyelik (+channel.created/member.added event'leri), markRead; `MessageService.send` — **persistence-first** (mesaj satırı + agent.message.sent [+help_request'te agent.help.requested] tek tx'te commit OLDUKTAN sonra delivery), kanal FOR UPDATE kilidi (uuidv7 id = sıralama; kanonik şemada channel_seq kolonu yok — kayıtlı sapma; unread=last_read_at, mention'lar refs'te), üyelik/mention/kind doğrulamaları, idempotencyKey=mesaj id replay'i, status/system asla uyandırmaz; `deliverMessage` + **SignalPort** portu (aktif oturum → `messageReceived` sinyali, yoksa `signalWithStart agent-inbox.<agentId>`). **Provisioning wiring** (aynı-tx, 11 §2): TasksService.create → task_thread (+creator üye), assign → owner thread'e katılır; OrgService createUnit → team/department kanalı, member_of edge → kanal üyeliği. **Worker**: 6 sinyal tanımı + handler'ları (messageReceived dedupe'lu 50'lik buffer, dependencyResolved, reviewVerdict, approvalVerdict, managerDirective pause/resume, cancel); **wait_for = Temporal condition + timer** (08 §6, polling yok) — WAITING'e tek-yazardan geçiş, uyanınca system aktörüyle IN_PROGRESS, sinyal buffer'ı sonraki working set'e `[signal:...]` marker'ları + Thread bölümü olarak akar; `send_message` aksiyonu MessageService+SignalPort'tan; **agentInboxWorkflow** (08 §7): signalWithStart, 5s debounce, `triageInboxActivity` (purpose **fast**; profil/parse yoksa ignore fallback) → act/queue/ignore, her verdict mark-read, 10dk boşta tamamlanır; Temporal SignalPort implementasyonu worker main + server main'de (server'a @temporalio/client eklendi — best-effort, Temporal yoksa mesajlar yine durable). **REST** (11 §4.2): channels list / dm get-or-create / messages page (beforeId) / **Founder send** (server-mint idempotency + post-commit delivery) / read / members — agent'lara HTTP yolu YOK; contracts+SDK `comms` + OpenAPI **44 path**. **Communication UI**: kanal listesi (kind pill'leri, dm etiketi çözümü) + thread pane + Founder composer; nav aktif; agent.message.sent/channel.* invalidation'ları. **Kabul (4 worker int testi)**: mesaj+event **delivery'den önce** durable + dm get-or-create stabil + idempotent replay; **IDLE agent'a mesaj → inbox workflow uyanır** (describe RUNNING/COMPLETED, triage sonrası last_read_at doluyor); **AKTİF agent'a mesaj → messageReceived sinyali `wait_for(reply)`'ı uyandırır** (ASSIGNED→IN_PROGRESS→WAITING→IN_PROGRESS→REVIEW birebir trail; inbox workflow'u hiç başlamadı — sinyal yolu kazandı); task_thread auto-provisioning. Server int 71/71 (T22 matrisi channel.created/member.added ile güncellendi); **demo 14 e2e `07-communication`**: takım kanalı auto-provision + Founder mesajı persist (reload sonrası da) — compose'da yeşil. Not: anti-ping-pong guard (e) sayaç bookkeeping'i T34'ün guard paketinde. |
| T34 Guards + continueAsNew + cost accounting | ✅ Tamamlandı | 2026-08-11 | **Taşıma**: DelegationService + CostService da `@acos/db`'ye (task-engine/comms ile aynı gerekçe; server re-export); `recordCost`'a deterministik `id` (uuidv5'ten) — replay'de exactly-once. **Altı guard, kod olarak** (08 §9; prompt metni değil): **(a) bütçe** — her adım ÖNCESİ `getGuardSnapshotActivity` (task budget−spent + son-5-adım ort. maliyet×1.5 tahmini); remaining ≤0 veya ≤tahmin → adım hiç başlamaz, guardEscalate. **(b) deadline** — workflow başında kurulan Temporal timer'ı **bekleme ortasında bile** ateşler; bir kez zorunlu escalate, iş yönetici direktifiyle sürer. **(c) step cap** — ≥40 soft uyarı section-10 marker'ı olarak working set'e; kümülatif >50 hard (carried state üzerinden continueAsNew'leri sayar) → forced help. **(d) loop dedektörü** — son 6 adımın normalize hash ring'i (lowercase + uuid/timestamp strip, saf FNV); ≥3 özdeş → adım guard gözlemiyle persist + forced help, execute edilmez. **(e) ping-pong** — merkezi olarak MessageService.send İÇİNDE (hiçbir workflow bypass edemez): aynı çiftin >8 sıkı-alternasyonu → agent.guard.triggered(ping_pong). **(f) derinlik** — create_task/delegate_task dispatch'i artık worker'da (taşınan DelegationService; TaskEngineError yapılandırılmış gözlem olarak sonraki working set'e düşer, loop guard'ı tekrarları yakalar). `guardEscalateActivity`: agent.guard.triggered + agent.escalated event'leri + task thread'e help_request (tek gönderim yolundan) + bütçe/loop/step_cap'te task WAITING (deadline yalnız escalate). **continueAsNew (08 §10)**: 50 lokal adımda veya historyLength>5000'de `CarriedState {stepNo, loopHashes(6), pendingMessages(≤50), seenSignalIds(≤200), version:1}` ile; **session continue boyunca açık kalır** (finally close'u continuing bayrağıyla atlanır). **Adım-başına maliyet**: persistStep → CostService.recordCost (idempotent id) — şirket günlük breaker'ı artık agent harcamasından da tetiklenir (T28 breş/breaker mantığı aynen). **Kabul — 32 §3 guard süiti 6/6** (TestWorkflowEnvironment time-skipping + stub aktiviteler): bütçe adım-öncesi durdurma (0 persist); **time-skip deadline** (4h wait_for 1h deadline'ı atlar → sonraki adımda escalate, iş request_review'a devam); loop 3× özdeş (3. persist edilir ama execute edilmez) + **normalizasyon kanıtı** (taze uuid'ler dedektörü atlatamaz); **50 adımda continueAsNew** (ilk run history'sinde ContinuedAsNew event'i + carried stepNo ile 51'de kümülatif cap + session tek kez kapanır); pause park/resume/cancel deterministik wait_for-park akışıyla. Ping-pong servis-düzeyi testi (10 volley → guard event). Worker int 21/21, server int 71/71, repo 52 task yeşil. |
| T35 Approvals engine + Approval Center | ✅ Tamamlandı | 2026-08-11 | **Domain** (`policies/approval-rules.ts`): S6 sabitleri — `FOUNDER_ONLY_CATEGORIES` (payments/legal/credentials/destructive_prod, platform-hard-coded) + kanonik 8'li `APPROVAL_KINDS` (20 §12.5) + devredilemez alt küme; türetilmiş expiry `approvalExpiresAt` = min(created + [critical 24h/high 48h/normal 72h/low 7d], iş deadline'ı) — şemada expires_at kolonu olmadığından (kanonik 20 §12.5) expiry HESAPLANIR, engine/sweep/workflow hep aynı değeri görür (kayıtlı sapma); reminder kesirleri 0.5/0.85. **S6 matris testi**: authorize() diğer TÜM eksenlerin (6 seviye × 4 risk × 4 scope × 2^6 boolean) üretilmiş 6144 kombinasyonunda founderOnly ⇒ require_approval(founder) — tek istisna devredilen spend bandı (standing grant, 19 §8). **Brief kontratı** (`@acos/events/briefs` — hem @acos/db hem contracts matris-içi paylaşabilsin diye kanonik ev; contracts re-export): 11 alan zorunlu `.strict()`, alan-başı uzunluk cap'leri, urgency seviye-önekli, **transcript sezgiseli** (≥3 `Speaker:` satırı veya agent.message referansı → red, 19 §10); 19 §3.1 CMO örneği birebir parse. **Engine** (`@acos/db/approvals` — task-engine/comms ile aynı gerekçe, tek yol): `create` (deterministik id = idempotent replay, gap-free approval_number, chain=[requested], approval.requested + audit), `verdict` (FOR UPDATE, yalnız pending; 2. verdict `already_decided` 409; needs_review'da decided_* boş kalır; approved/rejected + audit + **APPROVAL'da park etmiş task'i `approval_engine` aktörüyle DONE/REJECTED'a taşır** — TaskActorInput'a approval_engine eklendi, envelope'ta system olarak yazılır, `transitionInTx` verdict transaction'ıyla atomik), `expire` (açık-değilse no-op — verdict/sweep/timer yarışı kimin commit'i önceyse onda biter; approval.expired + rejected semantiği), `endorse` (**recursive reports_to CTE doğrulaması — zincir dışı executive → red, 34 T7 forged endorsement imkânsız**; objected bloklamaz), `resubmit` (needs_review→pending AYNI id, chain+=revised), `sweepApprovals` (keşif unguarded [relay deseni], mutasyon guarded; reminder audit_log (approval, fraction) satırıyla exactly-once; `now` enjekte edilebilir — testler uykusuz). **Worker**: `escalate` aksiyonu → 11-alanlı brief kompozisyonu (08 §4 union'ı alt küme taşıdığından eksikler açık placeholder'larla doldurulur — kayıtlı sapma) → idempotent create (uuidv5('approval', stepId), workflow_id session'dan) + agent.escalated (yalnız taze insert'te) + task IN_PROGRESS→WAITING; **workflow 19 §7 birebir bloklar**: `condition(approvalVerdict, until derived expiry)`, sessizlikte `expireApprovalActivity` (sweep ile idempotent yarış) → marker `[signal:approvalVerdict=expired]`; approvalVerdict sinyal union'ına "expired" eklendi. **Server**: `GET /approvals` (status/kind/urgency filtreleri, urgency-desc+oldest-first), `GET /:id` (typed brief + chain + task ref — raw passthrough YOK), `POST /:id/verdict` — **yalnız interaktif Founder session'ı; PAT tümden red 403** (19 §12; 21 §3.10 perm sütununun kayıtlı daraltması), not zorunluluğu şema refine'ında; verdict sonrası post-commit `approvalSignalPort` (Temporal handle.signal, ulaşamazsa DB otoritedir); main.ts'te 30s **expiry sweep** interval'i (expired workflow'lara verdict=expired sinyali). OpenAPI 47 path. **UI**: Approval Center (Inbox/History, urgency pill + time-to-expiry bar, 11 alan sabit sırada + options karşılaştırma tablosu + chain timeline, APPROVE/REJECT/REQUEST EXECUTIVE REVIEW [not zorunlu], 409 yüzeye çıkar); APPROVALS nav aktif; `approval.*` invalidation ailesi. **Kabul**: server int 10/10 (brief/transcript red, endorse + forged red, PAT 403, notsuz reject 400, approve→event+audit+ikinci verdict 409, needs_review→resubmit→pending, parked task DONE/REJECTED, sweep reminder-once→expire→idempotent, detail 11 alan); worker int 2/2 — **escalate workflow'u gerçekten bloklar** (approval pending + task WAITING + describe RUNNING) → Founder verdict sinyaliyle complete; **sessizlikte timer approval'ı kapatır** → approval.expired + abandon (rejected semantiği). Repo 52 task + tüm int süitleri yeşil. Not: Web Push/e-posta bildirimleri (19 §9) MVP'de in-app WS event'i olarak; UI demo e2e'si M3 gate (T36) senaryosuna bağlanacak. |
| T36 M3 gate: toolless delegation E2E | ✅ Tamamlandı | 2026-08-11 | **Kaskad tutkalı** (delegasyon E2E'nin eksik parçaları): (1) **assignment → workflow auto-start** (09 §4 trigger'ı) — server twin modülü `modules/workflows/client.ts` (deterministik id + REJECT_DUPLICATE; assign route'u post-commit best-effort başlatır) + worker'da delegate_task başarısında `startAgentWorkflow` portu (main.ts Temporal client'ı; kaskad kendi kendini sürer). (2) **Context sentinel** (`CONTEXT_SENTINEL_UUID`, @acos/llm): aksiyonlar somut id bilmeden "kendi task'im / sıradaki çocuk / uygun report / kendi thread'im" diyebilir — worker dispatch'i deterministik çözer: create_task.parentTaskId→mevcut task, delegate_task.taskId→en eski sahipsiz açık çocuk (FIFO), delegate_task.toAgentId→not'taki `@İsim` mention'ı (kazanır) yoksa en az yüklü uygun DIRECT report (mayDelegate süzgeçli, employee_number tie-break), send_message.channelId→kendi task thread'i; update_task_status/request_review.taskId→kendi task'i (kayıtlı karar — scripted VE live modeller aynı semantiği kullanır). (3) **delegateTask auto-groom**: DRAFT çocuk tek status-yazarından DRAFT→BACKLOG→PLANNED groom'lanır (aktör=delege eden manager; izin matrisi bypass edilmez) — sonra PLANNED→ASSIGNED. (4) **Scripted eşleşme**: `match.agentName` (CEO/CTO ikisi de executive — seed adlarıyla ayrışır) + oturum anahtarına `[ctxTask:]` (aynı generic script'i paylaşan iki dev'in cursor'ları karışmaz); working set'e `[agentName:][ctxTask:]` marker'ları; normalizeStep'e create_task shorthand'i. **4 kanonik YAML** (packages/llm/testing/scripts): ceo.objective-decompose (goal→initiative+delege), cto.initiative-design (epic+delege), em.epic-decompose (2 task + `@Alex Demir`/`@Deniz Kaya` mention delegasyonu), dev.toolless-task (IN_PROGRESS→thread mesajı→request_review). **Kabul (worker int)**: `delegation-cascade` — Founder goal'ü (bütçe 20000¢) → CEO wf → **tam kaskad kendiliğinden**: initiative(CTO,depth1)→epic(EM,depth2)→2 dev task REVIEW'da; owner'lar mention'a göre Alex/Deniz; **pro-rata bütçeler birebir** (16000/12800/6144/4096 = 0.8-havuz×effort ağırlıkları); her dev task trail'i `DRAFT→BACKLOG→PLANNED→ASSIGNED→IN_PROGRESS→REVIEW` hepsi agent aktörlü; ≥5 thread mesajı = agent.message.sent sayısı; CTO/EM workflow'ları COMPLETED; **approvals tablosu BOŞ** (adım 25 M3: Founder inbox'ı boş). **R1 worker-kill**: WAITING'de worker A tamamen kapatılır → workflow RUNNING kalır → worker B devralır → mesajla uyanıp bitirir; stepNo tam [1,2,3,4] (duplikasyonsuz), llm_calls=4, tek session (completed), trail eksiksiz. **Golden replay net'i** (32 §3/09 §7): `gen:histories` script'i (Testcontainers Temporal + canned stub aktiviteler) `test/histories/{toolless-review,approval-escalate}.bin` üretir (binary proto — historyToJSON bu SDK sürümünde payload metadata bytes'ında patlıyor, kayıtlı sapma); `test/replay` süiti **Worker.runReplayHistories ile OFFLINE** her history'yi HEAD koduna karşı replay eder, nondeterminizm CI'da build kırar (worker `test` script'ine bağlı). **E2e**: compose agent-worker'a `LLM_MODE` env'i (CI e2e job'ı `scripted`; doc 32 §5 gereği), `08-objective-to-tasks.spec.ts` (doc'taki 05 senaryosu — repo numarasıyla): UI'dan goal→groom→Aylin'e assign → board'da initiative/epic belirir → iki dev task Review kolonunda → Communication'da task_thread mesajları → Office debug hook'u canlı → **Approval Center boş**; `06-tasks-board` scripted stack'e uyarlandı (probe task artık Baran/QA'ya — script eşleşmez, kart yerinde kalır; kayıtlı not). **Nightly live-LLM**: `live-llm.nightly.int.test.ts` (`LIVE_LLM=1`+ANTHROPIC_API_KEY yoksa skip; 500¢ hard bütçe + guard'lar altında gerçek modelle terminal outcome + hesaplı llm_calls) + `.github/workflows/nightly.yml` (cron 02:00 + dispatch; secret yoksa temiz skip) — M3 DoD'nin "real LLM calls" ayağı gece kapısında, PR CI'ı deterministik. Yerel: repo 52 task + int 11/11 (db 26, server 81, worker 26) + **compose e2e 8/8** yeşil. |
| T37 services/sandbox-manager | ✅ Tamamlandı | 2026-08-11 | **İzolasyon tanımları `@acos/tools`'ta** (gateway T39 ile paylaşılacak, pure — dockerode YOK): 3 seviye `analysis`/`coding`/`testing` + 27 §11 kaynak matrisi birebir (`ISOLATION_LIMITS`: cpu/ram/pids/disk/network) + **`hardenedHostConfig`** (S8): CapDrop ALL, no-new-privileges, ReadonlyRootfs, size-cap'li **noexec** tmpfs `/tmp`, NanoCpus/Memory/PidsLimit, mount YALNIZ caller'ın verdiği (implicit host mount yok); analysis→NetworkMode `none`, egress seviyeleri→internal `acos-workspaces` ağı + `workspaceEnv` proxy env'i (27 §12; başka route yok → bypass imkânsız). 8 unit test. **Contracts sandbox API'si** (execution plane ile Docker-socket sahibi arasındaki TEK kontrat, T40 buradan tip alır): Create/Exec(buffered+streaming ack)/Workspace/TerminalFrame şemaları. **services/sandbox-manager** (S1 — TEK Docker-socket sahibi): `DockerSandbox` (dockerode) create→start (workspaceId idempotent; keepalive `sh -c while sleep`; ro-rootfs'te var olmayan WorkingDir hatasını önlemek için WorkingDir yalnız /workspace mount'u varsa — kayıtlı ince nokta), **buffered exec** (demuxStream, exit+stdout/stderr) + **PTY exec** (Tty stream→TerminalSession), **in-container timeout** (busybox `timeout -s KILL <sec>` → wall-clock'ta ölür, exit 137), destroy (force, idempotent), list (acos label filtreli), **GC** (exited + 6h orphan reaper). **Terminal streaming** (22 §4/§5.2): `TerminalSession` monoton per-session seq + ts, base64 frame, NATS ephemeral `term.<sessionId>` publish + **64KB drop-oldest ring** (byte-hesaplı; late subscriber ring'i alır) + rolling log `/data/terminals/<id>.log`. **Internal-auth** (18 §2): `/healthz` hariç TÜM route sabit-zaman `INTERNAL_API_TOKEN` bearer'ı ister (session/PAT yolu YOK; 401). **Boot**: kontrol düzlemi DIŞINDA — full config yerine yalnız kendi env'lerini okur (DB/master-key sırlarını asla tutmaz, 02 §3), NATS + Docker socket + disk + 60s GC interval'i. compose: sandbox-manager env'ine INTERNAL_API_TOKEN + DATA_DIR eklendi. **Kabul**: unit 14 (auth 401/200, route şekilleri, ring drop-oldest, framing); **S1 yapısal invariant testi** (compose.yaml'ı tarayıp `docker.sock`'u mount eden TEK servisin sandbox-manager olduğunu doğruluyor — başka servise sızarsa build kırar); **Docker-gated int 4/4** (daemon yoksa temiz skip; top-level `docker.ping()` ile collection-time guard): **create→exec→destroy round-trip** (gerçek container'da HostConfig doğrulaması: PidsLimit 512/Memory 4GB/NanoCpus 2/CapDrop ALL/ReadonlyRootfs/NetworkMode; `echo` exit 0 + stdout; list→destroy→list), **fork-bomb pids cap'te ölüyor** (600 bg proc denemesi → 256 cap'te `can't fork` stderr'de; daemon-taraflı inspect ile container hâlâ Running — cap'te fork imkânsızlığı ps'i bile engellediğinden kanıt stderr imzası), **analysis ağsız** (NetworkMode none + container içinde yalnız `lo` arayüzü), **timeout runaway'i wall-clock'ta öldürüyor** (sleep 30 / timeout 1s → timedOut + exit 137). Repo 52 task + tüm süitler yeşil. Not: gerçek workspace image'ları (`acos/workspace-node`) + worktree volume provisioning + `workspace.*` event'leri T38'de (şimdilik default alpine + boş mount). |
| T38 Git model + workspaces | ✅ Tamamlandı | 2026-08-12 | **Git plumbing sandbox-manager'da** (ADR-010/15 §3.1; S1 korunur — git yalnız Docker-socket sahibinin spawn'ladığı helper container'larda koşar, sandbox-manager'da git binary'si YOK): `GitWorkspaces` + `DockerGitRunner` — pinli `alpine/git:v2.45.2` helper'ları **NetworkMode none** ile; bare repolar `acos-repos` named volume'unda (helper'lara `/data/repos` mount'u → `bare_path` doc'taki `/data/repos/<project_id>.git` ile birebir), `ensureBareRepo` idempotent init + **seed boş root commit** (main'siz bare clone'lanamaz), `provisionWorktree` = bare'den fresh clone (15 §3.1'in worktree tanımı) + `task/<n>-<slug>` checkout — **bare repo helper'a bile read-only mount** (provisioning origin'i yazamaz), worktree volume işgalinde idempotent (EXISTS→aynı HEAD). **Shell-safety**: contracts'taki strict pattern'ler (`TASK_BRANCH_PATTERN`, `WORKTREE_VOLUME_PATTERN`, uuid) script'e girebilecek HER değeri doğrular; `removeWorktree` pattern'i repos volume'unu yapısal koruyor. Routes: `POST /internal/v1/repos`, `POST/DELETE /internal/v1/worktrees` (internal bearer). Mount'lara `type: bind\|volume` eklendi (tools+contracts); workspace container'ı worktree'yi **rw `/work`'te** mount eder (15 §3.1 — T37'nin /workspace taslağı düzeltildi); `acos/workspace-node` imajı (node22+git, `pnpm build:workspace-images`). **Kontrol düzlemi `@acos/db/workspaces`** (task-engine ile aynı gerekçe; sandbox'a SandboxPort seam'i — db matris gereği tools'a bağımlı DEĞİL, limits snapshot'ı caller'dan): `WorkspaceService.provision` — repositories get-or-create (`/data/repos/<pid>.git` audit path'i), workspaces satırı provisioning→(port: repo+worktree+container)→ready + `workspace.status.changed`+`workspace.provisioned`; port hatasında failed + `workspace.failed` (tx'li); (task,level) başına tek canlı workspace (partial-uq destekli idempotency). **TEK status yazarı `transition`**: kanonik makine guard'ı (illegal → `WORKSPACE_INVALID_TRANSITION`), merged→`workspace.merged`(branch+mergeCommit), destroyed→destroyedAt+`workspace.destroyed`; merged/discarded **soft lock'ları otomatik release** (+`workspace.lock.released`). **Soft locks (15 §3.8, warn-not-block)**: `acquireLock` çift-yönlü prefix overlap sorgusu (aynı repository, diğer workspace'ler) → yapılandırılmış `conflicts[]` UYARI (yazma asla bloklanmaz) + `workspace.lock.acquired`/`workspace.lock.conflict` event'leri; idempotent re-acquire event üretmez; `liveLocks` delegasyon planlaması için. **Terminal kayıtları**: open/close → terminal_sessions satırı (`/data/terminals/<id>.log`) + `workspace.terminal.opened/closed`, close idempotent. Kayıtlı sapmalar: volume adı `ws-<task_number>-<uuid8>` (doc `ws-<n>` per-company sayaç → host-global çakışma riski); base_commit/last_activity_at kanonik şemada kolon yok (base commit caller'a döner, git'te yaşar). **Kabul**: sandbox-manager unit 24 (git script/validation/route süitleri dahil); **Docker-gated git int 4/4** — bare repo idempotent seed; **bir projede iki task → tam izole worktree'ler** (aynı baseCommit'ten `task/81-add-oauth-login`/`task/82-fix-signup-form` branch'leri — HEAD dosyasıyla kanıt; ws-81'e yazılan dosya ws-82'de YOK → ISOLATED probu), re-provision idempotent, bare'siz proje `REPO_NOT_FOUND`; **PG int 8/8** — iki task tek repository satırından izole kayıtlar (branch/volume ayrışık, port'a giden değerler satırla birebir), idempotent provision (port'a 0 çağrı), port failure→failed+event→destroyed, ready→merged illegal 409-tipli, **`src/auth/` kilidi varken `src/auth/login.ts` yazan ikinci task'e yapılandırılmış conflict uyarısı + `workspace.lock.conflict`** (taskIds her iki task), non-overlap temiz, merge'de release + ws82 kilitleri dokunulmadan, terminal open/close+idempotency. Repo 52 task + tüm int süitleri (server 81, worker 26, sandbox 8, db) yeşil. |
| T39 Tool Gateway | ✅ Tamamlandı | 2026-08-12 | **`packages/tools` kontratı** (17 §2 birebir, zod eklendi): `ToolDefinition` (input/output Zod, risk R0–R3 [domain enum'u], scopes, sandboxLevel, sideEffectFree, estimateCost, timeoutMs, rateLimit override, credentialRefs, S6 founderCategory) + risk-sınıfı default rate limitleri (17 §6) + `matchesToolPattern` (exact/`git.*`/`*`) + `elevateRisk` (taint, R3 cap) + `scrubSecretEnv` (S2 — /TOKEN\|KEY\|SECRET\|PASS/i). **Registry fail-closed**: ad deseni + duplicate reddi; **13 MVP tool** 35 §9 listesiyle birebir (fs.read/write/search, git.commit/branch/diff/merge, terminal.run, db.inspect [şema katmanında SELECT-only refine], web.fetch/search, task.query, memory.search) — 17 §3.1 şekilleri korunarak (terminal.run=terminal.exec şeması; web.search credentialRefs=['search.api_key']). 18 unit test. **Gateway** (`apps/server/modules/tools/gateway.ts`, 17 §4 normatif sıra, fail-closed): (1) identity — agent aktif+şirkete ait; geçersizse `tool_invocations`'a FK yazılamayacağından **audit_log'a** düşer (audit-always korunur); (2) registry+şema — parse ÖNCESİ S2 env scrubbing; (3) grant lookup `tool_permissions` agent>position>org_unit önceliği (member_of edge'lerinden unit'ler), aynı kademede **kesişim birleştirmesi** (array∩, sayı=min, branchPattern'ler AND, onCapExceeded=escalate yalnız hepsi opt-in ise); (4) constraint'ler — pathPrefixes/repoAllowlist/branchPattern/domainAllowlist/spendCapCents/monthlySpendCapCents (30g cost_entries toplamı)/maxTimeoutSec (**clamp**, deny değil); cap aşımında `onCapExceeded:'escalate'` → require_approval(manager) (17 §4.2 ör.2); (5) policy bantları deny→require_approval→allow (18 §4.2; priority asc; `standing_approval`+`tool` kind'ları; condition maxCostCents/riskAtMost/requiresTaint); allow eşleşmesi = R3 standing grant; (6) bütçe — en sıkı kapsam: task total (hard, T28) yoksa şirket daily; (7) **karar TEK kanonik `authorize()`'dan** (T10 domain, 06 §2) — gateway yalnız girdilerini hesaplar; taint → `elevateRisk` + `decision_detail.elevated_from` (17 §7.4); policy require_approval allow'u çevirir; (8) rate limit — T11 `rate_limits` UNLOGGED token bucket'ı (kayıtlı sapma: 17 §6'nın tool_rate_counters taslağı yerine; Redis'siz) agent+company anahtarları, deny `RATE_LIMITED`+retryAfterSec+`tool.rate.throttled`. **Audit her karardan**: tool_invocations (denied/awaiting_approval/dispatched→succeeded/failed) + `tool.invocation.requested/denied/completed` event'leri aynı tx'te; **R2+ audit_log'a mirror (S7)**. **Dispatch seam'i** `ToolDispatchPort` (gerçek sandbox/egress/adapter dispatch T40'ta; `app.toolDispatchPort` geç bağlanır) — timeout Promise.race, output Zod doğrulaması, `cost_entries` (kind=tool, id=invocationId exactly-once), idempotencyKey → `idempotency_keys` (endpoint tools.invoke; kayıtlı sapma: kanonik 20 §12.3'te kolon yok) replay'de kayıtlı yanıt. **S2 credential injection**: `credentialRefs` YALNIZ dispatch anında sealed-box `secrets`'tan (T16 unsealSecret) çözülür, audit'e asla girmez. Approval OLUŞTURMA çağıranın escalate makinesinde (19 §7, T40 bağlar) — kayıtlı karar. **Route**: `POST /internal/v1/tools/invoke` — sabit-zaman INTERNAL_API_TOKEN bearer (17 §1 WRITER-DECISION'daki ayrı token yerine mevcut config anahtarı — kayıtlı sapma), session/PAT yolu YOK, OpenAPI'den gizli (snapshot değişmedi). **Kabul — int süiti 17/17**: **ALLOW** (fs.read R0 L2 → dispatched+output validated+succeeded+events); **DENY×2** (grant yok → NO_PERMISSION_GRANT; pathPrefixes ihlali → GRANT_CONSTRAINT_VIOLATED:path_not_in_prefixes — aynı position grant'i apps/web/ altında allow); **REQUIRE_APPROVAL** (git.merge R2, L2 → manager + awaiting_approval; L5 CEO → succeeded); branchPattern main reddi/task-* geçişi; spend cap default deny vs escalate→approval; hard task bütçesi BUDGET_EXCEEDED; **policy deny grant'i kırar**; **taint zinciri**: tainted R2→R3 founder — standing allow İLE BİLE allow'a düşmez, approver founder→manager'a iner + audit'te elevated_from (S5 invariantı: injeksiyon en fazla DAHA incelenen aksiyon); **audit-always**: bilinmeyen tool/geçersiz input/ghost/paused agent — 4 yol da satır bırakır (identity → audit_log); R2+ audit_log mirror'ı; **S2**: GITHUB_TOKEN env'i audit+dispatch'te yok, sealed-box search anahtarı yalnız port'a gider; rate limit 4. çağrıda deny+retryAfterSec+throttled event; idempotent replay ikinci satır açmaz; internal HTTP 401 (tokensız + session'la) / token'la uçtan uca. Repo 52 task + tüm int süitleri yeşil. |
| T40 workers/execution-worker | ✅ Tamamlandı | 2026-08-12 | **Wire kontratı `@acos/contracts/tool-invoke`**: ToolInvokeWireRequest/Response Zod çifti — server route'u, execution-worker istemcisi ve (yapısal olarak) agent-worker istemcisi AYNI şemayı konuşur (agent-worker matris gereği contracts'a bağlanamadığından istemcisi yapısal-tipli düz fetch — kayıtlı not). **Gerçek dispatch kolu** (`apps/server/modules/tools/dispatch.ts`, 17 §4 adım 7): `createSandboxDispatchPort` — workspace-seviyeli tool'lar için T38 `WorkspaceService.provision` (bare repo→worktree volume→hardened container; SandboxPort'un HTTP implementasyonu sandbox-manager'ın internal API'sine) + ready→in_use geçişi; **terminal.run** (buffered exec `/work`'te, HOME=/tmp+npm_config_cache — ro-rootfs'te npm çalışır; terminal_sessions satırı open/close + compute cost ceil(ms/60000)), **fs.read** (file/dir + maxBytes truncation), **fs.write** (base64 yazım + **T38 soft lock upsert → lockConflicts[] yapılandırılmış uyarı**), **fs.search** (grep, exit 1=eşleşme-yok sonuçtur), **git.diff**, **git.commit** (15 §3.4: `Task: TASK-n` trailer server-side türetilir — farklı task referanslanamaz; author=agent kimliği `<Ad> <agent-NNN@slug.acos>`, asla model); git.branch/merge→T43, db.inspect/web.*→T42/T45 tipli failure (gateway failed olarak audit'ler). Shell-safety: rel-path guard (.. / mutlak / NUL reddi) + tek-tırnak kaçışlı quoting. Boot: main.ts `app.toolDispatchPort`'u config.sandbox.managerUrl ile bağlar. **execution-worker tam yeniden yazım** (09 §3/§4.1): activities-only Temporal worker'ı `execution` kuyruğunda (workflow YOK; maxConcurrentActivity **16**, shutdownGrace 30s, healthz ready-gate) — **4 kanonik aktivite**: `runCommandActivity` / `runTestsActivity` (default `npm test`, 600s) / `buildActivity` (default 1800s) / `gitOperationActivity` (diff|commit) — hepsi Tool Gateway internal HTTP'sinden (S3: execution plane sandbox-manager'a da DB'ye de ASLA doğrudan gitmez; S1 socket'siz); **10s heartbeat** interval'i koşu boyunca (08 §12 sınıfı; MockActivityEnvironment'ta kanıtlı); **sonuç semantiği**: non-zero exit + gateway deny SONUÇTUR (workflow akıl yürütür), yalnız transport/5xx `GatewayUnreachableError` fırlatır (retry sınıfı). compose: execution-worker env SERVER_INTERNAL_URL+INTERNAL_API_TOKEN (SANDBOX_MANAGER_URL kaldırıldı — S3), server-healthy dependency; agent-worker'a SERVER_INTERNAL_URL. **Agent loop wiring**: `deps.invokeTool` seam'i — use_tool artık gateway'e gider (idempotencyKey=`tool:<stepId>` deterministik ⇒ Temporal retry'ları kayıtlı sonucu replay'ler); **kanonik 32 §6.1 script sözlüğü → registry eşlemesi** (`mapToolCall`: write_file→fs.write, run_command/run_tests→terminal.run, read_file→fs.read; bilinmeyenler passthrough — gateway fail-closed); gözlem şekli scripted dalların anahtarladığı exitCode'u korur; seam yokken (dar guard/loop testleri) T32 stub gözlemi aynen — **golden replay net'i değişmedi** (workflow koduna dokunulmadı; kayıtlı sapma: agent-loop tool çağrıları executeActionActivity üzerinden agent-tasks kuyruğunda gateway'e gider, execution kuyruğunun 4 aktivitesi intake/review/CI workflow'larının ağır koşuları için — 08 §12 tablosunun kuyruk sütununa bilinçli daralma). **Kabul**: (1) **Docker-gated dispatch süiti 3/3** — sandbox-manager GERÇEK child process (compose topolojisi), fs.write package.json'u taze provision'lanmış worktree'ye yazar (workspace.provisioned + in_use + branch task/81-…), **terminal.run('npm test') gerçek node:22-alpine hardened container'da koşar** (stdout '2 passing', exitCode 0) + **tool_invocations succeeded satırı (costCents+durationMs) + cost_entries kind=tool + terminal session open→closed**; başarısız komut exit 3 SONUÇ olarak döner; (2) **scripted-loop int 2/2** — backend-dev YAML'inin iki use_tool'u gateway seam'inden `fs.write`+`terminal.run` olarak çıkar (idempotencyKey ^tool:uuid$), happy-path'te rework dalı atlanıp task REVIEW'da, step sırası birebir; (3) execution-worker unit 7/7 (heartbeat kanıtı, default komutlar, deny/non-zero sonuç, unreachable retry sınıfı, git op eşlemesi, bearer + 5xx istemci); (4) **check-deps: execution-worker db'siz** (matris değişmedi, 13 workspace uyumlu). Repo 52 task + tüm int süitleri yeşil. |
| T41 Terminal streaming UI | ✅ Tamamlandı | 2026-08-12 | **Frame kaynağı zinciri tamam** (22 §5.2, _DECISIONS §16): sandbox-manager'da session registry (bounded 200; `terminalSession()` ring erişimi) + `GET /internal/v1/terminals/:id/ring` (canlı ring → yoksa **rolling-log tail fallback'i** 256KB, torn ilk satır düşürülür) + `GET .../log` (tam scrollback düz metin) + **`waitForResult` streaming exec** — frame'ler `term.<id>`'ye canlı akarken buffered sonuç senkron döner (TTY modunda stdout artık hem frame hem buffer; T37'nin fire-and-forget 202 yolu korunur) + **7 gün log retention sweep'i** GC interval'inde. **WS gateway**: `term.>` NATS aboneliği → `terminal:<sessionId>` abonelerine seq-dedupe'lu fanout (`frames` anahtarıyla — events'ten ayrı, 22 §4); subscribe/resume → sandbox-manager ring'inden replay (after_seq filtresi), **ring penceresinden eski resume'a `gap` frame'i** (kabul edilmiş kayıp — client re-resume ETMEZ, truncation marker basar; lossy sözleşme) sonra canlı akış; TerminalRingSource seam'i app.ts'te internal-token'lı HTTP ile bağlanır. RealtimeClient: `frames` dalı cursor persist eder (reconnect → after_seq ring replay). **REST** (24 §6.9): `GET /companies/:id/terminals` (workspace/task/agent join'li liste; **founder/admin — route guard topic authz'ı aynalar**) + `GET .../terminals/:id/log` (sandbox-manager proxy'si, attachment; OpenAPI 48 path). **UI**: TERMINALS nav aktif — session listesi (status/task/agent/isolation/zaman) + attach pane: **xterm.js** (@xterm/xterm, DOM renderer, read-only, JetBrains Mono) `terminal:<id>` topic'inden base64 decode ederek yazar, gap'te `--- output truncated ---`, follow-tail toggle + log indirme; dispatch terminal.run artık `sessionId+waitForResult` ile koşar (canlı PTY + sonuç). **Seed upgrade (additive)**: engineering/backend/frontend unit'lerine `fs.*`/`git.*`/`terminal.run`/`task.query`/`memory.search` grant'leri — mevcut kurulumlarda da uygulanır (agent-varsa-erken-dön yolu grant'leri yine işler). CI e2e job'ına `pnpm build:workspace-images` adımı. **Yol üstü düzeltmeler**: (1) 21 int test dosyasının pg Pool'una `error` dinleyicisi — container kapanırken idle-client FATAL 57P01'i vitest'i unhandled-error'la kırıyordu (T40 CI kırmızısının kökü); (2) testlerin oluşturduğu `acos-workspaces` ağı compose.yaml ile birebir (subnet 172.30.0.0/16 + compose label) — etiketsiz ağ compose boot'unu reddettiriyordu. **Kabul**: sandbox unit +7 (ring route'ları/log tail/retention/traversal guard); **ws-gateway int 8/8** — subscribe→ring replay [1,2]→NATS canlı frame 3→**WS kill→resume after_seq=3 ⇒ tam kaçırılan kuyruk [4]**; ring penceresi dışı resume ⇒ `gap{3..6}` + [7,8]; **e2e `09-terminals` yeşil (26.7s)** — gerçek zincir: internal gateway'den terminal.run (seed grant'li mühendis, taze provision'lanan acos/workspace-node workspace'i) → Terminals view'da session belirir → **xterm'de canlı npm çıktısı** → **reload (WS kill) → ring replay'de erken satırlar + akış devam** → invoke sonucu succeeded exit 0 + costCents≥1. Repo 52 task + tam int bataryası + tüm e2e süiti yeşil. Not: PTY exec'in ayrı interaktif oturumu ve xterm search T43+ cilası; canonical dosya adı sapması: spec repo numaralandırmasıyla `09-terminals.spec.ts` (handoff 08 der; 08 objective-to-tasks'a T36'da verildi). |
| T42 Projects + intake | ✅ Tamamlandı | 2026-08-12 | **Ingest (sandbox-manager)**: `POST /internal/v1/repos/ingest` — `git_url` clone'u (**bridge network'lü helper**: clone repo kodu çalıştırmaz, container unprivileged+kısa ömürlü, URL Founder girdisi — 27 §12'nin bu TEK sistem operasyonu için kayıtlı yumuşatması; testler için host-gateway alias'ı) sentinel-parse'lı (::created/branch/head/size/ref), `empty`=greenfield (T38 seed'li bare); **intake worktree volume'u** `ws-0-<uuid8>` (task 0 rezerve; history dahil clone, analysis container'lara **RO mount — P2**); local-path import host-path eşleme kararına ertelendi (kayıtlı sapma — MVP git URL). **Analyzer'lar (execution-worker, 14 §3.1 doc-atamalı doğrudan sandbox yolu — agent tool'ları gateway'de kalır S3)**: 8 MVP analyzer (repo_profile/languages/structure/dependencies/tests/docs/config_env [S2: yalnız env ADLARI]/security_smells [redakteli — eşleşme asla alıntılanmaz]) — `node -e` argv-array (shell yüzeyi yok), **TEK paylaşımlı analysis container** (kayıtlı sapma: per-analyzer fresh yerine), 5dk bütçe, hata=SONUÇ (bölüm degrade olur, P6). **ProjectsService (@acos/db)**: create (proposed→intake tek tx + project.created/status.changed/intake.started), canonical machine guard'lı transition, recordIngest (repositories + project.imported), saveIntakeReport (**P6: tam bir artifact — idempotent**, artifact.created + intake_report_artifact_id), topExecutive (reports_to'suz executive), **routeIntake** — GOAL→CEO (rapor context.artifactIds'te) **founder otoritesiyle** (import komutu yetkidir — kayıtlı karar; matris system'e groom/assign vermez) + project.analysis.completed + intake→active (14 §1.2'nin CTO adımına kayıtlı daralma). **projectIntakeWorkflow** (intake kuyruğu; sandbox aşamaları execution proxy'siyle): ingest (10dk, tek fail-eden aşama → failIntakeActivity timeline'a Founder-görünür hata — machine'de intake→proposed edge'i yok, kayıtlı sapma) → 8'li paralel fan-out (catch'li — degrade) → workspace cleanup → **deterministik 16-bölümlük rapor** (`intake/report.ts` — 14 §3.2 başlıkları BİREBİR; LLM interpretif pas canlı-cilaya kayıtlı daralma) → routeIntake (+startAgentWorkflow → scripted cascade CTO/lead task'lerini üretir); greenfield: analyzersız route. **REST+UI**: POST/GET projects + /report (OpenAPI 51 path); SDK projects namespace; **PROJECTS nav aktif** — 3 alanlık form (P4), liste, detay Overview + Intake Report tab'ı. **Motor düzeltmeleri (gerçek bug'lar)**: (1) **çocuk task projectId mirası** (TasksService.create — parent'lı task parent'ın projesine ait, 14 §1; cascade görevleri projesiz kalıyordu); (2) **worktree volume adı uuid'nin SON 8 hex'i** (ilk 8 = uuidv7 timestamp üst bitleri — aynı ~65s penceresinde ÇAKIŞIYORDU; hostile testi temiz fixture'ın worktree'sini yeniden kullanmıştı — T38'deki üretim riski de kapandı); (3) **createWorkspace 409 TOCTOU iyileşmesi** (paralel analyzer'lar idempotency check'ini yarışıyordu — kaybeden kazananı benimser). **Kabul (Docker-gated int 2/2, demo 6–7)**: host'tan dumb-git-HTTP ile servis edilen fixture repo import'u → gerçek zincir (intake wf → execution aktiviteleri → sandbox child → bare+RO worktree+analysis container) → **16 bölümlü rapor artifact'ı** (fixture gerçekleri: fastify/DATABASE_URL/.ts histogramı; 8/8 analyzer ok) → repo satırı P1 path'iyle → proje active → **GOAL→CEO + cascade CTO initiative/EM epic/2 dev task** (projectId miraslı); **HOSTILE fixture**: bozuk manifest ≥1 analyzer degrade + `_analysis unavailable_` + **planted secret'lar redakteli** (AKIA/parola raporda YOK) + rapor+routing yine de tamam — **intake oluşturmayı asla bloklamaz**. **e2e `10-projects` yeşil**: UI'dan 3 alanla greenfield → intake→active (Founder'a soru yok) → detay Overview + /data/repos origin'i → boş rapor durumu → board'da 'Deliver:' GOAL cascade'i. Repo 52 task + tam batarya + e2e 10/10 yeşil. Not: Card testid'leri inner-wrapper ile (UI Card ekstra prop spread etmiyor); memory seeding aşaması T44'te. |
| T43 Engineering review flow + injection defenses | ✅ Tamamlandı | 2026-08-12 | **M4 kapanış görevi.** **ReviewsService (@acos/db, tek yol)**: `requestReview` (get-or-reset — changes_requested satırı AYNI reviewId'de pending'e döner, 15 §5; **reviewer independence** author≠reviewer erken red + DB CHECK'i, T12); `pickReviewer` en az yüklü uygun ajan (rol→kind eşlemesi, aynı takım önce, author asla); `verdict` — FOR UPDATE, kararı **TEK status-yazarından reviewer kimliğiyle** sürer (code approved→REVIEW→QA, changes_requested→CHANGES_REQUESTED, qa approved→merge-eligible, qa rejected→QA_FAILED); `mergeEligibility` (tüm review satırları approved); review.requested/started/completed event'leri. **reviewWorkflow** (agent-tasks kuyruğu): startReview→decide→submit; MVP deterministik verdict politikası (ilk code turu changes_requested, re-review approve, qa approve — demo-19 şekli; canlı-LLM reviewer nightly'de; "prior code round" `verdictMd`'den okunur çünkü re-review reset'i decidedAt'i null'lar); submit follow-up'ları sürer: rework re-entry (owner loop'u verdict pre-seed'li yeniden başlar — `initialReviewVerdict`, allowReentry=ALLOW_DUPLICATE çünkü önceki run request_review'da KAPANDI), QA turu, LEAD merge. **request_review dispatch**: checkpoint (auto commit+push — yalnız workspace'i olan task'ta) → REVIEW → review satırı + reviewWorkflow start; complete_task rework-sonrası self-approve edemez, re-review'a yönlenir. **git.merge (server-side, 15 §3.6)**: sandbox-manager `mergeTaskBranch` bare repo'da squash-merge + Reviewed-by trailer + main push; **conflict → tipli `conflict{files}` (owner kendi workspace'inde rebase eder, 15 §3.7 — kısmi yazım YOK)**; gateway dispatch eligibility gate'i + workspace→merged (lock release) + reviews merge-commit damgası + task→DONE. `git.branch` push (worktree→bare, task/* only). **S5 injection savunmaları** (`@acos/tools/taint.ts`, versiyonlu): `detectInjection` deterministik (LLM YOK) — override/exec/exfil/role-forgery/fence-escape/b64-imperative-blob/link-mismatch pattern'leri; `provenanceFence` (18 §11.1 — source-tag'li fence, escape nötrleme); `derivesFromTainted` (≥16-char substring). **Gateway output flagging** (17 §7.4): external-provenance çıktı (workspace/web) heuristiklerden geçer → hit'te `tool.output.flagged`+`policy.injection.flagged` (+severe'de `security.alert`) + response `outputFlagged`. **Working-set taint taşıma**: bir session'da bir çıktı flaglenince sonraki tool call'lar `tainted` (sticky bit — kayıtlı MVP kabalaştırması) → gateway effective risk +1; external gözlemler prompt'a YALNIZ provenance fence'inde girer + FENCE_PREAMBLE. **REST+UI**: reviews list + branch diff (OpenAPI 53); TASKS detayına Reviews paneli (durum/reviewer/merge-commit + diff toggle). **Bulunan gerçek bug'lar**: (1) **S8 cap-drop yazma bug'ı (T38'den beri)** — hardened container TÜM cap'leri düşürdüğünden (CAP_DAC_OVERRIDE dahil) root tabanlı workspace imajı bile worktree'ye yazamıyordu; worktree provisioning artık `chown 1000 + chmod go+rwX` (hem node-1000 hem root imajları yazar); (2) **git dubious-ownership** — helper (root) ve container (node) uid'leri farklı → helper script'lerine `safe.directory '*'`; (3) git.branch/git.merge şemaları branch/expectedHeadSha zorunluydu → dispatch workspace'ten çözebilsin diye optional. **Kabul — review-flow int 3/3 (Docker-gated, demo 17–19)**: scripted dev gerçek workspace'te implement → **review(changes_requested) → rework re-entry → re-review(approved) → QA(approved) → LEAD squash-merge → task DONE**; code reviewer bağımsız same-team lead (author≠reviewer), qa reviewer ayrı; **REVIEW→CHANGES_REQUESTED bacağı reviewer kimliğiyle** trail'de; workspace merged + review lifecycle event'leri; **author==reviewer çift-guard** (service reddi + ham insert 23514); **S5**: zehirli workspace dosyası okuması `outputFlagged` + `tool.output.flagged`/`policy.injection.flagged` event'leri, türetilmiş tainted R1→R2 elevation ile require_approval (asla sessiz yürütme). **Injection corpus 55 vaka** (40 saldırı hepsi flag + 15 benign hiç false-positive + fence escape + taint derivation). Repo 52 task + tam int bataryası + e2e yeşil. Not: architecture/security review kind'leri label-matrix'i, CI gate suite'i (`ci.run`) ve Guardian T-sonrası; memory learning candidate'ları T44'te. |
| T44 memoryConsolidationWorkflow | ✅ Tamamlandı | 2026-08-12 | **Konsolidasyon boru hattı uçtan uca (12 §5)**. **Saf kurallar `@acos/domain/memory-rules`** (workflow içinde deterministik koşar, unit-testli): `adjustImportance` (§5.2 — +0.1 escalation/FAILED costly trigger, +0.05 ≥2 evidence, −0.1 entity'siz episodic; discard <0.30, active ≥0.45), `detectMemoryScope` (§5.3 kural 1–5 — relationship→agent, projesiz→agent, files/components→project, kişisel teknik→agent, tie→project; LLM tiebreak deterministik default'a kayıtlı daralma; **company scope yapısal olarak erişilmez**), `computeConsolidationConfidence` (§5.8 formülü birebir — base cap 0.6, +0.15·count(event∨review) max +0.30, +0.25 metric, −0.20 yalnız-statement), `classifySimilarity` (§5.5 bantları: ≥0.95 fast-merge / ≥0.86 compare-merge / 0.70–0.86 compare-no-merge / <0.70 unrelated). **`MemoryCandidate` Zod kontratı `@acos/llm`de** (§5.1 birebir: 8 tip, suggested_scope YALNIZ agent\|project, evidence_refs min 1, 0–8 cap; doc'un domain yerleşimi AgentAction emsaliyle llm'e — kayıtlı sapma) + `parseMemoryCandidates` (bare array/candidates-objesi toleransı, tek repair retry hook'u). **`MemoryConsolidationService` (@acos/db, TEK persist yolu)**: `loadTaskWindow` (significant event penceresi — prefix eşlemesi task./review./tool.invocation./workspace. + agent.escalated; `memory_significant` katalog flag'i yerine kayıtlı daralma), `similarTopK` (**pgvector cosine query-builder'dan** — tenancy guard company_id predicate'ini görür; exact scope + embedding_dim eşit + status active\|candidate, k=8; HNSW partial index'lere hizalı), `resolveEvidence` (§5.7 anti-halüsinasyon — event/review/artifact ref'leri gerçek satıra çözülmeli, çözülmeyen düşer, HEPSİ düşerse hallucinated_evidence discard; metric/statement pass-through w=1/0.5), `persistCandidate` (tek tx: memories + version 1 + evidence + relations + memory.created/relation.created/evidence.added/contradiction.detected/superseded event'leri; **company-scope hard assert**; contradiction'da confidence cap 0.6; importance'a göre active/candidate), `mergeIntoExisting` (fast path + duplicate verdict: içerik korunur, evidence dedupe'la eklenir, version+1 `consolidation_merge`, last_verified_at, memory.updated), `completeRun` (memory.consolidation.completed — katalog payload'ı batchId/candidates/persisted/merged/discarded). **Workflow + aktiviteler (agent-worker, memory kuyruğu)**: `memoryConsolidationWorkflow` — id `memory-consolidation-<company>-<trigger_ref>` (client helper güncellendi); extract→(saf §5.2 skor + §5.3 scope)→evidence→(§5.8 confidence)→embed→top-k→fast-merge \|\| compare döngüsü→persist/merge/discard→rapor `{extracted,persisted,merged,contradictions,discarded,hallucinatedEvidence}`; **embedding hatası retry sonrası NULL embedding + memory.embedding.failed ile persist** (SQL lane'leri servis etmeye devam eder). Aktiviteler: scripted modda **canned consolidation fixture'ları + FNV-1a pseudo-embedding** (32 §6 — tamamen deterministik, model YOK; canlı modda router purpose fast extraction + tek repair retry, purpose embedding); **compare verdict'i her iki modda deterministik bant politikası** (compare-merge⇒duplicate; compare-no-merge⇒aynı tip+farklı içerik⇒contradiction; T43 verdict politikası emsalinde kayıtlı MVP daralması — canlı-LLM compare nightly'ye). **Tetikleyici (12 §5.0)**: task-engine DONE'da artık `task.completed` de emit eder (katalogda vardı, hiç emit edilmiyordu — memory-trigger consumer'ın filtresi co.*.task.completed bunu bekler); **server'da T21 `memory-trigger` durable consumer'ı drain eden `startMemoryTrigger`** — task.completed/task.failed → Temporal start (deterministik id dedupe, already-started yutulur), diğer kind'ler ack (escalation/experiment/N-event tetikleri T45+ — kayıtlı daralma), hata → nak(5s) → max_deliver 5 → DLQ. **Kabul — pipeline int süiti 15/15, HER İKİ boyutta (768+1536, ADR-020)**: (1) extract→persist (demo 20–21): 2 tipli satır (failure→project scope files kuralıyla, procedural→agent), status active, embedding+dim+model satırda, sourceEventId gerçek pencere event'i, version 1 + evidence satırları GERÇEK event id'lerine çözülü, confidence formülü birebir (0.9), memory.created + consolidation.completed sayaçlı; (2) **fast path ≥0.95**: aynı bilgi ikinci pencereden gelince YENİ satır yok — version 2 `consolidation_merge`, evidence birleşti (4), last_verified_at, memory.updated; (3) **compare bandı 0.86–0.95 ⇒ duplicate merge** (Gram-Schmidt ile tam cosine'e yerleştirilen komşu vektör); (4) **0.70–0.86 ⇒ contradiction**: İKİSİ DE yaşar, relations kind=contradicts, aday confidence ≤0.6 cap, memory.contradiction.detected — asla auto-resolve yok; (5) **demo 20 şekli**: FAILED task'ın flaky-test dersi +0.1 costly-trigger ayarıyla 0.40'a — **status=candidate type=failure** satırı gerçek event evidence'ıyla (düşük-önem kuyruğu); (6) importance <0.30 discard; (7) **halüsinasyon**: tüm evidence ref'leri çözülmezse hiçbir şey persist edilmez + rapor sayar; (8) **embedding failure yolu**: NULL embedding + memory.embedding.failed + satır yine servis edilir. **memory-trigger consumer int testi**: publish task.completed/task.failed → doğru input'la start, agent.escalated start ÜRETMEZ. Domain unit +4 (formül/bant/scope/importance). Repo 52 task + tam int bataryası yeşil. Kayıtlı şema notu: memory_evidence'ta task_id/project_id denormalizasyonu kanonik 20 §10 şemasında yok — promotion sayımı (T45) ref join'iyle. **M1 (2026-08-15): 12 §5.0 tetikleyici tablosunun tamamı bağlandı** — "recorded MVP narrowing" (yalnız terminal görev) kaldırıldı. `trigger.ts` artık 4 NATS tetikleyicisi üretiyor: task completion (`task-<taskId>`), **N-anlamlı-olay** (ajan başına sayaç, N = `company_settings.consolidation_event_threshold`, ref `events-<agentId>-<seq>`), `escalation.resolved` (`escalation-<escalationRef>`), `experiment.completed` (`experiment-<experimentId>`); her ref tetikleyen event'ten yeniden üretilebilir, yani `memory-consolidation-<companyId>-<triggerRef>` dedupe'i yeniden teslimde de tutuyor. Reflection + Founder-manual satırları NATS tetikleyicisi değil (13 §7 activity çıktısı / Observatory REST) — bilinçli olarak consumer dışında. Anlamlı-olay kümesi tek tanıma indi (`@acos/events` `isMemorySignificant`); `memory-trigger` durable filtreleri o kümeyi kapsayacak şekilde genişletildi ve var olan durable'lar `consumers.update` ile yerinde uzlaştırılıyor. Toplamalı: `MemoryConsolidationInput.taskId` opsiyonel + `agentId`/`sourceEventIds`, `MemoryConsolidationService.loadAgentWindow` (görevsiz pencere, 12 §5.0 satır 2–4), `TriggerWindow.agent`. Test: 14 unit (sahte NATS) + genişletilmiş int testi. **M2 (2026-08-15, AGENT-BRIEF §5) — uçtan uca kanıt:** `apps/server/test/integration/task-done-memory-chain.int.test.ts` zinciri tek koşuda çakıyor — `complete_task` → REVIEW → bağımsız incelemeci (BİLEREK `manager`, `reviewer` sınıfı yalnız `requestReview`'ün yazdığı `task_assignments` satırından gelir) → CHANGES_REQUESTED → rework → re-review → QA → lead `git.merge` → **doğal** DONE → `task.completed` → outbox relay → JetStream `memory-trigger` → GERÇEK `memoryConsolidationWorkflow` (`memory-consolidation-<cid>-task-<tid>`) → `memories` satırları (INV-15 kapsam kontrolü, kanıt refleri koşunun gerçek olaylarına çözülüyor) → `memory.created` → `/ws`. Kırmızı-kanıt iki noktadan doğrulandı: `reviews.requestReview`'ün atama yazımı geri alınınca "manager may not perform REVIEW → CHANGES_REQUESTED", `trigger.ts`'te `task.completed` dalı kapatılınca görev DONE'a varıyor ama `memories=0`. sandbox-manager HTTP yüzeyi sahte (git plumbing'i T43 Docker testi kapsar). |
| T45 Retrieval in Working-Set | ✅ Tamamlandı | 2026-08-12 | **Working-Set retrieval uçtan uca (12 §7, _DECISIONS §10)**. **Saf kurallar `@acos/domain`**: `recencyDecay` (tip başına half-life — episodic 14g … procedural/semantic 365g; bilinmeyen tip 90g), `MEMORY_TOKEN_BUDGETS` (agent 1500 / project 2500 / company 1000 — bağlayıcı default'lar), `estimateTokens` (ceil(chars/4)), `packMemories` (§7.3 birebir: must-know [SQL-lane] önce → skor desc; title+content sığarken, taşınca title+summary fallback, o da sığmazsa drop sayılır; her blok `[memory <id> \| tip \| conf X]` etiketiyle — aksiyonlar id atıf yapabilir). **`MemoryRetrievalService` (@acos/db, TEK yol)**: (1) **structured SQL lane'ler önce, bütçe-rezervli "must know"** — proje kararları (son 10 decision; embedding NULL olsa bile bulunur — "decisions must never be missed"), rol prosedürleri (company+project scope, importance sırasıyla; rol-tag entity eşlemesi T47'ye kayıtlı daralma), aktif failure'lar (`entities.files ?| workspace-paths` TAM eşleme — anahtar yoksa lane sessiz), relationship'ler (`entities.agents ?| thread-agents`); (2) **semantik lane** scope başına (agent/project/company) cosine top-24, SQL-lane id'lerine dedupe, **bağlayıcı formülle re-rank** (0.55·cos + 0.20·imp + 0.15·recency + 0.10·conf; negatif cosine 0'a clamp'li), embedding_dim filtresi HNSW partial index'lere hizalı; (3) scope bütçesinde paketleme; (4) **YALNIZ status=active satırlar** — candidate/superseded asla dönmez; (5) **`memory_retrievals` UNLOGGED gözlemlenebilirlik satırı** her build'de (migration 0013, el-denetimli UNLOGGED; returned_ids/scores/budget_tokens_used + §7.5 degradasyon bayrakları: empty / truncated [≥%50 starvation] / semantic_skipped / slow [>1.5s]) — insert hatası loop'u ASLA kırmaz. **`applyRetrievalCounts` dakikalık batch (§7.4)**: uncounted satırları FOR UPDATE SKIP LOCKED ile toplar → `memories.retrieval_count += n` tek UPDATE (hot-table write-amplification yok) → counted işaretle → **14 gün retention sweep** aynı işte; server'da 60s interval (approvalSweep gibi, unguarded Db). **`buildWorkingSetActivity` entegrasyonu (08 §8 bölüm 4–6)**: sorgu metni = task title + objective + adım niyeti + son tool hatası → router purpose `embedding` (scripted router'a embedding profili eklendi — pseudo-embedding'lerle deterministik; hedef yoksa **semantik lane atlanır, SQL lane'ler servis etmeye devam eder** §7.5c); failure lane path anahtarları session'ın dokunduğu dosyalardan (agent_steps.action.input.path); Company/Project/Agent memory bölümleri task kartı ile thread arasında; retrieval hatası hiçbir koşulda build'i kırmaz. **Kabul**: **retrieval int süiti 6/6** — SQL lane'ler embedding mesafesinden bağımsız (NULL-embedding decision dahil) + anahtarsız exact-match lane'leri sessiz; re-rank sırası formülle birebir (yakın-ama-zayıf < uzak-ama-güçlü) + dedupe tam 1 kez; active-only; **bütçe testleri** (bölüm başına ≤ bütçe, summary fallback görünür, ≥%50 starvation → truncated bayrağı); semanticSkipped bayrağı; **log + batch**: returned_ids/scores/tokens dolu, retrieval_count tam n artar, ikinci batch 0 uygular (çift sayım yok), 15 günlük satırlar süpürülür. **Builder int 2/2** — bölüm 4–6 gerçek satırlarla etiketli render + embedding-hedefsiz build yine başarılı. **Perf (nightly, RUN_PERF=1 kapılı)**: 100k memory (768d, HNSW partial index; server-side LATERAL vektör üretimi) → 20 build p95 **177ms < 250ms** (aralık 153–178ms). Domain unit +4 (105 test). Repo 52 task + tam batarya yeşil. |
| T46 Promotion + contradiction handling | ✅ Tamamlandı | 2026-08-12 | **Promotion motoru + çelişki çözümü (12 §6, _DECISIONS §10)**. **İnvariant (§6.1): tek olay ASLA şirket politikası olamaz** — konsolidasyon company scope'a yapısal olarak erişemez (T44 hard assert), promotion TEK yoldur ve istatistiksel (eşikler) + sosyal (onaycı) kapılıdır. **Bağlayıcı 3 default kural `@acos/domain.DEFAULT_PROMOTION_RULES`** → `policies (kind=memory_promotion)` satırları olarak **şirket kuruluşunda seed** (`seedPromotionPolicies`, (company,name) uq ile idempotent; CompanyService.create aynı tx — mevcut kurulumlarda yeniden koşmak güvenli): agent→project failure/any (≥3 evidence ≥2 task, lead onayı), project→company any (≥4 evidence ≥2 proje, manager onayı); evidence taban ağırlığı 0.6 (§6.2 WRITER-DECISION). **`MemoryPromotionService` (@acos/db)**: `evaluateCompany` — kural × aktif kaynak bellek eşleşmesi (tip + min_confidence), **evidence istatistikleri ref join'iyle** (event→events[+task fallback], review→reviews→tasks, artifact→artifacts; kanonik memory_evidence'ta denorm kolon yok — T44 kayıtlı notu kapandı), distinct task/proje sayımı, hedef proje = evidence'ın baskın projesi; eşik geçen kaynak için TEK tx'te: **hedef scope'ta `candidate` kopya** (embedding dahil kopyalanır — onaylanınca retrieval'a girer) + version 1 (`promotion_proposal`) + **`derived_from` lineage edge'i** + memory_promotions satırı (evidenceCount/distinctTaskCount/approver/rulePolicyId) + `memory.promotion.proposed` & `memory.relation.created` event'leri; **dedupe**: kaynak+hedef-scope başına tek açık/onaylı teklif. Onaycı çözümü: lead → kaynak ajanın reports_to zincirinde ilk lead/manager/executive (fallback şirketteki ilk uygun rol), manager → ilk manager/executive, founder → NULL (Founder Observatory'den karar verir). `decide` — FOR UPDATE, yalnız atanan onaycı VEYA Founder(null); approve → kopya `active` + version (`promotion_approved`) + **`memory.promoted`**; reject → `rejected` + not version satırında; **orijinal her durumda kendi scope'unda aktif kalır (kopyalar, taşımaz)**. `resolveContradiction` (§6.4.2) — kazanan seçilir: kaybeden → `superseded` + version + **winner→loser `supersedes` edge'i** + `memory.superseded`; kaybedenden türetilmiş promoted torunlar (derived_from) **döndürülür/bayraklanır, ASLA otomatik arşivlenmez**. **Tetikleme (§6.2)**: `evaluatePromotionsActivity` memory aktivitelerinde; `memoryConsolidationWorkflow` persist+merge>0 olan HER koşudan hemen sonra çağırır ("immediately after any consolidation merge that adds evidence" birebir; nightly cron lane'i aynı aktiviteyi kullanır — zamanlayıcı kaydı T50 hardening'e). Kayıtlı daraltmalar: LLM generalization rewrite'ı yerine birebir kopya (nightly-live lane); onaycıya review_request mesajı yerine proposed kuyruğu (Observatory T48 yüzeyler). **Kabul — promotion int süiti 8/8**: 3 kural idempotent seed; **agent→project uçtan uca** (3 evidence/2 task → proposed satır [lead onaycı, rulePolicyId dolu] + candidate kopya + derived_from + event; **yanlış ajan karar veremez** WRONG_APPROVER; lead onayı → active + memory.promoted + version zinciri proposal→approved; **orijinal aktif**; re-evaluate dublike üretmez); **negatif: tek task'tan 3 evidence HİÇBİR ŞEY önermez** (tek olay kuralı) + 0.6 altı ağırlık sayılmaz; **project→company**: 2 projeye yayılan 4 evidence → manager onaylı company kopyası (scopeRef NULL), tek-projeli kontrol önerilmez; **konsolidasyondan company scope MEMORY_SCOPE_FORBIDDEN** (tek yol promotion — negatif test); reject yolu (not version'da + double-decide ALREADY_DECIDED); **çelişki çözümü** (kaybeden superseded + supersedes edge + event; torun bayraklı ama aktif). **Pipeline zincir testi (16/16 süit)**: eşiği aşan konsolidasyon merge'i → workflow'un kendisi teklifi üretir (proposed + lineage) → founder onayı → kopya aktif + memory.promoted. Repo 52 task + tam batarya yeşil. |
| T47 Skills & careers | ✅ Tamamlandı | 2026-08-12 | **Kanıta dayalı beceri seviyeleri + kariyer akışı (13 §2–6/§10, _DECISIONS §11)**. **Saf formül `@acos/domain/skill-rules`** (seviye ASLA LLM'den gelmez): `EVIDENCE_WEIGHTS` + bant clamp'leri (13 §3 tablosu birebir — task_success +0.3, review_accepted +0.5, production_result ±0.7 işaretli, failure −0.4, failure_resolved +0.6…), `decayedSkillScore` (S = Σ w·2^(−yaş/180g)), `computeSkillLevel` — eşikler L2:3.0(+≥5 kanıt) / L3:8.0(+≥2 farklı pozitif tür + kabul sinyali) / **L4:16.0(+promotion_review artifact'ı) / L5:28.0(+pozitif production_result + İKİNCİ artifact)** (senior+ artifact kuralı §11 birebir); **aşağı hareket otomatiktir** — eşik−1.0 histerezis altına düşünce artifact'sız iner. **Kabul (1): golden sayısal vakalar** — §4.3 işlenmiş örnek RAKAM RAKAM (9 satırın decay katkıları, S=1.840→2.429, histerezisle L2 tutuşu, anti-gamification: 10 ayda 9 pozitif satır L3'e YETMEZ; sürekli taze yüksek-değer kanıtla 8.0 aşımı→L3; L4/L5 artifact kapıları skor ne olursa olsun; sınır bantları clamp) + **monotonluk özellikleri** (pozitif kanıt skoru asla düşürmez, yaşlanma asla yükseltmez, negatif kanıt seviye yükseltmez, failure+failure_resolved çifti net pozitif AMA temiz review_accepted'ın altında §5.5, histerezis yalnız tutar asla kaldırmaz) — domain 117/117. **`SkillsService` (@acos/db, TEK kanıt yazarı)**: `ensureSkill` (taksonomik get-or-create + skill.created), `appendEvidenceInTx` — ilk kanıt agent_skills satırını L1'de açar → bant-clamp'li kanıt satırı → **TAM izden deterministik recompute** → level/confidence/evidence_count/last_used_at güncelle → skill.evidence.recorded (+seviye değişince agent.skill.updated); promotion_review artifact işareti `note='promotion_review:<id>'` konvansiyonuyla (kanonik şemada kolon yok — kayıtlı). **Kanıt üreticileri (§5, canlı hook'lar)**: task-engine tek-yazarında DONE→task_success, FAILED/QA_FAILED→failure (task'ın `context.skills` etiketlerinden — etiketsiz task no-op, mevcut akışlar kırılmaz); ReviewsService.verdict'te **REVIEW→QA geçişi = review_accepted** (ref=review id, aynı tx). production_result/peer_eval/experiment/failure_resolved üreticileri + 5dk debounce + nightly decay sweep kayıtlı daralmayla sonraki dilime (deploy penceresi/eval task'ları henüz yok). **Kariyer akışı (§6.1)**: `recommendPromotion` — agent.promotion.recommended event'i + **lead/expert hedefi Founder-gated** (`promotionNeedsFounder`; Approval Center isteğini çağıran açar), altı manager zinciri; `applyPromotion` — seniority + **§6.2 default autonomy eşlemesi** (junior L1…lead L4) + agent.promoted. Eligibility scan'i positions.core_skills kolonu kanonik şemada olmadığından manager-judgment girişli (kayıtlı daralma). **REST+UI**: `GET /companies/:cid/skills/matrix` (OpenAPI 54 path) + SDK skills namespace + **SKILLS nav aktif** — ajan×beceri matrisi, seviye rozetleri (L1–L5 renk tonları), kanıt sayısı, confidence tooltip'i; canlı 15s refetch. **Kabul (2) — skills int süiti 5/5 (demo 22)**: scripted zincir ASSIGNED→IN_PROGRESS→REVIEW→(bağımsız reviewer verdict)→QA→DONE gerçek tek-yazarlardan — **review_accepted(+0.5, ref=review:id) + task_success(+0.3, ref=task:id) satırları her iki etiket için** + recompute (2 kanıt, L1, confidence>0); FAILED→failure(−0.4) + etiketsiz task no-op; 15 kanıtlık tırmanış → **L3 + agent.skill.updated{toLevel:3}** + matriste görünür; L4 artifact kapısı servis yolunda da tutuyor; promotion önerisi (2 event) + founder gate ayrımı + applyPromotion etkileri (senior + autonomy 3 + agent.promoted). Repo 52 task + tam batarya yeşil. |
| T48 Memory Observatory | ✅ Tamamlandı | 2026-08-12 | **Observatory MVP dilimi (12 §8; demo 21)** — her görünüm GERÇEK satırlardan, simüle veri yok. **REST (`/api/v1/companies/:cid/memories…`, OpenAPI 59 path)**: liste+arama (scope/type/status/q filtreleri — q ILIKE; `websearch_to_tsquery`+semantik hibrit arama kayıtlı daralmayla canlı-LLM lane'ine) **+ §8.7 kuyruk rozetleri aynı yanıtta** (contradictions/lowConfidence); **graf** (düğümler + memory_relations kenarları, 500 düğüm sunucu cap'i + capped bayrağı); **çelişki kuyruğu** (çözülmemiş contradicts çiftleri yan yana içerikle; çözülen çiftler kuyruğu terk eder) + **çözüm endpoint'i Founder-only** → T46 `resolveContradiction` (kaybeden superseded + supersedes edge — TEK implementasyon); **detay/provenance** (kaynak event + yaratıcı ajan adı + evidence satırları + yönlü ilişkiler [derived_from lineage dahil] + version geçmişi); **Founder edit/archive PATCH'i** → `MemoryConsolidationService.founderUpdate` — **version satırı (changed_by founder) + audit_log satırı (S7) + memory.updated/archived event'i tek tx'te; hard delete yok** (§8.8 birebir). SDK `api.memories.*` namespace'i. **UI (`/memory`, MEMORY nav aktif)**: LIST (tablo: title/type/scope/imp/conf/retrieval_count/status; tip+durum filtreleri + arama; canlı 10s refetch), **GRAPH** (Cytoscape — düğüm boyutu ∝ importance, opaklık ∝ confidence, renk scope'a göre, candidate kesikli amber çerçeve; kenar stilleri §8.2 birebir: contradicts kırmızı kesikli / derived_from kalın mor / supports yeşil / supersedes gri / related_to ince; tıkla→inspector), **QUEUES** (çelişki çiftleri + Founder 'Keep this' çözümü + kırmızı rozet sayacı), **Inspector paneli** ('Why this exists' + Evidence + Related + History bölümleri) ve **Founder edit formu** (title/importance + archive). Kayıtlı daralmalar: timeline/cluster görünümleri + semantic arama + graf komşu-genişletme + low-confidence kuyruğu aksiyonları T50 cilası/canlı lane (liste zaten zaman sıralı; cluster tabloları nightly job'suz boş). **Kabul — e2e `11-learning-and-memory` yeşil (demo 20–21 GERÇEK zincirle)**: Founder task'ı REST'ten FAILED'a geçirir → task.failed outbox→NATS→memory-trigger consumer→**memoryConsolidationWorkflow compose stack'inde gerçekten koşar** → 'Signup e2e test is order-dependent' failure belleği (importance 0.45 → active) Observatory listesinde belirir → tip filtresi → **inspector'da gerçek evidence (event ref'leri) + v1 consolidation history** (provenance ✓); seed'li çelişki çifti → rozet + kuyruk → **Founder 'Keep this' → kaybeden superseded, kuyruk boşalır** (DB'den doğrulanmış); **Founder edit** → başlık değişir + version 2 changed_by=founder. Tam e2e süiti 11/11 + repo 52 task + tam batarya yeşil. |
| T49 Executive report + cost dashboards | ✅ Tamamlandı | 2026-08-12 | **CEO yönetici raporu + maliyet panoları (26 §7–9/§12; 29 adım 23–24)** — para yolunda LLM yok, her sayı GERÇEK defterden. **`ExecutiveReportService` (@acos/db)**: `maybeCompleteProject` — projenin TÜM task'leri terminal + ≥1 DONE olunca kanonik makineden active→completed (demo 23; ikinci çağrı makine guard'ıyla no-op); `generateReport` — **deterministik markdown** (Outcome: task sayıları + merge'lenmiş teslimat sayısı reviews.merged_commit'ten; **Cost: cost_entries defterinden tür bazında toplamlar + isim çözümlü top-3 harcayan** [reviewer atıf kuralı 26 §13 korunur]; **Learnings: proje-scope bellekler** importance sırasıyla) → `executive_report` artifact'ı (proje başına idempotent) + `artifact.created`/`project.completed{reportArtifactId}`/`report.published` event'leri + **CEO'nun Founder DM'ine status mesajı** (ChannelService dm[ceo,founder] + MessageService.send, refs artifact+project — 29 adım 24 birebir; executive'siz şirket yine artifact alır). **Tetik**: memory-trigger consumer'ın terminal-task sinyaline `onTaskTerminal` kancası (T44 zincirine ek — ayrı consumer yerine kayıtlı paylaşım). **Saf forecast (@acos/domain)**: `projectedSpendCents` (26 §8 — trailing-24h/24 oranı, <24h veri için dönem ortalaması fallback'i) + `forecastBreach` (limit aşımı VE >12saat aksiyon payı) — unit testli (120 test). **Costs+Reports REST (OpenAPI 63 path)**: `GET /costs?groupBy=kind\|agent\|project\|task&from&to` (defterden SUM + isim çözümü + NULL→'platform' kovası 26 §13; MV+tail kompoziti hacim büyüyünce — kayıtlı daralma), `GET /costs/entries` drill-down, `GET /costs/forecast` (aktif budget'lar × dönem penceresi → spent/projected/breach), `GET /reports` (executive_report artifact'ları). **MV refresh**: server'da 5dk interval'le `REFRESH MATERIALIZED VIEW cost_rollup_daily` (Temporal-cron yerine kayıtlı daralma). **UI**: **COSTS nav aktif** — dönem seçici (24h/7d/30d), boyut değiştirme (kind/agent/project/task) + bar'lar, **burn-rate projeksiyon paneli** (on track / forecast breach rozetleri), canlı defter tablosu; **REPORTS nav aktif** — rapor listesi + içerik görüntüleyici. **Kabul — executive-report int 3/3 (demo 23–24)**: açık task varken proje tamamlanmaz; son task DONE → proje `completed` + artifact (295¢ = 188+46+0+61 GERÇEK defter toplamı, 'llm: 2.49 USD (249¢)', top-spender isimle, 'CSV export must stream' learning'i alıntılı) + CEO→Founder DM mesajı ('total spend 2.95 USD' + artifact ref'i) + project.completed{reportArtifactId}; **tetik replay'i idempotent** (tek artifact/tek event). **e2e `12-costs-reports` yeşil**: seed'li 1300¢ defter → COSTS toplam + proje bar'ı + forecast 'projected' satırı; REPORTS'ta rapor içeriği gerçek sayılarla. Tam e2e 12/12 + repo 52 task + tam batarya yeşil. Not: budget editor CRUD UI'ı + budget.forecast_breach event emisyonu + cost_rollup_monthly T50/canlı-lane cilası (kayıtlı). |
| T50 Full MVP E2E + hardening gate | ✅ Tamamlandı | 2026-08-12 | **MVP kapanış kapısı (29 §3/§6/§8; 35 §8)**. **Master demo süiti**: 25 adım, sıralı senaryo dosyaları 02–14 olarak (`apps/web/e2e/README.md` — adım↔dosya/int-süit eşleme tablosu; mvp-demo.spec.ts yerine senaryo dosyaları — kayıtlı adlandırma; adım 1 = boot'un kendisi + CI health probe'ları). **R1 (worker kill) `13-r1-worker-kill.spec.ts`**: canlı cascade ortasında `docker kill agent-worker` → restart → **koşu kayıpsız tamamlanır** — kanıt Postgres SATIR-SAYISI DELTA'ları (kalıcı stack'lerde başlık-varlığı yalanı imkânsız); WIP-hazırlık adımı önceki senaryoların açık cascade'lerini Founder transition API'sinden iptal eder (TEAM_WIP_LIMIT'e karşı — motorun REDDİ doğru davranış, üçüncü eşzamanlı cascade'i bilinçli sınırlıyor). **R1 İKİ GERÇEK resumption bug'ı yakaladı ve düzeltildi**: (1) `update_task_status` aktivitesi at-least-once altında idempotent değildi — kill sonrası replay IN_PROGRESS→IN_PROGRESS'i 'illegal' diye sonsuz retry'a takıyordu; artık 'zaten hedef durumda' = başarı (`replayed:true`); (2) `create_task` replay'de/scripted-cursor-reset'te DUPLICATE child üretiyordu — artık **doğal idempotency anahtarı** `uuidv5(delegator+title, parentId)` (aynı delegator + aynı parent + aynı başlık = aynı amaçlanan child; hem activity replay'ini hem fake-LLM'in worker-restart'ta script'i baştan oynatmasını dedupe eder — canlı modeller çağrı başına stateless, kayıtlı gerekçe); TasksService.create opsiyonel `id` + var-olanı-döndür; delegate/assign zaten aynı-sahip no-op ✓. **R2 (iki şirket izolasyonu) `14-r2-isolation.spec.ts`**: API'den ikinci şirket + org + ajan; B'de GERÇEK öğrenme zinciri (FAILED→NATS→konsolidasyon→B-scoped bellek) koşar; **probe'lar**: B'nin satırları yalnız B company_id'siyle (tasks/projects/events SQL), A'nın memories API'si B'ninkileri LİSTELEMEZ, A scope'undan B task'i 404, B scope'u kendi satırlarını servis eder + **demo adım 25**: Approval Center'da sıfır rutin/teknik (tool_execution pending=0). **Out-of-scope guard'ları (29 §6)**: 1–3 zaten CI'da (eslint agent-framework banı, check-deps Redis karalisti, office animasyon lint'i); **4–5 bu görevde eklendi** — `apps/server/test/scope.test.ts`: OpenAPI path'lerinde karanlık Phase-2/3 yüzeyi yok (assets/experiments/content/campaigns/deployments/incidents/decisions) + web nav'da karanlık view yok + **S1 statik probe: docker.sock compose'da YALNIZ sandbox-manager bloğunda** (3/3 yeşil, her CI'da koşar). **Injection süiti**: T43'ün 55-vakalık corpus'u + gateway flagging int'leri gate'in parçası (yeşil). **Perf bütçeleri**: retrieval p95 177ms<250ms @100k (T45, RUN_PERF) — nightly lane'de. **Nightly wiring `.github/workflows/nightly.yml`**: cron 03:00 — perf-budgets job'ı (100k retrieval) + temiz-boot master e2e (scripted) + **live-LLM lane'i** (`vars.ENABLE_LIVE_LLM` + `secrets.ANTHROPIC_API_KEY` ile; secretsız repo'da atlanır). **Kabul — temiz compose boot'ta (down -v + data wipe) tam master süiti 14/14**: 02-org, 03-hire, 04-events, 05-office, 06-board, 07-comms, 08-cascade(+adım 25), 09-terminals, 10-projects, 11-memory (gerçek zincir), 12-costs-reports, **13-R1 (kill→resume→tamamlanma)**, 14-R2 (sızıntısız) — tek boot üzerinde art arda; repo 52 task + tam int bataryası yeşil. **29 §8 çıkış kriterleri durumu**: #1 ✓ (temiz boot'ta 25 adım+R1+R2), #2 ✓ (guard'lar CI'da), #4 ✓ (35 §8 M0–M5 kalemleri kod/test karşılıklarıyla kapalı — M3 'gerçek LLM' kalemi scripted+nightly-live lane ayrımıyla, canlı doğrulama nightly'de), **#3 OPERASYONEL**: 3 ardışık yeşil nightly live-LLM koşusu — pipeline kuruldu, sayaç provider secrets tanımlandıktan sonra işler (kayıtlı: bu kriter doğası gereği takvim ister, kod tarafı tamam). |

## Canlı koşu düzeltmeleri — 2026-08-15 (AGENT-BRIEF Faz A/B + hafıza)

Bu satırlar `CODE-REVIEW-2026-08-15.md` + `CODEREVIEWHAFIZA.md` kesişiminden doğdu ve **canlı şirketin kendi koşusunda** doğrulandı. Ortak ders: bu kusurların hiçbiri tip sistemine görünmüyordu; hepsi wiring ya da yalnız canlı modelde ortaya çıkan davranıştı.

| Madde | Durum | Tarih | Not |
|---|---|---|---|
| **A1** `model_providers.pricing` (26 §3.1) | ✅ | 2026-08-15 | Migration 0014 + `loadProviderPricing` (doküman şeklinden port şekline çeviri, boş `{}` → derleme-zamanı varsayılanlarına düşer) + router wiring + seed. B1'in son parçası: LLM maliyeti artık gerçekten akıyor. |
| **A2/A3** maliyet tx bütünlüğü + bütçe yarışı (26 §2, Y6/O7) | ✅ | 2026-08-15 | `recordCostInTx` — fatura kaydı ile maliyet girdisi tek transaction. `tightestBudget` yöneten satırı `FOR UPDATE` kilitliyor ve uçuştaki rezervasyonları düşüyor; eşzamanlı iki çağrıdan yalnız biri geçiyor. Kilit sırası tersliğinden doğan 40P01 deadlock'ı bulunup düzeltildi. |
| **A4** devre kesici uçtan uca kanıtı | ✅ | 2026-08-15 | `cost-breaker.int.test.ts` — gerçek Temporal + gerçek workflow + gerçek fiyatlama. Üç sabotajla kırmızı doğrulandı (guard devre dışı, maliyet yazımı devre dışı = B1 regresyonu, tripBreaker no-op). |
| **Kesicinin sinyal yarısı** (26 §5 madde 3) | ✅ | 2026-08-15 | A4 sırasında çıktı: kesici ajan satırlarını `paused` yapıyordu ama koşan workflow o satırı okumuyor, adım atmaya devam ediyordu. Yeni `breaker-consumer` T21'de provision edilmiş ama tüketicisi olmayan `cost-aggregator` durable'ını dinleyip canlı oturumlara `managerDirective(pause/resume)` gönderiyor; hedef workflow id'si `agent_sessions.workflow_id`'den okunuyor. |
| **B1'** `task.query` + `memory.search` dispatch (17 §4) | ✅ | 2026-08-15 | İkisi de kayıtlıydı ve grant'i vardı ama dispatch'te karşılığı yoktu — ajan çağırıp adım+LLM yakıyordu. `MemoryRetrievalService.searchScoped` eklendi: yalnız aktif satırlar, 12 §7 skor formülü, INV-15 scope izolasyonu servisin içinde. Embedding dispatch kenarında olmadığından semantik şerit atlanıyor (§7.5c) ve `memory_retrievals`'a `lane=tool_search` ile dürüstçe kaydediliyor. |
| **B5** gözlem kırpması | ✅ | 2026-08-15 | Alan-farkındalı bütçe (son adım 24k, öncekiler 12k) + pencere 5→8. Canlı kanıt: aynı ajan düzeltmeden sonra 40./42./47./49. adımlarda `fs.edit`'i başarıyla kullandı. |
| **İnceleme zinciri kilidi** (07 §5, INV-14) | ✅ | 2026-08-15 | `requestReview` incelemeciyi `task_assignments`'a da yazıyor. Yetki matrisi rolü oradan türetiyordu; incelemeci olarak seçilen bir manager kendi kararını uygulayamıyor ve **her inceleme kilitleniyordu**. Canlıda TASK-18/19/22'de görüldü; düzeltmeden sonra TASK-22 doğal yoldan `code:approved → qa:approved → DONE`. |
| **M1** hafıza tetikleyicileri (12 §5.0) | ✅ | 2026-08-15 | Tetikleyici tablosunun tamamı: N-anlamlı-olay sayacı (eşik `company_settings`'ten), `escalation.resolved`, `experiment.completed`. Hafıza "görev sonu toplu"dan "çalışırken sürekli"ye geçti. |
| **M2** doğal DONE → hafıza zinciri kanıtı | ✅ | 2026-08-15 | `task-done-memory-chain.int.test.ts` — `complete_task` → REVIEW → manager incelemeci → düzeltme turu → QA → `git.merge` → DONE → outbox → JetStream → gerçek konsolidasyon workflow'u → `memories` → `memory.created` → `/ws`. Bu dikişi hiçbir test görmemişti. |
| **Hafıza pencere özeti** (12 §5.1/§5.7) | ✅ | 2026-08-15 | **En sinsi kusur.** Özet satırları `- [zaman] tip: özet` biçimindeydi, olay kimliği yoktu; §5.1 "penceredeki somut kanıtı göster" diyor, §5.7 çözülemeyen referansı halüsinasyon sayıp adayı atıyor. Model gösterecek gerçek bir şey bulamayınca UUID uyduruyor, **her aday çöpe gidiyordu** — panel hep boştu. Scripted yol referansları `window.events[].id`'den aldığı için testler bunu göremiyordu. Satırlar artık `- id=<uuid> …`. Canlı kanıt: aynı görev, düzeltme öncesi `7 aday → 0 kalıcı`, sonrası **`8 aday → 8 kalıcı`**; şirket hafızası 5 → 13 satır. |
| **0015** telafi kayıtları (26 §1.1) | ✅ | 2026-08-15 | `cost_entries.amount_cents >= 0` kısıtı kaldırıldı: doküman düzeltmeleri **negatif tutarlı telafi kayıtları** olarak tarif ediyor, kısıt bu yolu imkânsız kılıyordu. |
| Migration journal + branch pattern regresyonu | ✅ | 2026-08-15 | `_journal.json`'daki çift `0013` girdisi taze veritabanında **tüm** entegrasyon testlerini kırıyordu. Ayrıca Y4 çapalama düzeltmesi önek desenlerini bozmuştu (`^task/` → `^(?:^task/)$` hiçbir şeyle eşleşmiyordu); başı çapalı desen artık önek kuralı, çapasız desen tam eşleşme. |

### Faz B–D — 2026-08-15 (ikinci tur)

| Madde | Durum | Tarih | Not |
|---|---|---|---|
| **B2'** `web.fetch` / `web.search` (17 §3.1, S5) | ✅ | 2026-08-15 | İstek sunucu sürecinden doğrudan çıkmıyor: egress allowlist proxy'sinden geçiyor. Grant'teki `domainAllowlist` ajan bazlı kural, squid ACL'i altyapı bazlı kural. Gövde `maxBytes` tavanında kesiliyor. İkisi de `provenance: "web"` dönüyor — S5 çiti buna bağlı. Arama anahtarı S2 gereği kimlik bilgisi seam'inden geliyor; anahtar yoksa araç uydurmuyor. Test gerçek bir CONNECT proxy'siyle koşuyor. |
| **B3'** `db.inspect` salt-okunurluğu (Y2) | ✅ | 2026-08-15 | Eski koruma tek önek regex'iydi: yazan CTE (`WITH x AS (INSERT … RETURNING)`) ve `EXPLAIN ANALYZE DELETE` geçiyordu — üstelik araç R0 + sideEffectFree, yani en düşük denetimle. Şema katmanı sıkıldı; **asıl garanti** dispatch'te: projenin kendi veritabanında `READ ONLY` transaction + `statement_timeout`. Platform veritabanına yöneltilmesi ayrıca engelli (tek sorguda her şirketin satırları). Katalog satırı koşullu — yalnız projenin veritabanı tanımı varsa. |
| **B4'** büyük dosya yazımı (Y3) | ✅ | 2026-08-15 | İçerik tek shell argümanı gidiyordu; MAX_ARG_STRLEN 128 KB + base64 şişmesi = ~96 KB gerçek tavan, şema ise 2 MB vaat ediyor. `fs.edit` tüm dosyayı geri yazdığı için 100 KB'lık dosyada üç satırlık değişiklik de patlıyordu. Baytlar artık stdin'den. Bu sırada iki hata daha: stdin yarı-kapanınca süreç bitmeden çıkış kodu okunuyordu (`exit -1`) ve yazma sonrası çıktı kayboluyordu (`created:false`). Kanıt: 300 KB testi, argv yoluna dönünce "argument list too long". |
| **C1** grant kısıtları denetimi (Y4) | ✅ | 2026-08-15 | Üç fail-OPEN atlatma testle çivilendi: `src` öneki `srcret/`'i yetkilendiremez (önek bir YOL sınırı), çapasız desen tam eşleşmedir (`main` ≠ `not-main-really`), bozuk URL fail-closed reddedilir. Ayrıca allowlist host bazlı — `registry.npmjs.org.evil.example` reddediliyor. |
| **C2** görev başına tek workspace (Y5) | ✅ | 2026-08-15 | Arama `(taskId, seviye)` ile anahtarlanırken volume adı yalnız görevden türüyordu: `fs.read` (analysis) ve `fs.write` (coding) aynı volume üzerinde İKİ workspace açıyordu; `analysis` `network:none` olduğu için oraya düşen `terminal.run`'da `npm install` sessizce ölüyordu. Artık tek workspace, gerektiğinde seviye yükseltiliyor (konteyner aynı volume üzerinde yeniden yaratılır, iş kaybolmaz), aşağı düşürme yok. |
| **C3** O8–O11 | ✅ | 2026-08-15 | O8 oturum çerezine `secure` (prod'da). O9 `AUTH_AUTOLOGIN` açıkken boot yüksek sesle uyarıyor — tek kullanıcılı mod bilinçli bir karar ama sessiz varsayılan olmamalı. O10 workspace konteynerleri root koşuyordu; yığının geri kalanı zaten uid 1000 varsayıyordu (`chown -R 1000:1000 /work`), eksik olan tek şey `User` alanıydı. O11 squid ACL'i sabit subnet yazıyordu, compose ise onu değiştirilebilir yapıyor — ayrıştığında proxy her isteği sessizce reddediyordu; config artık aynı ortam değişkeninden render ediliyor. **Ayrıca:** ACL yalnız workspace subnet'ine izin veriyordu, ama `web.fetch` sunucudan dispatch ediliyor — B2' bu düzeltme olmadan canlıda 403 alırdı. |
| **Faz D** O1, O3–O6 | ✅ | 2026-08-15 | O1 `fs.search` `glob`'u yok sayıyordu (ajan filtresinin düştüğünü göremiyordu) + açıklama "ripgrep" derken düz `grep` koşuyordu, yani `\d+` sessizce eşleşmiyordu. O3 iki yutulan hata gerçek arıza gizliyordu (mesaj teslim sinyali, ofis canlılık projeksiyonu). O4 checkpoint idempotency anahtarı `Date.now() >> 13` idi — retry ikinci kez commit ediyordu; artık `stepId`. O5 working set her adımda tüm ajanları yüklüyordu. O6 relay 500'lük partide 500 UPDATE atıyordu. |
| **M3** scripted modda sahte hafıza | ✅ | 2026-08-15 | Bilinmeyen fixture için `{title: "Consolidated: <key>", importance: 0.5}` uyduruluyordu; fixture yalnız demo görevlerinde olduğu için scripted modda koşan her gerçek görev çöp bir satır kaydediyor ve bu panelde gerçek bir anıdan ayırt edilemiyordu. Artık hiçbir şey üretmiyor — anlamlı hafıza canlı model ister. |
| Seed grant yolu | ✅ | 2026-08-15 | `ensureSeed` mevcut kurulumda erken dönüyor ve araç grant listesini hiç çağırmıyordu: yeni bağlanan her araç `NO_PERMISSION_GRANT` ile düşüyor, gateway hatası gibi görünüyordu. |

**Açık kalanlar:** O12–O15, M4 (episodik adım hafızası — INV-15/INV-11 gözetilerek değerlendirilecek), `web.search` için arama API anahtarı yapılandırması, `db.inspect` için proje veritabanı tanımı (`environments.config.databaseUrl`).

## AGENT-BRIEF rev 2 — 2026-08-15 (üçüncü tur)

Brifing yeniden numaralandı; aşağıdakiler o numaralandırmaya göre. A1–A3, B1–B2, C1–C5 ve E'nin beş kalemi önceki turlarda bitmişti (yukarıdaki tablolar).

| Madde | Durum | Tarih | Not |
|---|---|---|---|
| **A4** `dependencyResolved` köprüsü (07 §3, 09 §9) | ✅ | 2026-08-15 | Emit vardı, workflow handler'ı vardı, **gönderen yoktu** — repoda tek bir `signal("dependencyResolved", …)` çağrısı bile bulunmuyordu. DAG'ın "A bitince B başlasın" vaadi pratikte "B kendi zaman aşımı dolunca fark eder"e dönüşmüştü. Kendi cursor'ı olan `workflow-signals` durable'ı + köprü; workflow handler'ı artık tekrar teslimatı dedupe ediyor (çözülen bağımlılıklar bir KÜME). |
| **A5** konteyner roll-up (07 §2) | ✅ | 2026-08-15 | "Derived-but-persisted … child-completion triggers" tarif edilmiş, tetikleyici yazılmamıştı: `to === "DONE"` dalında çocuk kontrolü yoktu, CEO hedefi delege edip hemen kapatabiliyordu. Çocuklar terminal olunca ebeveyn türetiliyor; konteyneri elle kapatmak reddediliyor. Konteynerler iş görevi makinesini yürümüyor (kimse bir hedefi kod incelemesinden geçirmez) — bu yol yalnız goal/initiative için açık, bir `task` DONE'a asla review/QA olmadan ulaşamaz. |
| **A6** stuck-task-sweep (09 §9, 07 §8) | ✅ | 2026-08-15 | İş yalnız üç yoldan ilerliyordu; WAITING'e park edilen görevi geri alan mekanizma yoktu. Sweep süresi dolanı BLOCKED'a alıp yöneticiye eskalasyon üretiyor, sahibinin döngüsü ölmüşse yeniden başlatıyor. **Doküman içi çelişki:** §8 prosası "→ BLOCKED" diyor ama §5'in kanonik tablosunda o kenar yok (WAITING gönüllü, BLOCKED zorunlu); kanonik tablo bağlayıcı olduğu için hedefe iki meşru adımda gidiliyor. |
| **A7** TEK KANIT KOŞUSU | ✅ | 2026-08-15 | Tek senaryo, yedi faz, üç kanıt: hedef → decompose → iki alt görev (aralarında DAG kenarı) → gerçek inceleme/QA/merge → roll-up ile girişim+hedef → hafıza zinciri → /ws; ardından bütçe ihlali → devre kesici → restore; son olarak sweep. Sahte olan tek şey sandbox'ın HTTP sınırı ve Temporal'ın "start" semantiği. |
| **B3** prompt caching (26 §3) | ✅ | 2026-08-15 | Brifingin "sıra zaten doğru" premisi yanlıştı: katalog DEĞİŞKEN user mesajının sonundaydı ve sistem mesajı her adımda değişen markers'ı taşıyordu — cache'lenecek sabit önek hiç yoktu. Mesajlar artık konuya göre değil KARARLILIĞA göre bölünüyor; `LlmMessage.cacheable` → Anthropic `cacheControl` (1h). Asıl test önekin iki farklı adımda birebir aynı kaldığı. |
| **B4** intake sentez katmanı (14 §3.1/§3.2) | ✅ | 2026-08-15 | Beş bölüm sabit `_analysis unavailable_` idi: Founder JSON dökümü alıyor, okumasını almıyordu. Yorumlayıcı geçiş eklendi (analizör çıktısı modele DATA olarak veriliyor, P3); model yoksa/çıktı bozuksa deterministik metne düşülüyor (P6). **Repo'suz proje** artık rapor alıyor — önceden bu yolda hiç artefakt üretilmiyordu. |
| **D1** teslimat kaydı (14 §5) | ✅ *(MVP dilimi)* | 2026-08-15 | Tablo ve `project.deployment.*` olayları vardı, yazan yoktu. DeploymentsService + olaylar + onay kapısı. **Brifingin "dış git remote push + deploy hedefi" isteği uygulanmadı:** 14 §5 bunu açıkça sınırlıyor — "MVP scope: sandbox-kind environments only; external environments are Phase 3". Sınır kodda uygulandı, yorum olarak bırakılmadı. REST yüzeyi yok (29 §6.5 guard'ı `/deployments` yolunu yasaklıyor). |
| **D4** proje-bazlı egress (27 §12) | ✅ | 2026-08-15 | squid.conf'un kendi yorumu bunu öngörüyordu ("generated include"), üretici yazılmamıştı: ajanlar yalnız paket registry'lerine çıkabiliyordu. `projects.settings` (migration 0016) + include üreticisi + salt-okunur mount + doğrulayan reload. Asıl risk konfigürasyon enjeksiyonuydu (satır sonu içeren "alan adı" allowlist'i açabilirdi) — desen fail-closed. Canlıda kanıtlandı. |
| **E** kalanı | ✅ ◑ | 2026-08-15 | `guard_stopped` oturumu artık "completed" sayılmıyor (teslimat yoksa oturum başarılı değildir); `subjectFilters[1]` ölü kodu kaldırıldı; MVP_TOOLS yorumu düzeltildi. **Dispatch hatasında olay yayılması YAPILMADI:** 10 §10'un tablosunda `tool.invocation.failed` yok ve katalog testi kaydı dokümanla birebir eşleştiriyor. Gözlemlenebilirlik bugün `tool_invocations` satırı + ajan adımının `observation` alanından sağlanıyor; olayı zaman çizelgesine taşımak doküman kararı ister. |

### D2 / D3 — Founder onayıyla açıldı (2026-08-15)

Aşağıdaki gerekçe **kaydedilmiş tarihçedir**; Founder Faz 2 yüzeylerini açma kararını verdikten sonra üç kalem uygulandı:

| Madde | Durum | Not |
|---|---|---|
| `tool.invocation.failed` (10 §10.1) | ✅ | İzin verilmiş ama çalışırken patlayan çağrı artık zaman çizelgesine düşüyor. Olay önce **dokümana** eklendi (katalog testi kaydı dokümanla birebir eşleştiriyor), sonra koda; sayaç 190 → 191. |
| **D2** entegrasyon adapter'ları (ADR-017, 30 §) | ✅ *(dış hesapla kanıtlanmadı)* | `SocialChannelPort` (packages/domain, 30 §'in saydığı sekiz yetenek; `capabilitiesOf` beyan ile gerçeği ayrı tutmuyor) + Instagram adapter'ı (iki adımlı Graph API yayını, 5xx/429 geçici · 4xx kalıcı sınıflaması, metrik okuma). Token ajan girdisinden değil bağlantı kimliğinden sunucuda çözülüyor (S2); bağlantı yoksa platforma hiç gidilmiyor. |
| **D3** yayın kuyruğu + dispatcher (30 §) | ✅ | `publish_jobs` ve `marketing.content.publish.*` şemada/katalogda vardı, kodda tek referansı yoktu — pazarlama "içerik üret"ten öteye geçemiyordu. Zamanla → sahiplen → yayınla/başarısız ol; iki dispatcher aynı işi iki kez alamıyor; kalıcı hata deneme hakkını tüketmeden bitiyor; adapter'ı olmayan platform sessizce beklemiyor (30 §'in manuel-yayın düşüş yolu tetiklenebilsin diye). |

**Kendi iddiamın düzeltmesi:** çift yayın korumasının `SKIP LOCKED`'tan geldiğini yazmıştım. Kaldırıp ölçtüm: koruma tek ifadelik `UPDATE … WHERE status='scheduled'` yükleminden geliyor; `SKIP LOCKED` doğruluk için değil, dispatcher'ların birbirini beklememesi için. Kırmızı kanıtı doğru yerden alındı.

**Dış hesapla kanıtlanmadı:** gerçek bir Instagram hesabı olmadığından giden isteğin şekli ve akış kayıtlı fixture'lara karşı doğrulandı — 30 §'in kendi risk azaltması bunu öngörüyor (*"contract tests on recorded fixtures"*). Canlı hesap bağlanınca gereken tek şey `secrets` satırı + bağlantı kaydıdır.

**Açık kalan:** REST/UI yüzeyi hâlâ kapalı — `scope.test.ts` (29 §6.5) `/marketing`, `/campaigns`, `/content` yollarını engellemeye devam ediyor. Pazarlama panosu açılacaksa o guard'ın da bilinçli olarak gevşetilmesi gerekir; bu ayrı bir karardır.

### D2 / D3 — ilk değerlendirme (karar öncesi kayıt)

Brifingin **D2 (entegrasyon modülü, ADR-017)** ve **D3 (pazarlama aktüasyonu)** maddeleri, mimari dokümanların **Phase 2** olarak ayırdığı yüzeylerdir:

- `30-PHASE-2.md`: *"Per ADR-017, integrations are adapters in the `integrations` module … the first adapter is Instagram Graph API"* — entegrasyon ve sosyal yayın oradadır.
- `apps/server/test/scope.test.ts` (29 §6.5, T50) bunu **CI'da aktif olarak engelliyor**: `/campaigns`, `/marketing`, `/content`, `/deployments`, `/assets`, `/experiments` yollarından biri OpenAPI'ye girerse test kırılıyor. Guard'ın kendi ifadesi: *"schema-only subsystems … tables exist, no API surface"*.

Yani bu iki maddeyi yazmak, projenin kendi kapsam guard'ını ihlal etmek olurdu. Faz D'nin MVP'de dayanağı olan iki dilimi (D1'in sandbox teslimat kaydı, D4'ün egress include'u) uygulandı; D2/D3 bir **faz kararı** olarak Founder'a bırakıldı.

## 2026-08-18 — Founder gözlem + izin sürtünmesi düzeltmeleri

| Madde | Durum | Not |
|---|---|---|
| HEAD derlenmiyordu (a3d90bd üç kırık) | ✅ | `client/index.ts`'te tools namespace eklenirken approvals kuyruğu dosya sonunda sarkık kalmıştı (7 sözdizimi hatası); `tools.ts` zod v4'te tek argümanlı `z.record` kullanıyordu; `tools/routes.ts` hiç var olmayan `request.companyId`/`request.principal.agentId` dekorasyonlarına yaslanıyordu. Ayrıca web'de ApprovalsView `limit` (şemada yok) ve PermissionsPanel `Card description` (prop yok) kırıktı. Çalışan stack imajları bu commit'ten ESKİ olduğu için "Ayarlar sekmesi disabled" görünüyordu — kırık hiç canlıya çıkmamıştı. |
| Tool Management API şirket kapsamına taşındı | ✅ | `/api/v1/tools/permissions` → `/api/v1/companies/:companyId/tools/permissions` (+POST/DELETE). Auth, kod tabanının standardı: `requireUser` + membership → founder/admin. Grant'i veren her zaman insan → `grantedByUserId`. SDK + PermissionsPanel companyId geçiriyor; izin ekleme formu artık UUID yapıştırtmıyor — ajan/birim/pozisyon LİSTEDEN seçiliyor. OpenAPI snapshot yenilendi (`apps/server/scripts/gen-openapi.ts` eklendi — 21 §6 drift check'inin üretici yarısı artık betik). |
| Grant mutabakatı tüm şirketlere | ✅ | `seedToolGrants` yalnız seed şirketine koşuyordu; sihirbazla kurulan şirketin CEO'su ilk görevde `NO_PERMISSION_GRANT`'a çarpıp Founder'dan izin diliyordu. Boot artık HER şirket için idempotent grant listesi basıyor (bilinen birim slug'ı yoksa no-op). |
| `agent.step.recorded` emit (10 §10.1) | ✅ | Olay katalogda ve 10 §10 tablosunda vardı ("agent-worker → OP, WS"), göndereni yoktu. `persistStepActivity` tx'i içinde emit — replay yolu erken döndüğü için exactly-once (INV-11). |
| Komuta merkezi "oturum" hücreleri | ✅ | Founder isteği: CEO dahi olsa aldığı görevin düşünce/aksiyon akışı ana sayfadaki terminal ızgarasında izlenmeli. Yeni `GET /companies/:id/agent-sessions` (canlı + son 10 dk bitmiş oturumlar, ajan/görev etiketli) + `AgentSessionCell`: agent_steps read-model'i Claude-Code-vari kronolojik akış olarak render ediliyor (✻ düşünce · ▶ araç+gözlem · ⤷ delege · ⚠ eskalasyon…), WS invalidation + poll ile canlı, ⤢ modalında Founder direktif formu (mevcut DM yolu, N4). PTY hücreleri (kabuk) aynen duruyor — oturum hücresi döngüye bakar, kabuğa değil. |
| Izgara sürükle-bırak + oturum durumu satırı | ✅ | Founder isteği (ekran görüntüsü): hücreler başlığından sürüklenip ızgarada istenen konuma bırakılıyor (HTML5 DnD, sıra `acos-terminal-grid` store'unda kalıcı; oturum + PTY hücreleri tek sıralı listede). Oturum hücresinin altına ekran görüntüsü dilinde durum satırı eklendi: `✳ Çalışıyor — 7dk 22sn · 128.4k jeton` (canlıda saniyelik sayaç, bitmişte ✔/✕). |
| Ofis: oturum aktivitesi koreografisi + panel duyarlılığı | ✅ | Founder isteği (AGENTDESK referansı): (1) Projektöre `agent.step.recorded` satırı — düşünme→THINKING, araç→WORKING, mesaj→COMMUNICATING, inceleme→REVIEWING rozetleri; `complete_task` adımında ajan YÖNETİCİSİNE yürüyüp raporluyor, dwell sonrası masasına dönüyor (mevcut walkAndInteract/return_home yolu; N2 korunur — yürüyüş gerçek olaydan). 3 yeni golden test. (2) Pixi resizeTo yalnız window resize dinliyordu; dockview paneli pencere değişmeden büyüyünce ofis küçük kalıyordu — ResizeObserver → app.resize(). (3) AGENTDESK dili: +/−/⤢ zoom kontrolleri (kamera komutları, sahte animasyon değil) ve başlıkta "aktif/toplam" sayacı. |
| Ofis sanat turu (AGENTDESK yoğunluğu) | ✅ | Zeminler aydınlık DAMALI karo (koridor açık gri; oda zeminleri departman vurgusu %25 + açık nötr; toplantı odası ahşap tahta); duvar yüzüne süpürgelik. Yeni dekorlar: kitaplık, su sebili, dosya dolabı, beyaz tahta, lobi halısı + kanepe — oda başına deterministik yerleşim. Sahipli her masada koyu isim plakası (BÜYÜK harf ilk ad, oda vurgusu çerçeve); avatar üstü etiket 1.25x zoom'a çekildi, aydınlık zemin için koyu kontur. Canlı ekran görüntüsüyle doğrulandı. |
| Ofis: sabit contain-fit + boşluksuz kompakt plan | ✅ | Founder kararı: pan/zoom tamamen kaldırıldı (tekerlek/sürükleme/butonlar yok) — ofis panele her zaman contain-fit sığar, panel boyutu değişince ResizeObserver→fitCamera ile yeniden ölçeklenir. Auto-layout: ROOM_GAP 0 (odalar duvar paylaşır), salonlar yüksekliğe göre raf-paketlenir, satır genişliği toplam alandan yatay hedef oranla türer, toplantı odası son satırın artığına oturur. Canlı ekran görüntüleriyle üç iterasyonda doğrulandı. |
| Ajan döngüsü: sessiz park sorunu (Founder: "40 dk'dır iş yapılmıyor") | ✅ | Kök neden `wait_for`: model şema varsayılanıyla 120 dk'ya kadar uyuyabiliyor, bekleme adımı ancak UYANINCA persist edildiği için ekranda iz bırakmıyor ve yalnız kendi sinyal türünü dinliyordu. Düzeltmeler: (1) park süresi 15 dk ile sınırlı — zaman aşımında model `[signal:wait_timeout]` marker'ıyla yeniden karar verir; (2) bekleme adımı uyumadan ÖNCE persist edilir — hücrede anında "⏸ bekliyor: X"; (3) HERHANGİ bir gelen mesaj (Founder direktifi) her bekleyişi uyandırır; (4) oturum `currentActivity` WAITING↔WORKING dürüst; (5) sunucu starter'ı REJECT_DUPLICATE→ALLOW_DUPLICATE (kapalı koşunun yeniden başlatılması — A6 sweep'in restart vaadi ancak böyle çalışır; koşan aynı id her politikada reddedilir). UI: durum satırı bilinen bekleyişleri adıyla söyler (⏸ wait_for · 🔔 onay · ⤷ alt görev). Takılı iki workflow terminate edilip taze oturumla yeniden başlatıldı. |
| GitHub entegrasyonu + direktife proje (Founder kararı, Faz-2 açılışı) | ✅ | (1) Direktif şemasına `projectId` — hedef projeye bağlanır; `createChildTask` aksiyonda proje yoksa EBEVEYNden kalıtır ("no project" eskalasyon döngüsünün kökü). Direktif diyaloğuna proje seçici. (2) GithubService: PAT secrets seam'inde mühürlü (S2), GET /user ile doğrulanır; Ayarlar→GitHub paneli (token asla geri okunmaz). (3) Yayın: sandbox-manager `/internal/v1/repos/publish` iç bare repo'yu remote'a iter (kimlik http.extraheader — token URL'e gömülmez, hata çıktısına sızmaz; --force: iç repo tek gerçek kaynak, ADR-010 korunur). Lead merge başarısında dispatch fire-and-forget yayın hook'u çağırır: remote yoksa Founder hesabında private repo AÇILIR (422→mevcut repoya bağlan, idempotent), `repositories.github_remote` işlenir, tüm branch'ler itilir. Elle yayın: POST /projects/:id/github/publish. Merge conflict çözümü zaten yapılandırılmıştı (conflictFiles ajana döner, workspace'te çözüp yeniden merge eder — 15 §3.7). |
| Ofis: delegasyon koreografisi + reset titremesi | ✅ | (1) `agent.task.assigned` projektörde: görevi veren yönetici (byAgentId) atadığı ajanın masasına yürür, teslim balonu (dm) gösterir, dwell sonrası masasına döner — Founder isteği "CEO görevi verdiyse Mert'in masasına gitmeli". 2 golden test. (2) "Arada bir tekrar render" kökü iki katmandı: WS her reconnect'te snapshot yollar ve applySnapshot sahneyi sıfırlayıp herkesi ışınlıyordu → aynı epoch+layout'ta YUMUŞAK birleştirme (pozisyon/yürüyüş korunur; sert sıfırlama yalnız epoch değişince — 23 §6 niyeti korunur, kayıtlı sapma); React Query odak yenilemesi layout'u yeniden çekip layoutVersion zıplatıyor ve tüm zemini yeniden boyatıyordu → birebir aynı layout sessizce yutulur. |
| Workspace: npx/npm "Permission denied" (exit 126) | ✅ | Canlı bulgu (Emre Şahin, TASK-43, 10 başarısız deneme → Founder onayına eskalasyon): S8 sertleştirmesi altında rootfs salt-okunur (→ ~/.npm yazılamaz) ve /tmp noexec (→ npx'in indirdiği create-next-app çalıştırılamaz) — npx'in indir-ve-çalıştır modeli iki çitin arasında ölüyordu. Çözüm: /home/node exec'li tmpfs (uid=1000; tmpfs varsayılanı root'un) + HOME/npm_config_cache/COREPACK_HOME oraya sabit; imajın .gitconfig'i tmpfs altında kaybolduğu için safe.directory GIT_CONFIG_* env'iyle verilir. S8 gevşemez: /work zaten yazılabilir+exec'lidir (aynı tehdit sınıfı), /tmp'nin noexec çiti yerinde. Gerçek konteynerde kanıtlandı: `npx create-next-app@latest --version` → 16.3.1. Canlı workspace'ler yeni konfigle yeniden yaratıldı (worktree volume'ları korunur — iş kaybolmaz). |
| Kaynak çakışması + onay hijyeni (Founder kararları) | ✅ | (1) **Ajan başına tek canlı oturum**: starter (server + worker delege yolu) meşgul ajana ikinci workflow başlatmaz — görev ASSIGNED kuyruğunda bekler; `agent.session.ended` drain'i (main.ts NATS aboneliği) sıradaki en eski ASSIGNED görevi başlatır; 30 dk sweep yedek ağ. TASK-43/44 self-deadlock sınıfı ve "aynı ajanda 2 terminal" bitti. (2) **Otonomi**: canlı kadro L1/L2'deydi — her R1/R2 aracı onay istiyor ve Founder'a taşıyordu. Aktif kadro ≥L3 (lider/yönetici ≥L4), UI işe alım varsayılanı 2→3; fs.write/terminal.run artık kendi takım kapsamında onaysız, R3 Founder kapısı duruyor. (3) **Lider-önce zincir**: request_help(manager) boştaki yöneticiyi P1 müdahale göreviyle uyandırır (DM'ler yalnız koşan oturumlara sinyal taşıyordu — boş yönetici talebi hiç görmüyordu); istek sahibi wait_for(reply) ile bekler (mesaj artık uyandırıyor). Prompt: escalate SON ÇARE — önce request_help(manager); Founder yalnız zincir sonu. |
| Çok sağlayıcılı LLM zinciri (Founder kararı) | ✅ | Anthropic kredisi bitince şirket tek kalemde durmuştu (kredi hatası HTTP 400 → bad_request → fallback yok; düzeltildi: 402/credit/quota → rate_limited sınıfı). Zincir: Anthropic(0) → OpenAI(5: reasoning gpt-5-mini, coding gpt-5.1, fast gpt-5-nano) → Ollama(10: mistral, çekildi). OPENAI_API_KEY .env'de; embedding de aynı anahtarla. Canlı doğrulama: Claude kredi hatası → gpt-5-mini devraldı; OpenAI TPM limitine takılan çağlar mistral'a taşıyor — şirket hiçbir sağlayıcıda tek başına ölmüyor. Not: OpenAI'de ücretsiz model yok; en ekonomik karışım seçildi. Disk krizi (mistral pull C:'yi doldurdu) + Docker daemon kilidi (14 Ağustos'tan zombi com.docker.backend süreçleri) çözüldü. |
| Gemini katmanı (ücretsiz bulut; Founder anahtarı) | ✅ | Google'ın OpenAI-uyumlu endpoint'i üzerinden `createGeminiAdapter` (kayıtlı sapma: model_providers.kind CHECK'i beş türle sabit olduğundan satır kind='openai' + name='gemini'; adaptör ada göre seçilir — Gemini zaten OpenAI protokolü konuşur). Zincir artık: Anthropic(0) → Gemini(3: gemini-3.7-flash / fast lite) → OpenAI(5) → Ollama(10, llama3.2:3b'ye küçültüldü — mistral CPU'da 200sn/çağrıydı). Ücretsiz katman doğrulandı; iki kredisiz sağlayıcı bypass ediliyor. Sıradaki: Claude Code CLI köprüsü (abonelik kotası katmanı). |
| Claude Code CLI köprüsü — CANLI (Founder yol haritası 1/4) | ✅ | scripts/claude-cli-bridge.mjs: host'ta OpenAI-uyumlu yüzey → `claude -p --max-turns 1` (VS Code eklentisindeki claude.exe, ABONELİK kimliği). İki canlı bulgu düzeltildi: (1) saf OpenAI adaptörü /responses API'sine gidiyor — köprü openai-COMPATIBLE adaptörle bağlanır; (2) köprünün 404'ü bad_request sayılıp zinciri kilitliyordu — bilinmeyen yol artık 503 (yedeğe düşülebilir). Doğrulandı: claude-cli-sonnet 11 OK, 4 dk'da 11 adım. Zincir: Anthropic API(0) → Claude CLI(1, abonelik) → Gemini(3, ücretsiz; embedding dahil) → OpenAI(5) → Ollama(10). Köprü host'ta koşmalı: scripts/claude-bridge-baslat.bat. Sıradaki: Codex CLI → Cursor → Gemini CLI (aynı köprü deseni). |
| Egress TAM AÇIK (Founder kararı, kayıtlı sapma) | ✅ | S8/27 §12 alan adı allowlist'i Founder talimatıyla kaldırıldı — uyarı verildi (prompt injection ile sızdırma riski), karar yinelendi. Workspace + servisler artık HER hedefe çıkar; proxy mimaride kaldı (tek kapı + access log), geri dönüş tek satır (allow satırlarına `allowed_dst` geri eklenir). Ayrıca aynı gün: bayat squid.pid sert kapanışta proxy'yi kalıcı öldürüyordu — entrypoint temizliği eklendi (workspace ağı 6 saat çıkışsız kalmıştı; TASK-43 EAI_AGAIN blokajının kökü). |
| REVISION paketi (AGENT-COMPANY-OS-REVISION.md, 7 task) | ✅ | 2026-08-19. (1) Scheduler: kuyruk priority+dependency-aware (`pickNextQueuedTaskId`), delege hedefi deterministic skor (skill/workload/familiarity/success/memory-affinity) — LLM yalnız işi tanımlar. (2) Interactive Shell: uzun ömürlü çift yönlü PTY + terminal.write/resize + agent_control↔human_control takeover (web'de "Kontrolü al/ver"). (3) Preview Gateway: /proc/net/tcp port keşfi + server→sandbox→container reverse proxy + çerezli 404 fallback ("Open Preview"). (4) CodeIndex: migration 0019 (code_files/symbols/edges), TS compiler API AST parser, project.imported→tam indeks, merge diff→incremental. (5) Memory↔CodeIndex: canonical entities.files/symbols/commits/tests + retrieval'da kod grafiği genişletmeli lane + [CodeIndex] sembol haritası. (6) Approval fail-closed: intake consultation zaman aşımı/hatası otomatik onay üretmez, GOAL PLANNED bekler. (7) Production auth: NODE_ENV=production+AUTH_AUTOLOGIN=true boot reddi; tek kullanıcılı kurulum AUTH_AUTOLOGIN_ALLOW_PRODUCTION=true açık opt-in (kayıtlı sapma). Özet kökte NEW-REVISED.md. |
| PROJECT-LIFECYCLE-CONTEXT paketi (20 task) | ✅ | 2026-08-19. İki yollu proje girişi + GitHubConnection; proje yaşam döngüsü state machine (migration 0020) + senkron indeks kapısı (READY⇔commit SHA); LanguageIndexer kayıt sistemi (TS/Python/Generic, implements/extends — 0022) + canonical/task-overlay indeks (0023); greenfield bootstrap + github.repo.ensure; Requirement Analyzer + deterministik staffing gap + tek toplu hire onayı + Agent Factory; model binding amaçları (0024) + registry tabanlı seçim UI; Scheduler capability filtresi (CEO ajan seçmez, INVARIANT 10); Context Compiler (code.search, anti-rescan kuralları, okuma önbelleği, stale işareti); preview.ports/http.request + workspace.port.opened; Ready ekranı (Project Understanding + hedef) ve proje Durum paneli. Özet NEW-REVISED.md'de. |
| Runtime: yaşayan oturum dayanıklılığı + yaşayan ajan terminali (Munder davranışı) | ✅ | 2026-08-19 (runtime-eng). Canlı kanıt: callModelActivity 120s genel tavanda + 08 §12'nin şart koştuğu heartbeat hiç yazılmamış — claude-cli-bridge (180s bütçe, eşzamanlılık 2, SINIRSIZ kuyruk) yük altında 137s+ sürünce 3 retry tükenip agentTaskWorkflow FAİL oluyordu (aynı görevde 5 ölü oturum); iptal edilen deneme köprüde koşmaya devam edip kuyruğu işgal ediyordu (retry fırtınası). Düzeltme: (1) llmActivity proxy'si 8 dk + heartbeatTimeout 60s + aktivitede 10 sn Temporal heartbeat (memory extractCandidates aynı desen, 10 dk — canlıda 2 dk tavanda ölüyordu); (2) köprüde MAX_QUEUE=4, doluysa anında 503 bridge_saturated → router yedeğe düşer; (3) YAŞAYAN ajan terminali: 23 saatlik canlı koşuda terminal_sessions=0'dı ve model komut başına aç-kapaydı — terminal.run artık oturum bağlamında ajan+workspace başına TEK aktif oturumu yeniden kullanır (ensureAgentTerminal, "agent-live", komut banner'lı), kareler tek `term.<id>` akışında birikir, kapanış workspace transition'ında aynı tx'te (INV-11) + mevcut açılış süpürmesi; (4) parseMemoryCandidates çift-sarılı dizi/tek nesne toleransı (canlı zod düşüşü). İki düzlem ayrımı (term.* ham / rt.* yapısal) zaten doğruydu, korunuyor. Munder'ın birebir CLI-TUI yolu (araçlar CLI'da) INV-3 gereği reddedildi — kayıt: yaşayan oturum ACOS'ta workflow+terminal ikilisidir. Testler: tool-dispatch.int (yaşayan oturum yeniden kullanımı), workspaces.int (teardown kapanışı), memory-candidate.test (5 şekil). |
| P1-1: proje READY'de durur + sonraki Founder hedefi kabul edilir | ✅ | 2026-08-19 (runtime-eng, T7). Gözlenen: create-time objective planlamayı otomatik başlatıyordu — proje READY'den geçip planning/executing'e kilitleniyor, dokümanın "READY → Founder hedefi → PLANNING" adımı erişilmez kalıyor (golden path stage 04 yalnız fail-closed maskesiyle geçiyordu) ve ikinci hedef 409 alıyordu (stage 14 BREAK); kapı açılsa bile routeIntake proje başına TEK goal tutuyor, ikinci hedef sessizce birinci görevi döndürüyordu. Düzeltme: (1) projectIntakeWorkflow kaynak ne olursa olsun READY'de durur — greenfield kaskadı kalktı, planlama TEK kapıdan (POST /projects/:id/goal → projectGoalWorkflow) girer; doküman PROJECT-LIFECYCLE TASK 2 greenfield için "otomatik ilerleyebilir" DER (izin, zorunluluk değil) — Founder kararı bu izni daraltıyor, kayıtlı sapma. (2) Hedef kapısı yaşam döngüsüne uyar: indekslenmemiş (draft/repository_setup/indexing) + kapanmış (completed/archived/cancelled/failed) 409; ready/planning/staffing_review/waiting_for_founder/executing/paused kabul — READY→PLANNING yalnız ilk girişte, koşan proje durumunu geri sarmaz (state machine'e yeni kenar EKLENMEDİ). (3) routeIntake idempotanlık anahtarı (proje) değil (proje, hedef metni): aynı metinle replay aynı görevi (INV-15), yeni hedef YENİ goal ağacı. Kanıt: golden path 11/14 → 12/14 — stage 03 artık status=ready (waiting_for_founder maskesi yok), 04 PASS (gerçek yoldan), 14 PASS (2. hedef dispatch edildi; DB: golden-path-app executing, goals=2, tasks=6). Kalan BREAK'ler başka sahiplerde (02 CEO-bootstrap, 10 review satırı). Test: project-goal-lifecycle.int.test.ts 5/5. |
| P0-2 kalanı: inceleme kaydı + kapanış zinciri (T8) | ✅ | 2026-08-19 (runtime-eng). Gözlenen: REVIEW geçişi çalışıyor ama reviews tablosunda 0 satır, reviewer yok, 3 görev REVIEW'da asılı. Dört kırık: (1) requestReview inceleme satırını yalnız canlı task workspace'i varsa açabiliyordu → workspace yoksa depo GÖREVİN PROJESİNDEN gelir (workspace_id NULL, branch = varsayılan dal; şema/ad/INV değişmedi, depo hiç yoksa açık hata); (2) complete_task/request_review konteyner (goal/initiative) görevleri de REVIEW'a taşıyordu — 07 §2 'nobody reviews a goal': konteyner artık REVIEW'a taşınmaz, sonucu kaydedilir, kapanış A5 roll-up'tan gelir; (3) mergeIfEligible dalsız görevde git.merge'i yine de çağırıp QA'da bırakıyordu → dal yoksa merge'e girilmeden SYSTEM kapanışı (EMPTY_MERGE semantiği bir adım öne); (4) QA turu rolleri ['qa','reviewer'] idi, Agent Factory LEAD işe aldığı için 'no eligible qa reviewer' fırlıyor ve submitReviewVerdictActivity'yi öldürüyordu → roller lead/manager ile genişledi (15 §2 org tablosunda QA Lead zaten lead) ve QA turu açılamazsa aktivite artık patlamıyor. Fixture (Jim şeridi, tek dal): dev.toolless-task.yaml'a rework dalı eklendi (incelemeci ilk turda daima changes_requested verir; script exhausted ile ölüyordu). KANIT (canlı gate): kalite zinciri İLK KEZ uçtan uca kapandı — epic: code:approved + qa:approved → DONE, initiative → DONE (roll-up), goal → DONE (roll-up), iki hedef döngüsünde de. Stage 10 hâlâ BREAK ama sebebi değişti: runner reviews>0'ı KONTEYNER göreve (goal) sorguluyor, 07 §2 gereği orada hiç review olmaz → harness hedefleme sorunu (Jim). Testler: review-without-workspace.int 3/3, review-close-chain.int 2/2. |
| T29 — Yönetici kendine iş alabilir (Founder kararı) | ✅ | 2026-08-20 (controlplane-eng). Founder'ın sözü: "sana görev verdim, sen böldün ama burayı ben halledeceğim dedin — aynısı ACOS'ta da olmalı." Mekanizma YARIM vardı: `mayDelegate` kendine atamayı zaten onaylıyordu (delegation.ts, `managerAgentId === toAgentId`) ama `scoreDelegateCandidates` yalnız AKTİF DOĞRUDAN RAPORLARI skorluyordu → yönetici aday kümesinde yoktu, `resolveDelegateTarget` kendisini asla seçemiyordu; prompt da somut ajan seçmeyi yasaklıyordu. Dört parça (ADR-022, _DECISIONS.md): (1) aday havuzuna yönetici KENDİSİ eklendi — aynı formül, aynı yetenek filtresi, ayrıcalık yok (koşan ana görevi zaten yük sayıldığı için her sıralamaya 1 puan geride başlar); (2) açık niyet için `SELF_SENTINEL_UUID` (delegate_task.toAgentId) + prompt'ta meşru seçenek olarak adı geçiyor — TASK 12 yetenek-override'ı açık self niyetine UYGULANMAZ (o kural ilgisiz ajana dayatmayı engeller; kendi alt görevinin sorumluluğunu almak dayatma değildir); (3) tavan: iş yöneticinin KENDİ WIP limitine sayılır (capacityCheck değişmedi) — doluysa WIP_LIMIT + alternatifler döner, model delege etmeye yönelir; (4) doğrudan raporu OLMAYAN yönetici kendi adayı DEĞİLDİR — boş havuz hâlâ "önce ekibi kur" (agent.hire) demektir, E2 kadro yolu bozulmaz. Ek: `resolveDelegateTarget` artık kapasitesi dolu adayı ATLAR (sık meşgul bir adayı havuza eklemek, rutin "bu kişi meşgul" durumunu başarısız delegasyona çevirirdi). INV-10 korunur: atamayı yine Scheduler yapar; değişen, sıraladığı kümenin üyeliği. Testler: manager-self-assignment.int (db 5, worker 3); regresyon delegation-cascade 2/2, delegation-budgets 8/8, agent-task-loop+loop-status 10/10, container-rollup+stuck-tasks 12/12. |
| T36 — Planlama/intake yolunun LLM harcaması deftere düşer | ✅ | 2026-08-20 (controlplane-eng). `llm_calls` tablosuna yalnız ajan döngüsünün `callModelActivity`'si yazıyordu; intake raporu sentezi, gereksinim analizi, sihirbazın kadro önerisi ve hafıza çıkarımı hiç satır bırakmıyordu — router'ın `logCall` kancası bu çağrıları yalnız stdout'a basıyor (DB'ye değil), yani Founder'ın maliyet ekranında "sıfır" ile "ölçülmemiş" aynı görünüyordu. Ajan turu bir CLI oturumuna dönüşünce (E4) bu boşluk kozmetik olmaktan çıktı. Yazma yolu tek yerde toplandı: `@acos/db recordLlmCall` (deterministik id ile idempotent select-then-insert) — ajan döngüsünün kendi INSERT'ü de aynı fonksiyona taşındı, yani ikinci bir kopya kalmadı. Beş çağrı sahası deftere bağlandı: intake sentezi (kimlik projeye sabit), gereksinim analizi (kimlik HEDEF METNİNİ içerir — ikinci hedef ayrı satırdır), kadro önerisi (kimlik iş akışına bağlı), hafıza çıkarımı + onarım turu (kimlik pencerenin son olayına bağlı). **Kayıtlı sınır:** `llm_calls.purpose` kanonik bir CHECK'tir (reasoning|coding|fast|embedding|vision) ve YÖNLENDİRME amacını taşır; "hangi planlama adımı" bilgisini taşımaz, o yüzden yeni bir purpose UYDURULMADI. Planlama satırlarının ajan turundan ayırt edicisi `agent_session_id`'nin NULL olmasıdır. Çift sayım yok: router'ın logCall'ı DB'ye yazmıyor. Test: planning-llm-ledger.int 4 (satır gerçek sağlayıcı/maliyetle yazılır; aktivite yeniden denenince maliyet ikiye katlanmaz; ikinci hedef ayrı satır; sihirbaz kendi satırını yazar). |
| T11 — Süpürgenin iki kör noktası + iki sessiz yutma | ✅ | 2026-08-21 (controlplane-eng). Dört savunma-derinliği boşluğu, hepsi "sistem sessizce duruyor" sınıfı. **(a) ÇOCUKSUZ KONTEYNER:** 07 §2'ye göre hedef/girişim kendi işini yapmaz, çocuklarından türeyerek kapanır — sahibi hiç bölmeden turunu kapatırsa kapanışı türetecek çocuk yoktur; `rollUpContainer` 0 çocukta erken dönüyor ve stuck-sweep IN_PROGRESS'i hiç taramıyordu, yani konteyner SONSUZA KADAR IN_PROGRESS'te kalıyordu. Yeni sweep kuralı: konteyner + 0 çocuk + 30 dk → bulgu + BİR KEZ eskalasyon (`guardFlag: container_childless` işareti tekrarı engeller). Sweep görevi TAŞIMAZ: konteynerin durumunu roll-up yazar (INV-13) ve "bu hedef nasıl bölünmeli" bir insan kararıdır; `needsWorkflowRestart:false`, çünkü eksik olan bir tur değil bir karar. **(b) QA YETİMİ:** aynı yetimlik bir sonraki aşamada — sweep yalnız `status='REVIEW'`e bakıyordu, QA turu açılamayan görev sessizce QA'da asılı kalıyordu. Tarama artık REVIEW+QA; yeniden açılan tur da doğru türde (`kind:'qa'`), çünkü QA'daki bir göreve 'code' turu açmak incelemeyi bir adım geri sarardı. **(c) seed.ts boot yarışı:** Anthropic bloğunun içindeki `if (!providerId) return;` TÜM fonksiyonu iptal ediyordu — yarışı kaybeden boot, altındaki Claude CLI/Gemini/OpenAI/Ollama zincirini hiç tohumlamıyordu (9980503'ün düzelttiği hatanın aynı sınıfı: iç bloktan dış akışı sonlandırmak). Artık yalnız o blok atlanıyor. **(d) sessiz yutma:** `startAgentSessionActivity`'nin ASSIGNED→IN_PROGRESS geçişi `.catch(()=>{})` ile yutuluyordu; gerçek bir DB/izin hatası da aynı sessizliğe düşüyor ve üç park yolu `status==='IN_PROGRESS'` koşuluna bağlı olduğu için hepsi no-op oluyordu — T12'nin semptomu İZ BIRAKMADAN geri geliyordu. Yutma sürüyor (tur devam etmeli) ama artık görünür: yapılandırılmış konsol uyarısı + canlı konsola rt olayı. Testler: stuck-tasks.int 8 → 11 (QA yetimi turu QA olarak açar; çocuksuz konteyner bir kez eskale edilir ve durumu değişmez; çocuğu olan konteyner hiç girmez). |
| T22 — apps/server'daki 8 bayat entegrasyon testi | ✅ | 2026-08-21 (controlplane-eng). Her biri suçlu-sayılıp-aklandı: testin mi yoksa DAVRANIŞIN mı yanlış olduğu tek tek kanıtlandı; hiçbir test "hatayı beklemeyi öğrenerek" düzeltilmedi. **Bayat beklenti (4):** (1) companies.int `outputLanguage` — varsayılan migration 0017 ile 'tr' oldu (Founder kararı 2026-08-16), test 'en' bekliyordu; (2) companies.int olay sırası — şirket kurulurken varsayılan günlük bütçe artık CostService üzerinden kuruluyor ve `budget.created` zaman çizelgesine düşüyor, yani `company.settings.updated` seq 3 değil 4; (3) events-timeline üretici matrisi — elle tutulan liste aynı `budget.created`'i içermiyordu; (4) review-flow QA incelemecisi — 2026-08-19 canlı bulgusundan sonra QA'ya uygun roller genişletildi (qa|reviewer|lead|manager), seçim "önce aynı takım" olduğu için QA turunu görevin takımındaki lead alıyor; test dar rol kümesi dönemini bekliyordu (INV-14 iddiası korundu ve AÇIKÇA eklendi). **Bayat test altyapısı (3):** tool-gateway'in sahte dispatch portu `terminal.run` çıktısına sonradan eklenen `timedOut` alanını üretmiyordu → çıktı şeması düşüyor ve A2/A3 ölçmek istedikleri deftere/bütçeye hiç varamıyordu; web-tools'un sahte arama sunucusu eski sağlayıcının `{web:{results}}` şeklini konuşuyordu, kod ise AbacusAI kök dizisini ayrıştırıyor (`payload.slice is not a function`). **Fikstür artık geçersiz (1):** cost-breaker A4 adımlarını `#1/#2/#3` diye SAYIYLA ayırıyordu; `normalizedActionHash` her sayıyı `<n>`e indirdiği için (satır numarası gibi anlamsız farklar döngüyü gizlemesin diye — doğru davranış) üç düşünce aynı hash'e düşüyor ve guard (d) bütçe guard'ından ÖNCE tetikleniyordu; canlı kanıt guards:["loop"], 4 adım, 160¢. Ayrım sözcüğe çevrildi, test yine bütçe kesicisini ölçüyor. Sonuç: apps/server entegrasyon 8 kırık → 0. |
| T30 (1/2) — MCP tool-gateway + şirket oturum tavanı (E4/Decision A) | ✅ | 2026-08-20 (controlplane-eng). Ajan turu konteynerde koşan bir `claude` süreci olabildiğinde ACOS eylemlerinin ona AÇILMASI gerek — ama kontrol düzlemi BAYPAS EDİLMEDEN. MCP sunucusu bu yüzden Tool Gateway'in ÜSTÜNE oturur: her araç çağrısı ToolGateway.invoke()'a girer, yani aynı grant/risk/onay/oran-sınırı/denetim satırı. **Kimlik hiçbir aracın argümanında yolculuk etmez** — POST /mcp/v1 kimliği oturum jetonundan türetir (mcp_sessions, migration 0028): 32 rastgele bayt, tabloda SHA-256 özeti, (şirket, ajan, görev, oturum) dörtlüsüne mühürlü, sahibi oturum kapanınca iptal. Jetonun `"<companyId>.<secret>"` biçimi bir yetki iddiası değil, S4 kiracı korumasının SELECT'te şart koştuğu arama ipucudur. INTERNAL_API_TOKEN konteynere GİRMEZ (şirket çapında ana anahtar; kutudan bir kaçış bütün kontrol düzlemini teslim ederdi) — jetonu host tarafındaki broker POST /internal/v1/mcp/sessions ile basar. Araç adları MCP kuralına düzleştirilir (fs.read→fs_read); noktalı ad tool_invocations/grant/policy'de kanonik kalır. İlan edilen küme oturuma göredir (CEO yetenekleri sıradan ajana gösterilmez, db_inspect yalnız projede veritabanı varsa). Zarf tekdüzedir ve `awaiting_approval` bilerek isError DEĞİLDİR — hata olsaydı model aynı çağrıyı döngüye sokardı. **Şirket oturum tavanı**: MAX_LIVE_SESSIONS_PER_COMPANY (varsayılan 3 = bugünkü fiili paralellik, yani CLI yolunu açmak makineyi bugünkünden kötü duruma düşüremez); kapı iki başlatma yolunda da TEK koddan gelir (@acos/db checkSessionGate) ve rework yeniden girişi de dahildir — tavanın dışında kalan bir yol tavanı tavan olmaktan çıkarır. Tavan aşılınca hiçbir şey başarısız olmaz, görev ASSIGNED kalır; agent.session.ended drain'i artık ŞİRKET ÇAPINDA (boşalan kapasite yalnız kapanan ajanın kuyruğuna değil, öncelik/yaş sırasıyla bekleyen işe gider — yoksa tavanı dolduranların arkasındaki iş süresiz bekler). Sözleşme Kevin'e (CLI/broker) ve god'a DONMUŞ olarak yayımlandı. Testler: mcp-gateway.int 9, session-gate.int 4. Ayrıca full-schema.int'in "her tablonun satırı olmalı" iddiası onarıldı — 0026/0027 (T17/T19) satır eklemeden geçmişti, şimdi 0028 ile birlikte üçü de kapsanıyor. Aile B (create_task/delegate_task/complete_task/request_review… — worker'ın action dispatch'i) HENÜZ MCP'de sunulmuyor: onu sunmak dispatch'i ortak bir eve TAŞIMAYI gerektiriyor, yeniden yazmak İKİNCİ bir durum yazarı yaratırdı (INV-13). god'a sıralama sorusu olarak iletildi. |
