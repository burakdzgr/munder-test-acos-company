# PROGRESS-UI

UI overhaul ilerleme kaydı — görevler 36-UI-OVERHAUL-AND-DESKTOP.md §12 (U01–U16) sırasına göre işaretlenir.
Referans görünüm: `docs/architecture/docs/ui/acos-final.html` (onaylı mockup). PixelLab asset işi:
`docs/architecture/docs/PixelLab-ASSET-BRIEF.md`.

| Görev | Durum | Tarih | Not |
|---|---|---|---|
| U01 Dark theme tokens + Tailwind theme + PREVIEW page | ✅ Tamamlandı | 2026-08-13 | `packages/ui/src/theme/acosDark.ts` (36 §2 birebir: 4 surface + line + 3 fg, 7 department accent, 11-durum presence seti; `acosDarkCss` `:root` değişken bloğu — surface/text var adları mockup'la aynı: `--bg-0`…`--fg-2`, dept/presence `--dept-*`/`--presence-*`) + `theme/departmentColors.ts` (+`departmentColor()`/`presenceColor()` fallback'li yardımcılar). `acosPreset` genişledi (additive, N6): `acos.*`/`dept.*`/`presence.*` renk skalaları + fontFamily (Inter sans / JetBrains Mono mono); eski `ink`/`accent` skalaları mevcut view'lar için duruyor. PREVIEW: `apps/web/src/theme/PreviewPage.tsx`, auth'suz `/theme-preview` route'u — token swatch'ları, 11 presence pill, tipografi (13px Inter + tabular mono), 5 buton varyantı, mock panel (28px header + tab + hairline + status bar + ince scrollbar). Kabul doğrulandı: sayfa koyu temada render ediyor (Playwright screenshot ile bakıldı); `turbo build+typecheck+lint+test` 13 workspace'te 52/52 yeşil + check-deps temiz; @acos/ui testine token/CSS-var doğrulaması eklendi. Yeni dep yok. |
| U02 App shell restyle (icon rail + top bar) | ✅ Tamamlandı | 2026-08-13 | `AppShell.tsx` acosDark chrome'a taşındı: 38px top bar (marka · şirket seçici · org'dan canlı headcount'lu takım chip'leri [`member_of` edge sayımı] + "+ Takım" · global ajan arama [Enter→detay] · jeton/önbellek pill placeholder'ı [veri U11] · "+ Ajan Ekle"→HireWizard · gerçek `approvals.list(pending)` sayılı onay rozeti · 🔔/⚙ placeholder · "Founder ▾") · 44px sol icon rail (14 view, e2e erişilebilir adları `aria-label` ile birebir korundu, aktif vurgu) · 24px status bar (ws + son event + U11 maliyet şeridi yeri). Eski view'lar U03'e kadar koyu çerçeve içinde açık "ada"da (N6). **Aynı görevde Founder kararı (A11, _DECISIONS.md):** login UI kaldırıldı — `AUTH_AUTOLOGIN` (default true) ile server cookie'siz `/api/v1` GET'lerinde gerçek Founder session'ı basıyor (`AuthService.autologinFounder`, `auth.autologin.succeeded` audit'i; health/ws hariç); `/login` otomatik ana sayfaya sekiyor, Sign out kalktı; auth altyapısı (session/CSRF/PAT/TOTP/S6/S7) aynen yerinde, `false` ile eski akış döner. e2e helper'ı formsuz girişe güncellendi. Kabul: canlı stack'te cookie'siz `me()` → 200+cookie'ler doğrulandı; **14/14 Playwright e2e yeşil** (1.5m, autologin ile); ekran görüntüsüyle görsel doğrulama; tam repo turbo 52/52 + check-deps temiz. |
| U03 dockview CommandCenter + panel wrapper + presets + focusStore | ✅ Tamamlandı | 2026-08-13 | `dockview-react@8.1.0` (tek yeni dep, 36 §1). `shell/CommandCenter.tsx`: `/c/$companyId` varsayılan girişi artık 3 sütunlu dockview (sol Hafıza/Skiller/Tarayıcı-P2 · orta Terminal/Raporlar/Görevler · sağ Ofis/Kod + Çalışanlar/Takım/Konuşma) — 15 panel kaydı (14 view + Kod/Tarayıcı placeholder), sürüklenebilir splitter/tab'lar, yerleşim `uiPrefsStore`'a persist (500ms debounce `toJSON`, bozuk kayıtta default'a düşer). **Lazy panel**: içerik yalnız görünür tab'ken mount (`onDidVisibilityChange`) — gizli tab'lar Query cache'i ısıtmaz (aktivasyonda bayat query refetch olur), ağır view'lar (Pixi/xterm/cytoscape) aynı anda çalışmaz; landing istekleri 11→6. 3 preset (36 §3): Operations (ofis yarı ekran) / Engineering (terminal+kanban büyük) / Overview (maliyet+onay+rapor) — top bar Ops/Eng/Genel butonları (`requestPreset` → presetSeq). `stores/focus.ts` = `selectedAgentId` (panel bağlantıları U05–U09). Icon rail'e COMMAND girişi (exact-active); eski route'lar açık adada sürüyor (N6). dockview→acos tema köprüsü `index.css`'te (`--dv-*` token override, 28px header). **Bulunan+düzeltilen regresyon**: landing'in erken cache ısıtması + WS abonelik boşluğu 07-comms'u kalıcı-bayat listeye düşürüyordu → lazy panel + `team./department.created`→channels invalidation (11 §2). Kabul: **14/14 e2e yeşil (3.4m)**; 3 preset ekran görüntüsüyle doğrulandı; tam repo 52/52 + check-deps temiz. |
| U04 Office floorplan renderer (dinamik zonlar; U15'e kadar daireler) | ✅ Tamamlandı | 2026-08-13 | `features/office/floorplan.ts` (saf, Pixi'siz, 7 test): server layout'unun zon dikdörtgenlerinden deterministik görsel türetim — dış duvar zarfı, oda başına bölme duvarları (alt kenarda ortalanmış kapı boşluğu kesilmiş), giriş kapısı+paspas, prop yerleşimi (ilk odada server rack, oda başına bitki, meeting masası+sandalyeler, kahve, LOBBY resepsiyonu), departman accent'leri (36 §2 paleti, zone.color varsa o; executive→executive). `OfficeCanvas.tsx` painter: koridor zemini, accent-tonlu satranç oda zeminleri, pseudo-3D duvar vurgusu, ahşap masa+sandalye+monitör (monitör ekranı oturan ajanın CANLI durum rengiyle — dinamik layer), fit-to-view kamera (pan/zoom'a dek; resize'da yeniden fit), rozet renkleri §2 presence paletine hizalandı; avatarlar daire (U15). **Bulgu**: layout istemciye bugüne dek hiç ulaşmıyormuş (presence snapshot taşımıyor, endpoint yoktu — eski zone çizimi ölü koddu) → N1 kapsamında read-only `GET /companies/:id/office/layout` eklendi (projector.layoutFor, membership 404, OpenAPI 64 path, SDK `api.office.layout`); OfficeView query+`engine.setLayout` ile bağlıyor. N2 korunumu: hareket mantığına sıfır dokunuş — yalnız çizim; debug hook + DOM fallback + testid'ler aynen. Ayrıca 04-events-live spec'i veri hacmine dayanıklı yapıldı (sabit 20 tık → company.created görünene dek, 300 tavan; uzun ömürlü stack'in geçmişi 20×50 event sınırını aşmıştı). Kabul: **14/14 e2e yeşil (1.6m)**; canlı stack'te floorplan ekran görüntüsüyle doğrulandı (duvar/kapı/masa/prop/monitor-tint); tam repo 52/52 + check-deps temiz. |
| U05 Terminal S/M/L grid + per-agent aç/kapa | ✅ Tamamlandı | 2026-08-13 | `features/terminals/TerminalGrid.tsx` — CommandCenter'ın orta "Terminal" tab'ı (dark chrome; lazyPanel'e dark modu eklendi): açık `terminal_session` başına bir hücre, hücre başlığı = presence-dot (canlı WS badge, §2 paleti) · ajan adı (tıkla→`focusStore` seç/kaldır, seçilide accent çerçeve) · TASK-n · ⤢ (U06'da bağlanır, şimdilik disabled) · ✕ (yalnız görünümü kapatır — oturum sunucuda sürer). Gövde = gerçek PTY: `terminal:<id>` topic'i, ring replay + canlı frame'ler (T41 yolu aynen), ResizeObserver'lı FitAddon (dockview pane resize window event'i üretmiyor). S/M/L yoğunluk chip'leri → 2/4/6 kolon + yoğunluğa göre satır yüksekliği/font (S:12 M:10 L:9). `stores/terminalGrid.ts` (persist): density + `closedAgentIds` (kapalı-liste; oturumlar varsayılan açık) + hepsini-aç. Roster toggle: AgentsView kartlarına ⌨ +/✕ (Link içinde stopPropagation'lı; e2e `agent-grid a` sayımı etkilenmez). Kabul: gerçek sandbox oturumuyla canlı doğrulandı (gateway `terminal.run` → hücrede npm+tail akışı; S/M yoğunluk geçişi, hücre kapat → "n gizli · hepsini aç" → geri aç); **14/14 e2e yeşil (3.6m)**; TerminalsView route'u değişmedi (09 spec selektörleri aynen); tam repo 52/52 + check-deps temiz. |
| U06 Focus-terminal + Founder-directive prompt (MessageService) | ✅ Tamamlandı | 2026-08-13 | TerminalGrid'e `FocusModal`: ⤢ hücreyi tam-ekran modala büyütür — canlı çıkış (aynı `terminal:<id>` WS topic'i; modal açılınca hücre aboneliğini bırakır ki refcount-multiplex'te modalın taze subscribe'ı TAM ring replay alsın) + altta prompt. **N4 birebir**: prompt `MessageService` yolundan Founder→ajan direktifi — `POST /channels {agentId}` (get-or-create DM) + `POST /channels/:id/messages` (sender=Founder, server-mint idempotency); kind=text + `refs:[{kind:"task",id}]` (oturumun task'ı — yeni wire tipi YOK, N1); persisted + audited + `agent.message.sent` emit (ofis etkileşimi + Konuşma paneli invalidation'ı mevcut akışla). Agent loop'a doğrudan yazma yok. Esc/backdrop kapatma; ajansız oturumda prompt disabled; başarı/hata satırı. Kabul canlı doğrulandı: gerçek PTY oturumunda modal ring-replay+canlı akış; direktif gönderimi → UI onayı → API'den DM'de `senderAgentId:null` + task ref'iyle kalıcı; `agent.message.sent` event'i (#1785) status bar'a CANLI düştü; **14/14 e2e yeşil (3.7m)**. |
| U07 Sol paneller: hafıza graf + liste; skill matrisi | ✅ Tamamlandı | 2026-08-13 | `features/memory/MemoryPanel.tsx` (dark): üstte animasyonlu ilişkisel graf — canvas 2D, soluk "brain-field" nokta örgüsü + radial glow üstünde node'lar (renk=tip [failure/procedural/semantic/episodic/social/decision], boyut∝importance, opaklık∝confidence, candidate'a kesikli sarı halka), edge'ler `memory_relations` stiliyle (supports yeşil / contradicts kırmızı-kesikli / derived_from mor / supersedes gri); pozisyonlar node-id hash'inden deterministik + yumuşak drift; hover→provenance tooltip'i (tip/scope/conf/imp). Observatory endpoint'leri (T48) aynen. Altta canlı liste: `memory.created` WS invalidation'ıyla tazelenir, İLK render sonrası gelen anılar `mem-fade-in` ile belirir; "n anı · m çelişki" sayacı. Görünümler: Graf / Liste / Zaman (gün ayraçlı). `features/skills/SkillsPanel.tsx` (dark): "Beceriler" = T47 matrisi (ajan · beceri · L1-5 bar [kategori-hash accent, opaklık∝confidence] · kanıt/güven tooltip'i, boş-durum metni); "Terfi Adayı" = U12'ye işaretli boş durum. Invalidation map'e `memory.*`→memories ve `agent.skill.*`→skills aileleri eklendi (24 §4.2). CommandCenter sol tab'ları artık dark paneller; MemoryView/SkillsView route'ları aynen (N6 — 11-learning spec selektörleri değişmedi). Kabul: canlı stack'te graf+tooltip+liste ekran görüntüsüyle doğrulandı (16 anı, 8 çelişki rozeti); matris boş-durumu veriyle tutarlı (agent_skills kanıtla oluşur); **14/14 e2e yeşil (1.7m)**. |
| U08 Kanban drag-drop (yetkili geçiş/409) + Raporlar | ✅ Tamamlandı | 2026-08-13 | `features/tasks/TaskBoardPanel.tsx` (dark, HTML5 dnd): 16-durum makinesi kolonlara açılmış — çekirdek kolonlar (BACKLOG/ASSIGNED/IN_PROGRESS/REVIEW/CHANGES_REQUESTED/QA/APPROVAL/DONE) hep görünür, kenar durumlar (DRAFT/PLANNED/WAITING/BLOCKED/QA_FAILED/REJECTED/FAILED/CANCELLED) yalnız doluyken. Sürükleme sırasında makine-legal hedefler `TASK_NEXT_STATUSES` ile vurgulanır (yalnız affordance); drop MEVCUT transition API'sini çağırır, **optimistic yok** → illegal geçişte kart yerinde kalır ve sunucunun gerçek 409'u banner'da aynen yüzeye çıkar (N5). Kartlar: displayNumber · P0-P3 sol-kenar rengi · kind etiketi · başlık · owner avatar noktası+adı; `focusStore` seçili ajanın kartlarını çerçeveler; canlı güncelleme `task.*` WS invalidation'ı. "⧉ Tree/DAG" pop-out'u route view'a gider. `features/costs/ReportsPanel.tsx` (dark): T49 executive report listesi + içerik görüntüleyici (15s poll aynen). Kabul canlı doğrulandı: API'yle açılan DRAFT task sürüklemeyle BACKLOG'a geçti (sunucu onayı), BACKLOG→IN_PROGRESS sürüklemesi `task_transition_invalid: illegal transition BACKLOG → IN_PROGRESS` 409'uyla sekti ve kart BACKLOG'da kaldı; status bar canlı `task.status.changed`; Raporlar panelinde 9 gerçek rapor. **14/14 e2e yeşil (3.7m)** (06/08 spec'leri TasksView route'una karşı aynen); tam repo 52/52 + check-deps temiz. |
| U09 Sağ: ofis panel wiring + Kod (IDE diff) + Çalışanlar/Takım/Konuşma | ✅ Tamamlandı | 2026-08-13 | `OfficePanel` (dark): kenardan-kenara floorplan + ince başlık (canlı ajan sayısı · **"+ Çalışan"**→mevcut hire akışı · **"⧉ Ayır"**→`window.open` popup; U14 Electron 2. BrowserWindow'a terfi ettirir); avatar tıklaması `focusStore`'u besler. `OfficeWindow` + shell'siz `/c/$companyId/office-window` route'u (kendi RealtimeDispatcher'ı — popup ayrı döküman/WS); canlıda doğrulandı (28 ajan, tam kat planı). `CodePanel`: focus ajanın aktif task bağlamı ("Task: TASK-n · durum · branch") + unified diff'ten dosya ağacı + renkli read-only diff (MEVCUT T43 `reviews/:id/diff` endpoint'i); aktif task'ta review yoksa son 6 task'a düşen probe (`useQueries`); parser `code/diff.ts`'e ayrıldı + 5 unit test (stack'te review satırı 0 — gerçek PR akışı T43'le oluşur; boş durumlar canlıda dürüst doğrulandı: "Deniz Kaya · TASK-145 · REVIEW" başlığı + diff-yok metni). `RosterPanel`: initials avatar (U15 pixel yüz bekliyor) · ad/numara/seniority · canlı presence pill · ⌨ terminal toggle (36 §4) · ↗ Agent Monitor; satır=focus seçimi. `TeamPanel`: takım/departman başına üye sayısı (aktif member_of), lead, canlı presence dağılımı. `ChatPanel`: kanal şeridi (DM adları dmKey'den çözülür) + mesaj akışı + Founder gönderimi (mevcut comms API, N4); focus ajana "+ DM" kısayolu — canlıda DM aç+gönder+persist doğrulandı. Kabul: **14/14 e2e yeşil (1.9m)**; tam repo 52/52 + check-deps temiz. |
| U10 Hire modal (avatar/rol/engine/model/expertise/proje) + hire param'ları | ✅ Tamamlandı | 2026-08-13 | **Hire endpoint additive param'ları** (yeni tablo YOK, T19 tek-transaction aynen): `avatarId` (regex `avNN` → `agents.avatar_url="pixel:avNN"`) · `expertise[]` (≤12; T47'nin TEK evidence yazarı `appendEvidenceInTx` ile kind=manager_eval, ref=`hire:<id>`, note=`hire:expertise` — gerçek skill+agent_skills+evidence satırları, deterministik seviye) · `projectId` (şirket doğrulamalı → `project_members` role=engineer + katalogdaki `project.member.added`) · `modelBinding{provider,model}` (`model_providers.name` çözümü, bilinmeyene açık 412; primary binding değiştirilir + `agent.model.binding.changed` — kimlik modelden ayrık, INV-9). Contracts+SDK+OpenAPI güncel. **`HireModal.tsx`** (dark, mockup birebir): Ajan adı · kategorili rol kütüphanesi (LİDERLİK/MÜHENDİSLİK/ÜRÜN/PAZARLAMA — pozisyonlardan türetilir) · 24'lük piksel avatar grid'i (U15 kütüphanesi) · 6 AI engine kartı (Claude Code/Codex/Groq/DeepSeek/Kimi/Ollama→provider adları) + model select ("Default (şirket profili)"=binding gönderme) · expertise chip'leri (öneri+Enter) · Proje + Birim seçimi (yönetici birim liderinden otomatik) · "Yerleştir →" = hire&activate. Top bar "+ Ajan Ekle" ve ofis "+ Çalışan" bu modalı açar; eski HireWizard Agents route'unda aynen (N6 — 03 spec selektörleri). Kabul canlı: modal ile EMP-039 "pixel:av05" avatarıyla aktif yerleşti; skills matrisinde React L1 + PostgreSQL L1 (1'er kanıt); `project_members(engineer)` satırı + `project.member.added`/`skill.evidence.recorded` event'leri DB'de doğrulandı; toast + roster/ofis güncellendi (yürüyerek giriş projector `agent.hired` akışıyla). **14/14 e2e yeşil (4.4m)**; tam repo 56/56 + check-deps temiz. |
| U11 Token/cache göstergesi + toast + bildirimler + hedefler paneli | ✅ Tamamlandı | 2026-08-13 | Yeni read-only `GET /companies/:id/llm/usage` (N1; costs modülü): bugünün `llm_calls` agregatı (calls/tokensIn/Out/Cached/costCents — T29 cached-token muhasebesi); contracts `LlmUsageResponseSchema` + SDK `costs.llmUsage`, OpenAPI 65 path. **Top bar pill'i** gerçek veride: "⚡ bugün {out} jeton · %{hit} önbellek" (hit=cached/(in+cached), 30s poll; tooltip'te çağrı/in/out/cache dökümü). **Bildirimler**: `stores/notifications.ts` (toast kuyruğu + son-20 + unread) — dispatcher WS event'lerinden basar: `approval.requested`(warn) · goal-seviye `task.completed`(ok; kind board cache'inden çözülür) · `memory.created`(ok) · `security.alert`(danger); `Toasts` sağ-alt slide-in + 5s otomatik kapanış; zil rozeti + son-bildirimler dropdown'ı (aç=okundu). U14 aynı push'ları Electron native'e iletecek. **"Bugünün Hedefleri"** (`shell/GoalsPanel.tsx`): CommandCenter sol-alt overlay — kind=goal task'ları (server filtresi), done/total sayacı, strikethrough, `task.*` invalidation'ıyla canlı, katlanabilir. **Status bar maliyet şeridi**: company bütçe forecast'ı ($spent/limit + bar + on-track/breach). Kabul canlıda: pill "5.7k jeton · %0" (gerçek llm_calls), hedefler 0/33, şerit "$130.15/5000 on track"; toast GERÇEK pipeline'la tetiklendi (flaky-fixture task FAILED → konsolidasyon → memory.created → WS → "Yeni anı" toast'u + zil 1 → dropdown → temiz). **14/14 e2e yeşil (2.0m)**; tam repo 52/52 + check-deps temiz. |
| U12 Emergent skill discovery read-model + terfi akışı | ✅ Tamamlandı | 2026-08-13 | **Şema değişikliği YOK** — hesaplanan read-model (36 §10'un izin verdiği tek backend eklemesi). `apps/server/modules/skills/emergent.ts`: son 90 günün DONE task'ları taranır — sinyal 1: normalize başlık kalıbı (ilk 3 anlamlı kelime) ile ≥3 tekrar; sinyal 2: beceriye dönüşmemiş `context.skills` etiketleri; mevcut agent_skills'te olanlar elenir; skor=min(.95, n/6), sebep+kanıt task id'leri. `GET /skills/candidates` (yüzeye çıkarma = teklif: ilk-görüş (agentId,skillName) çiftleri **`agent.skill.candidate.proposed`** yayar — events tablosundan dedupe). Event kataloğa + doc 10 §10 tablosuna eklendi (190 durable; registry↔doc eşitlik testi güncel — bu arada testin eski iç-içe doc yolunu okuduğu keşfedilip gerçek yola düzeltildi; turbo cache maskeliyormuş). `POST /skills/candidates/promote`: zaten-beceri → 409; kanıt task'ları tek tek **T47'nin TEK evidence yazarından** (`appendEvidenceInTx`) geçer — skill get-or-create + agent_skills + skill_evidence(task_success, ref task:id, note emergent:U12) + deterministik seviye recompute (`agent.skill.updated` seviye değişince, T47 disipliniyle aynen). SkillsPanel "Terfi Adayı": gerçek adaylar (sebep + n-iş/skor çipleri + rozet) + "↑ Terfi Et" → toast + matris invalidation. Contracts/SDK/OpenAPI (67 path). Kabul canlıda: 4 benzer DONE iş → aday "Implement Csv Export · 4× benzer iş · skor 0.67" → proposed event (1, dedupe'lu) → terfi → matris satırı "Aylin · Implement Csv Export · L1 (4 kanıt)"; **14/14 e2e yeşil (4.0m)**; tam repo 52/52 + check-deps temiz. |
| U13 Electron scaffold (main+preload+builder) + backend lifecycle | ✅ Tamamlandı | 2026-08-13 | Yeni `apps/desktop` workspace'i (14.; check-deps WORKSPACES + eslint MATRIX/ELEMENT_DIRS + kök tsconfig references güncellendi; `@acos/*` bağımlılığı SIFIR — aynı apps/web bundle'ının üstünde kabuk, fork değil). Electron 43.4.0 + electron-builder 26. **U13a**: `main.ts` — güvenlik seti aynen (`contextIsolation:true, nodeIntegration:false, sandbox:true, webSecurity:true`, yalnız preload köprüsü); koyu zemin/titlebar, 1440×900 default + min 1100×720, geometri `userData/window-state.json`'da (clamp'li, junk'a dayanıklı); dev→Vite (`WEB_PORT`), paketli→`ACOS_BASE_URL ?? http://localhost:5173` (compose web nginx'i AYNI SPA'yı servis eder + /api,/ws proxy'ler — diskten file:// yükleme same-origin API'yi kırdığından bilinçli yapılmadı, kayıtlı karar). `preload.ts`: `window.acosDesktop{shell,platform,notify}` (U14a native-bildirim IPC'si stub olarak hazır). **U13b**: sağlık kapısı — `/api/health` down ise splash + (`ACOS_COMPOSE_FILE`+docker varsa) `docker compose up -d` + 180s health poll; ACOS_BASE_URL'li hazır stack'te boot atlanır. Tray: göster/gizle · stack başlat/durdur · compose loglarını aç · çıkış (U14c presence ikonuna terfi edecek). `config.ts` saf yardımcılar + 8 unit test. **Kabul canlı**: pencere compose stack'ine karşı açıldı, CDP probu → köprü `shell:"electron"`, renderer'da Node sızıntısı YOK (sandbox doğrulandı), autologin'le direkt şirket seçimi; `pnpm --filter @acos/desktop package` → **`ACOS Setup 0.0.0.exe` (95MB NSIS)** üretildi. Not: workspace `build`=tsc (CI güvenli), installer `package` script'inde. Tam repo 56/56 + check-deps 14 workspace temiz. |
| U14 Electron native bildirim + ofis detach + tray | ✅ Tamamlandı | 2026-08-13 | **U14a**: dispatcher `approval.requested`/`security.alert` WS event'lerini preload köprüsünden main'e iletir (`acos:notify` IPC, payload doğrulamalı) → Electron `Notification`; tıklama pencereyi odaklar + rotaya gider (approvals/events; yalnız `/` ile başlayan rotalar). **U14b**: "⧉ Ayır" köprü varsa `acos:open-office` → aynı güvenlik setiyle ikinci `BrowserWindow`, shell'siz `/c/:id/office-window` route'u (uuid doğrulamalı, singleton); tarayıcıda popup fallback aynen. **U14c**: AppShell hafif presence özetini besler (`needs-approval` > `active` [WORKING vb. rozet varsa] > `idle`) → `acos:presence` → tray ikonu renk değiştirir (gri/yeşil/amber) + tooltip; tray menüsünde "Onayları aç" (son companyId'ye navigate) + göster/gizle + stack başlat/durdur + çıkış. Köprü tipi `apps/web/src/types/acosDesktop.d.ts` — tüm çağrılar `window.acosDesktop?.` guard'lı, AYNI bundle tarayıcıda değişmeden çalışır (no-op). Kabul canlı (CDP): köprü fonksiyonları mevcut; native bildirim atıldı; detach → **ikinci BrowserWindow** açıldı (`/office-window`, 34 ajan, sayfa 1→2, ekran görüntülü); presence çağrısı temiz. Tarayıcı regresyonu: **14/14 e2e yeşil (2.1m)**; tam repo 56/56 + check-deps temiz. |
| U15 PixelLab asset entegrasyonu (atlaslar, avatar kütüphanesi) | ✅ Tamamlandı | 2026-08-13 | **Kayıtlı karar (Founder onaylı)**: PixelLab API anahtarı yok → sanat PROSEDÜREL üretildi (mockup'ın portrait/drawChar rutinlerinin portu); ÇIKTI SÖZLEŞMESİ brief §2 ile birebir, ileride PixelLab koşumu yalnız sanat dosyalarını değiştirir. One-time script `apps/web/scripts/generate-sprites.mjs` (pngjs, runtime-dışı): **24 karakter** (saç8×ten4×gömlek8 + gözlük/sakal/şapka/takım varyantları) → portre 24×24 + 4-yön × (4 yürüme + 1 idle) 16×20 = **504 frame tek sayfa atlas** (344×576, tek draw batch) + PixiJS `characters.json` + DOM için `portraits/avNN.png` + `avatars.json` indeksi + `CREDITS.md`; hepsi `apps/web/public/sprites/characters/` altında repo'ya gömülü. `features/office/characters.ts` (saf, Pixi'siz — ofis lint kuralı): `loadAvatars` + `resolveAvatarId(agentId, avatarUrl)` — hire modal'ın yazacağı `pixel:avNN` önceliği, yoksa agent-id hash'i (kalıcı kimlik = hep aynı karakter; 4 test). OfficeCanvas: daireler → `AnimatedSprite` — yön motorun AYNI interpolasyonundan türetilir (yeni hareket kaynağı yok, N2), yürüyüşte yön sprite'ı oynar, dururken idle frame; y-sort; presence rozeti baş üstünde, isim altta; atlas yüklenemezse daire fallback (N7). Roster piksel yüzleri (`portraits/avNN.png`, image-rendering:pixelated). Tile/prop'lar U04 procedural painter'da kalır (eşdeğer sonuç, CREDITS'te kayıtlı). Kabul: canlıda 36 ajan sprite olarak sahada (zoom görüntüsüyle varyant çeşitliliği + masa/monitör hizası), roster'da 36 portre; **14/14 e2e yeşil (2.3m)** (05-office spawn dahil); tam repo 56/56 + check-deps temiz. |
| R-P0A Test/demo verisi kirliliği (UI/UX review) | ✅ Tamamlandı | 2026-08-13 | **(1) Ephemeral e2e**: `pnpm e2e` → `scripts/e2e-stack.mjs` — ikinci compose projesi (`acos-e2e`) ofset host portları (pg 15432 / server 13000 / web 15173…), kendi atılabilir `data-e2e` dizini ve kendi workspace network/subnet'iyle (`acos-e2e-workspaces`, 172.31/16 — squid ACL'i e2e workspace egress'ini reddeder; hiçbir spec dış host'a çıkmadığı için kabul edilen daralma) dev stack'in YANINDA kalkar; Playwright `ACOS_WEB_URL/SERVER_URL/PG_URL/WORKER_CONTAINER` env'leriyle ona vurur; teardown `down -v` + dizin silme. Compose portları/network adı-subnet'i env-parametrik yapıldı (additive default'lar). Dev volume'una testler bir daha ASLA yazmaz. **(2)** `pnpm db:reset` (`scripts/db-reset.mjs`): down → bind-mount `data/` dizinini komple sil (pg + NATS stream + workspace'ler — kısmi silme yetim/replay bırakır) → `up --wait`; seed zaten yalnız Acme + 8 kanonik ajan. **(3)** Kirli volume sıfırlandı ve doğrulandı: şirket seçicide YALNIZ "Acme Technologies", ajanlarda YALNIZ EMP-001..008. |
| R-P0B Koyu tema app-wide (UI/UX review) | ✅ Tamamlandı | 2026-08-13 | Kök seviye: `index.css` `@layer base` → `html { color-scheme:dark; bg-acos-bg0; text-acos-fg0 }` — tüm route'lar, popup pencere ve modaller koyu tuvalde. **Anahtar hamle**: legacy `ink` skalası preset'te KOYU semantiğe ters çevrildi (50→#131820 yüzey … 900→#e6edf3 metin; muted 400 #7c8794 AA-ayarlı) — ink-50 yüzey + ink-800/900 metinle kurulmuş 14 eski view yeniden yazılmadan koyu dile geçti; `accent-600` metin linkleri koyuda okunur tona alındı. Primitifler token'a: Card/Dialog `bg-acos-bg1`, form kontrolleri `bg-acos-bg2`; TasksView kart `bg-white`→`bg-acos-bg2`; OrgChart cytoscape node/edge renkleri koyuya. AppShell'deki "açık ada" sarmalayıcı kaldırıldı; CommandCenter lazy panel gövdesi de token'da. **"Backend" çipi → beyaz ekran** teşhisi: route hatası değil, AÇIK temalı OrganizationView'un kendisiydi — artık koyu (P1-A ayrıca çip davranışını filtreye çevirir). Doğrulama: 13 routed view'un tamamı screenshot-luma taramasından geçti (ortalama parlaklık 13–25; açık kalıntı: YOK) + Organization/Costs/Memory gözle kontrol; AA için muted metinler açıldı. |
| R-P1A Takım çipleri = filtre (UI/UX review) | ✅ Tamamlandı | 2026-08-13 | Çip tıklaması artık org sayfasına gitmiyor — `focusStore.teamFilter` + paylaşılan `useTeamMemberSet` hook'u (aktif member_of edge'lerinden üye seti; boş set = geçerli durum) ile KOMUTA MERKEZİNİ o takıma filtreler: Roster (banner + boş-takım durumu "+ Ajan Ekle yönlendirmesi"), TerminalGrid (yalnız takım oturumları + "filtre: X" notu), TaskBoard (owner takımda VEYA task.orgUnitId=takım), Ofis (takım-dışı avatarlar 0.22 alfaya düşer). Aktif çip vurgulu; ikinci tık ya da "✕ filtre" temizler; çip adları max-w-28 + title tooltip (kırpılma düzeltmesi; "Comms Team…" kopyaları zaten P0-A resetiyle gitti — onlar e2e kalıntısıydı). 0 üyeli takım çipi de tıklanır → boş-durumlar, asla beyaz ekran değil. Canlı doğrulama: roster 8 → "Backend" çipi → 3 (Kerem/Alex/Deniz) → temizle → 8. |
| R-P1B Hafıza dedup + Observatory graf (UI/UX review) | ✅ Tamamlandı | 2026-08-13 | **Kök neden**: konsolidasyon workflow'u embedding BAŞARISIZ olunca (offline/scripted profil — seed/demo tam bu) benzerlik kontrolünü tamamen atlayıp persist ediyordu → her tekrar eden fixture hatası yeni kopya anı ("Retry uploads with backoff" ×10). **Düzeltme**: deterministik exact-title fast-merge — `MemoryConsolidationService.findExactActive` (aynı scope/scopeRef + case-insensitive başlık + active) → workflow'da §5.8 sonrası, EMBED HARCAMASINDAN ÖNCE probe; isabet varsa mevcut `mergeIntoExisting` yolu (evidence append + version satırı + memory.updated) ve `merged` sayacı. Online profilde de embed öncesi kesin-kopyaları ucuza yakalar; agent-worker imajı yeniden build edildi. **Observatory graf** (`MemoryGraph.tsx`): renk artık TİP'e göre (panelle aynı `memoryTypeColor` paleti; eskiden scope'tu), boyut∝importance, opaklık∝confidence; layout `cytoscape-fcose` (gerçek force, `packComponents`, cose-blob yerine küme ayrışması); etiket yönetimi — wrap + koyu outline + `min-zoomed-font-size` (uzaklaşınca etiketler tamamen kaybolur, yoğun graf metin çorbasına dönmez); seçili node mavi çerçeve; koyu zemin. |
| R-P2 Ofis doldurma + etiketler (UI/UX review) | ✅ Tamamlandı | 2026-08-13 | Fit-kamera **cover-fit**'e geçti (2.5x tavanlı): kat planı paneli her zaman kenardan kenara doldurur; taşan eksen üste/ortaya demirlenir ve pan/zoom ile erişilir — dar sağ panelde letterbox şeridi bitti (panel-only ekran görüntüsüyle doğrulandı: duvarlar panel kenarlarına dayanıyor, ENGINEERING odası + 8 sprite ekranı dolduruyor). **İsim etiketleri zoom-eşikli**: camera scale < 0.85'te tüm adlar gizlenir (seçili ajanınki kalır), wheel zoom'da anında tepki (ticker'da eşik-geçiş kontrolü) — kalabalıkta örtüşme yok; test-ajan çöpü zaten P0-A resetiyle gitti. |
| R-Kabul UI/UX review kapanışı | ✅ Tamamlandı | 2026-08-13 | Kabul kriterlerinin dördü de sağlandı: **(1)** temiz boot'ta yalnız Acme + EMP-001..008 (API'den doğrulandı); **(2)** 13 routed view'un tamamı koyu ve okunur (luma taraması + gözle); **(3)** hiçbir çip beyaz ekran açmıyor (çipler artık filtre; org view koyu); **(4)** **20/20 e2e YEŞİL — artık EPHEMERAL stack'te** (`pnpm e2e`, 1.8m; dev volume'una sıfır dokunuş, teardown temiz). Koşuda yakalanan ek kök neden: env'siz shell'den yapılan container rebuild'leri agent-worker'ı sessizce `llmMode:live`'a düşürüyordu (kaskad/konsolidasyon spec'leri stall) — e2e runner artık `LLM_MODE=scripted`'ı deterministik set ediyor (32 §6) ve dev worker scripted olarak yeniden yaratıldı; `WORKSPACE_NETWORK` de env-geçişli oldu (e2e workspace container'ları kendi network'üne biner, teardown'da compose-dışı workspace container'ları temizlenir). Tam repo 56/56 + check-deps 14 workspace temiz. |
| U16 Visual QA (Playwright preset screenshot + FPS + a11y + 14/14 e2e) | ✅ Tamamlandı | 2026-08-13 | Yeni `e2e/15-ui-overhaul.spec.ts` (6 test): **(1)** preset doğrulaması — default + Ops/Eng/Genel uygulanır, her biri karakteristik panelini gösterir (office-panel / task-board / costs-total) + 4 ekran görüntüsü test artifact'ı olarak ekli; **(2)** ofis spawn smoke — API'den hire → `__acosOffice` debug hook'unda agentCount artar + choreography akar ve **debug ring'deki HER talimatın causeEventId'si doğrulanır** (N2/DoD "no random motion" denetimi); **(3)** FPS smoke — canlı sahnede rAF ölçümü >30 tabanı (60 hedef); **(4)** N7 — 1000px viewport'ta çekirdek kontroller görünür + yatay taşma yok (top bar chips/preset/arama `lg`/`md` altında çekilir, header overflow-hidden); **(5+6)** no-WebGL — **bulgu**: Pixi v8 WebGL yokken kendi Canvas2D renderer'ına iniyor (ofis yine çizilir; testle sabitlendi), TOTAL canvas arızasında ise OfficeCanvas catch'i DOM listesine düşer (`office-fallback`). Kabul: **20/20 e2e yeşil (3.0m)** — 14 eski spec selektörleri değişmeden + 6 yeni QA testi; tam repo 56/56 + check-deps 14 workspace temiz. **DoD (36 §13) taraması**: koyu 3-sütun CommandCenter default giriş (tarayıcı+Electron) ✅ · pixel avatar yürüyüşleri causeEventId'li ✅ · terminaller gerçek PTY (S/M/L, aç/kapa, denetlenebilir focus+prompt) ✅ · kanban 409 sektirmesi ✅ · hire modal → yürüyerek giriş ✅ · emergent skill terfisi ✅ · NSIS installer + native bildirim + tray + 2. pencere ✅ · aynı bundle tarayıcıda değişmeden ✅. Doc-36 kapsamı dışındaki dokunuşların hepsi kayıtlı: AUTH_AUTOLOGIN (Founder direktifi, A11) · `agent.skill.candidate.proposed` (§10'un istediği katalog eki + doc 10 tablosu) · desktop workspace koruma-ağı kayıtları · e2e helper/spec dayanıklılık düzeltmeleri. |
| R-CanlıLLM Canlı model geçişi + board sadeleştirme + proje ortamı | ✅ Tamamlandı | 2026-08-14 | Founder direktifi: **(1) Canlı LLM**: `infrastructure/docker/.env` (GİTİGNORE'lu) ANTHROPIC_API_KEY + `LLM_MODE=` (boş=canlı) — env'siz rebuild'ler artık modu bozamaz; seed anthropic provider + model_profiles (reasoning/coding=claude-sonnet-4-5, fast=claude-haiku-4-5) idempotent kurdu; 8 kanonik ajanın boş binding'leri T19 PUT ile dolduruldu (8×primary + 8×fast, denetimli `agent.model.binding.changed`). Canlı probe anahtarı doğruladı ama hesapta KREDİ YOK ("credit balance is too low") — kredi yüklenene dek ajan koşuları başlamaz; stack talep gereği canlıda bırakıldı. **(2) Board sadeleştirme**: TaskBoardPanel çekirdek kolonlar BACKLOG/IN_PROGRESS/REVIEW/QA/DONE (ASSIGNED/CHANGES_REQUESTED/APPROVAL yalnız doluyken görünür — kart kaybolmaz); DONE sonrası **🚀 Deploy Ready** PRESENTASYONEL kolonu (16-durum makinesi sabit, CLAUDE.md) — `uiPrefs.deployReadyTaskIds` kalıcı Founder işareti, yalnız DONE kabul eder, DONE'a geri sürükleme işareti siler. Canlı doğrulama: kolonlar "BACKLOG | IN_PROGRESS | REVIEW | QA | DONE | 🚀 DEPLOY READY". **(3)** Workspace imajı hazır; proje başlatma yolu Projects → 3 alan → intake. |
| R-TR+Org Tam Türkçeleştirme + organizasyon yönetimi | ✅ Tamamlandı | 2026-08-14 | Founder direktifi. **(1) Organizasyon yönetimi (yeni yetenekler)**: `PATCH /agents/:id/placement` — birim/pozisyon/kıdem/yönetici değişikliği TEK tx'te org advisory lock altında (eski member_of/reports_to+manages kenarları biter, yenisi `createEdgeInTx`'ten — döngü kontrolü + kanal üyelik senkronu + org.edge.* event'leri; `agent.updated` + `org.reorg.applied` yayılır); `POST /org/units/:unitId/archive` — ön koşullar 409 (aktif alt birim yok, aktif/duraklatılmış ajan yok; draft bloklamaz çünkü draft→offboarded geçişi makinede yok), kalan birim kenarları biter + `org.unit.archived` (katalogda MEVCUTTU — yeni event yok). Contracts+SDK+OpenAPI 69 path. **Organizasyon ekranı**: Çalışanlar tablosu (rol/birim değiştir modalı, duraklat/devam, işten çıkar), birim arşivle, **"Sıfırdan kur"** akışı (SIFIRLA onaylı: tüm ajanlar offboard [hafıza korunur] + birimler yapraktan köke arşiv) — hepsi gerçek 409'ları aynen gösterir. **(2) Tam Türkçe UI**: 30+ dosya — nav rayı (KOMUTA/OFİS/GÖREVLER/…, `nav-*` testid'leri eklendi), tüm view'lar (auth/şirket seçimi/ajanlar/görevler/onaylar/iletişim/maliyet/rapor/olay/hafıza/yetenek/proje/terminal/ofis LOBİ-Toplantı dahil), kanban kolon başlıkları TR görünüm haritası (testid'ler ham enum'la), primitifler ("Diyaloğu kapat", "Kayıt yok."). e2e seçici sözleşmesi korunarak: nav seçicileri `nav-*` testid'e, TasksView kolonları `{id,label}` ayrımına (testid=id sabit), metin assert'leri yeni Türkçe literallere güncellendi ("İşe al & aktifleştir", "Founder (sanal)", "Görev yok", "Bekleyen onay yok", "{n} ajan ofiste", "öngörü", "Daha eski olayları yükle", "Birim oluştur", "Diyaloğu kapat"). Tam repo 50/50 + check-deps temiz; **20/20 e2e yeşil (3.6m, ephemeral stack)**. Canlı doğrulama: nav rayı 15 Türkçe etiket, org ekranında geçici hire → birim+kıdem placement (200) → dolu-birim arşivi 412 → self-manager 412 → offboard; "Arşiv Testi" birimi oluştur+arşivle (201/200). Not: dev DB'de 8 kanonik ajanın Founder oturumundan (21:34Z, autologin sonrası ardışık offboard çağrıları) işten çıkarılmış olduğu görüldü — kod bunu yapmıyor, kullanıcı denemesi; "Sıfırdan kur" + "Ajan işe al" ile yeniden kadro kurulabilir. |
| R-OrgTemiz Offboarded gizleme + JSON/CSV org içe aktarma | ✅ Tamamlandı | 2026-08-14 | Founder direktifi ("sıfırdan kur dediğimde şirket boşalmalı; org'u JSON/CSV import ile kurabilmeliyim"). **(1)** Organizasyon ekranı işten çıkarılanları artık HİÇ göstermez (şema + Çalışanlar tablosu; kenarlar endedAt=null filtreli) — "Sıfırdan kur" sonrası sayfa gerçekten boş; geçmiş kayıtlar Ajanlar sayfasının durum filtresinde durur. **(2)** `OrgImportModal`: JSON (units+positions+agents, slug/title/name referanslı, yönetici zinciri topolojik sırayla) veya CSV (yalnız ajan satırları; name,position,unit,manager,seniority,autonomyLevel,persona,expertise) yapıştır/dosya yükle → önizleme+doğrulama → sıralı kurulum. SIFIR yeni backend: her satır MEVCUT denetimli endpoint'lerden geçer (createUnit/createPosition/hire — org kuralları+model bağı seed'i sunucuda; satır satır hata raporu, mevcutlar "atlandı"). "Şablonu kopyala" web Claude'a organizasyon ürettirme akışı için hazır şablon verir. Kabul canlıda (UI'dan): 1 birim + 1 pozisyon + 2 ajan (yönetici zinciriyle) 4/4 kuruldu, şemada ok + tabloda satırlar doğrulandı, test verisi offboard+arşivle temizlendi (görünür durum değişmedi). |
| R-RolImport Rol/pozisyon toplu içe aktarma | ✅ Tamamlandı | 2026-08-14 | Founder direktifi. Pozisyonlar kartına "Toplu içe aktar": JSON dizisi `[{title, defaultRole}]` (ya da `{"positions":[…]}`) VEYA satır formatı `Unvan, rol` (rol boşsa member; başlık satırı atlanır). defaultRole doğrulaması (executive|manager|lead|member|reviewer) — geçersiz rol butonu kilitler ve satır bazlı neden gösterir; var olan unvanlar "atlandı". Ayrıştırıcılar saf modüle çıkarıldı (`orgImport.ts`, 7 vitest) — OrgImportModal da aynı modülü kullanır. Yine sıfır yeni backend: mevcut createPosition endpoint'i. Canlı doğrulama: geçersiz rol engeli + temiz giriş önizlemesi UI'dan; **20/20 e2e yeşil (1.7m)**. |
| R-TakımRol Takım yönetim modalı + pozisyon arşivi | ✅ Tamamlandı | 2026-08-14 | Founder direktifi ("toplu takım oluşturma + takım/pozisyon silme; sürekli sıfırlamadan müdahale"). **(1)** Üst bardaki "+ Takım" artık TeamManageModal açar: mevcut takımlar (üye sayısı + Arşivle — T18 archiveUnit, ön koşullar sunucuda), tekli oluşturma (üst birim seçimli) ve TOPLU oluşturma (satır başına "Ad" ya da "Ad, ÜstBirim"; Türkçe karakterli adlardan `slugify` ile slug türetilir — İçerik Ekibi→icerik-ekibi). **(2)** Yeni backend: `POST /org/positions/:id/archive` — pozisyonda aktif/duraklatılmış/draft ajan varsa OrgConflictError (412), yoksa archived_at + katalogdaki `position.updated` {diff:{archivedAt}} (yeni event YOK); SDK `org.archivePosition`, OpenAPI 70 path. Organizasyon → Pozisyonlar tablosuna satır bazlı "Arşivle". **(3)** orgImport.ts'e slugify+parseTeams (+4 vitest, toplam 11). Kabul canlıda: modaldan 2 takım toplu kuruldu (biri Growth altına) → ikisi de modaldan arşivlendi; "Import Müh" test artığı pozisyon gerçekten arşivlendi (200); dolu CEO pozisyonu 412 ile reddedildi. **20/20 e2e yeşil.** |
| R-BirimImport Birim toplu içe aktarma | ✅ Tamamlandı | 2026-08-14 | Founder direktifi. Organizasyon → Birimler kartına "Toplu içe aktar": satır başına `Ad, tür, ÜstBirim` (tür Türkçe/İngilizce: departman·takım·ofis·bölüm; boşsa departman; üst birim ad/slug, dosya içi ileri referans desteklenir — ebeveynler önce kurulur) ya da JSON `[{name, kind, parent}]`. Geçersiz tür butonu kilitler ve satır bazlı neden gösterir; var olanlar atlanır. `parseUnits` saf modülde (+2 vitest, toplam 13); sıfır yeni backend (T18 createUnit). Kabul canlıda: "süper-birim" türü doğru engellendi; "Deneme Departman" + altına "Deneme Alt Takım" tek koşumda 2/2 kuruldu, temizlikte ikisi de arşivlendi. 20/20 e2e yeşil. |
| R-ImportFix Import "duplicate" iki kök neden | ✅ Tamamlandı | 2026-08-14 | Founder saha raporu (2 tur). **(1)** Modallar var-olan kontrolünü mount-anı sorgu verisinden okuyordu; hızlı yapıştır+çalıştırda liste boş → var olanlar sunucuya çarpıp "duplicate org record", ebeveynsiz kalan alt birimler "üst birim bulunamadı". Düzeltme: dört import modalı da mevcut kayıt haritasını run() başında API'den taze çeker; birim 409'u "zaten var — atlandı" sayılıp ebeveyn olarak kullanılabilir. **(2)** Derin kök: `org_units_company_slug_uq` / `positions_company_title_uq` ARŞİVLİ satırları da kapsıyor — arşivlenen birim slug'ı rehin kalıyor, arşivle→yeniden-kur imkânsızdı. Düzeltme: arşivde slug'a `-arsiv-<id8>`, unvana `(arşiv <id8>)` eki (şema/kısıt değişmedi); dev DB'deki 20+1 rehin kayıt tek seferlik serbest bırakıldı. Kabul: Founder'ın birebir listesi (4 departman + 8 alt takım) UI'dan 12/12 kuruldu ve yerinde bırakıldı; çift-koşum "zaten var — atlandı"; 20/20 e2e yeşil. Not: doğrulama sırasında oluşan "undefined" çöp birimi arşivlendi. |
| R-OfisLayout Ofis: org değişiminde canlı plan yeniden kurulumu | ✅ Tamamlandı | 2026-08-14 | Founder raporu ("ajanlar aktif ama ofis boş"). Kök neden: projektör şirket durumunu İLK erişimde kurup bellekte donduruyordu — ofis boş org'la açıldıysa sonradan kurulan birimler oda alamıyor, 24 ajan sayaçta görünüp sahnede girişte yığılıyordu. Düzeltme: **(1)** projektör `department/team/org.unit.created` + `org.unit.archived` + `org.reorg.applied` event'lerinde şirket verisini DB'den tazeler — kayıtlı layout yoksa oto-plan yeniden hesaplanır (layoutVersion+1, snapshotEpoch+1), mevcut avatarlar yeni planda birimlerinin masalarına `desk_assign` yürüyüşüyle taşınır, causeEventId=tetikleyen org event'i (N2 nedensellik korunur; talimat seti DEĞİŞMEDİ). **(2)** `agent.hired` artık payload.orgUnitId ile kendi biriminin odasına oturur (önceden null → giriş). Yeni projektör golden testi senaryoyu sabitler (19/19 server). Kabul canlıda: restart sonrası 5 zone (4 departman + toplantı) / 31 masa, 24 ajan isimleriyle yerleşik (ekran görüntülü). 20/20 e2e yeşil. |
| R-Galaksi Hafıza grafiği 3D galaksi (ADR-021) | ✅ Tamamlandı | 2026-08-15 | Founder direktifi (codebase-memory-mcp benzeri "galaxy"). **Kapsam yalnız görselleştirme** — veri modeline/API'ye sıfır dokunuş; sahne mevcut `memories/graph` yanıtını tüketiyor. **Yerleşim = kapsam hiyerarşisi** (12 §2): company merkez çekirdek, project sarmal kollar, agent dış yörünge; konum düğüm KİMLİĞİNDEN türetiliyor (FNV-1a hash), yani her yenilemede aynı anı aynı yerde ve yeni anı eklendiğinde diğerleri zıplamıyor — kuvvet yerleşiminin veremediği şey buydu. **Performans**: tüm düğümler tek `InstancedMesh`, tüm kenarlar tek `LineSegments`, etiketler yalnız en yakın ~14 düğümde (500 DOM etiketi sahnenin kendisinden pahalıya gelirdi). **Dalga**: bulut 0.02 rad/s döner + her düğüme kimlikten türeyen fazla sinüs ofseti (ortak faz olsa "nefes alan tek yumru" olurdu). **Yeni anı pop'u**: `memory.created` zaten `[companyId,"memories"]` anahtarını geçersiz kılıyor (24 §5) — sahne yeni kimlikleri diff ile bulup 0→1 ease-out + aşma ile büyütüyor; ikinci soket dinleyicisi YOK. Bloom eşiği düşük tutuldu ki yüksek-güven düğümler hâlelensin. **WebGL yoksa** 2D cytoscape grafiğine düşülür (görselleştirme paneli bloke etmemeli). **Kollar gerçekten projeye göre** (Founder onayıyla backend eklendi): graph yanıtındaki düğümler artık `scopeRef` (proje/ajan id'si) ve `scopeLabel` (adı) taşıyor — kol anahtarı `scopeRef` olduğu için bir projenin bütün anıları aynı kolda toplanıyor, farklı projeler ayrı kollara düşüyor; tooltip "kimin anısı"nı söylüyor (`agent: Aylin Vural`). Etiketler iki toplu sorguyla çözülüyor (düğüm başına sorgu 500 gidiş-dönüş olurdu). Migration YOK — sütunlar `memories`'te zaten vardı, eksik olan yalnız yanıta konmalarıydı; OpenAPI yeniden üretildi (71 path, yeni uç yok). **Yan bulgu**: `Card` primitifi `data-testid`'yi DOM'a hiç taşımıyordu, yani eski 2D grafiğin `memory-graph` kimliği baştan beri ölüydü — primitife eklendi. Yeni `e2e/16-memory-galaxy.spec.ts` (canvas + 13 gerçek anı + canlı filtre) ayakta duran yığına karşı yeşil. |
| R-HafızaAudit PROMPT1 (T44+T45 canlı hafıza) denetimi + eksik tamamlama | ✅ Tamamlandı | 2026-08-14 | PROMPT1'in "T44/T45 PENDING" teşhisi ESKİ bir okumaydı — denetim kanıtı: PROGRESS.md'de ikisi de 2026-08-12'den beri DONE; motor uçtan uca bağlı (JetStream `memory-trigger` tüketicisi → task.completed/failed başına `memoryConsolidationWorkflow` → MemoryConsolidationService persist/merge → memory.created/updated event'leri → WS → panel canlı; T45 working-set retrieval bölüm 4-6 skorlamayla; T46 promotion her koşu sonrası `evaluatePromotionsActivity`). Panelin "0 anı" göstermesi motor eksikliği değil: canlı şirkette henüz HİÇ görev DONE/FAILED olmadı (tetikleyici tasarım gereği uç-durum bekler); e2e 11 aynı akışı scripted kanıtlıyor. Prompt'tan kalan GERÇEK eksikler tamamlandı: **(1)** Hafıza Gözlemevi'ne "ZAMAN" sekmesi — gün gruplu kronolojik akış (tip renkli nokta + saat + kapsam/önem; canlı invalidation'la akar); e2e 11'e timeline assert'i eklendi. **(2)** Canlı semantik hafıza hazırlığı: Anthropic embedding sunmadığından seed artık `OPENAI_API_KEY` varsa openai sağlayıcısı + `text-embedding-3-small` (1536 — HNSW partial index'le eşleşir) embedding profili kurar; compose passthrough + .env yer tutucusu. Anahtar yoksa boru hattı belgeli degrade (NULL embedding + semantik şerit atlanır, exact-merge devrede) — davranış değişmedi. |
